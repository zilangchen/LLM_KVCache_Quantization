# This is Chapter 5 Writing

**文档身份**：逐小节写作施工文档  
**当前用途**：记录第五章各小节的写作计划、段落职责、结构边界、图表纪律与删改动作  
**当前状态**：第五章 `5.1–5.5` 已完成施工层冻结；Draft 层仍待接入正文草稿。

---

## 0. 与 story / Draft 文档的分工

本文件与 [thesis_story_20260420.md](/Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/thesis_story_20260420.md) 及 [Chapter 5 Draft.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/Chapter 5 Draft.md>) 的分工如下：

- `story` 文档负责冻结高层主线、章节职责、目录结构与术语边界。
- 本文件负责第五章逐小节施工，直接服务于后续 `thesis/chapters/ch5_conclusion.tex` 的改写。
- `Draft` 文档负责接收外部正文草稿、逐段审校、标记 `采用 / 需改写 / 拒绝`，并沉淀最终可回写正文块。
- 本文件不是正文，不替代 `Draft`；本文件也不是实时工作台，不替代 `thesis_upgrade_live_plan.md`。

### 0.1 维护规则

- 默认按“小节”维护，不做整章无边界扩写。
- 只有写满完整施工模板的小节，才可作为当前阶段的施工层结构与边界裁决 authority。
- 当前已进入完整施工模板的是：
  - `5.1`
  - `5.2`
  - `5.3`
  - `5.4`
  - `5.5`
- 每个已冻结小节至少写清：
  - 小节职责
  - 本节不做什么
  - 最优段落结构
  - 公式安排
  - 图表安排
  - 相对当前稿件的删改动作
  - 完成后的目标印象

### 0.2 术语冻结与正文用语纪律

第五章后续统一遵守 `story` 已冻结的术语与章职能：

- 主线固定为 **behavior-guided framework**
- `INT8 canonical path` 是最干净的验证实例
- `INT4-RoleAlign` 是低比特恢复实例
- allocator / `AutoK` 是统一框架的扩展层
- `regime map` 统一表述为 **跨模型策略适用区间图谱**
- heuristic 必须正面承认为 **强基线**

必须避免：

- 回到旧稿的“主要发现 / 五条经验规律”叙事
- 把第五章写成第四章长版结果节
- 把第五章写成摘要扩写版
- 把第五章写成新 claim 生成器
- 把 `competitive parity` 写成“普适胜利”

### 0.3 跨章引用纪律

第五章是全文收束章，不独立创造新证据。后续写作时必须显式承接以下上游冻结输入：

- [objective.md](/Users/chenzilang/Desktop/LLM_KVCache_Quantization/objective.md)
  - 冻结主线：统一原则、规范实例、扩展层、regime-based interpretation
- [thesis_story_20260420.md](/Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/thesis_story_20260420.md)
  - 冻结 Chapter 5 的 `5.1–5.5` 结构与章级职责
- [This is Chapter 3 Writing.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/This is Chapter 3 Writing.md>) 与 [Chapter 3 Draft.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/Chapter 3 Draft.md>)
  - 提供第三章已冻结的方法主线、实例层级与系统落地边界
- [This is Chapter 4 Writing.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/This is Chapter 4 Writing.md>) 与 [Chapter 4 Draft.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/Chapter 4 Draft.md>)
  - 提供第四章已冻结的 `RQ1–RQ3` 证据链、部署边界与效度边界

职责切割必须锁死：

- `4.7` 负责回答：**第四章已经证明了什么**
- `5.1` 负责回答：**整篇论文最终对 `RQ1–RQ3` 说明了什么**
- `5.2` 负责回答：**这些结论为何具有独立学术价值**
- `5.3` 才正式讨论：**这些价值的边界和限制**
- `5.4` 负责回答：**沿这些已承认边界，下一步最值得往哪里推进**
- `5.5` 负责回答：**整篇论文最终应被怎样理解**

### 0.4 图表、公式与数据门禁

当前 `5.1–5.5` 默认采用最克制的结论章策略：

- **0 张图**
- **0 张表**
- **0 个显示公式**
- 如确需保留极少量行内记号，仅限必要专名

数据与 claims 门禁：

- 模型名、表号、图号、附录号、具体读数不能成为主句
- `5.1` 只允许保留问题级判断与一句条件语气，不承担结果回放
- `5.2` 只允许提升学术价值层级，不新增证据或 claim
- `5.3` 只允许上收已冻结边界，不撤回已成立结论
- `5.4` 只允许把已承认边界转写为未来方向，不把 exploratory 结果写成当前结论
- `5.5` 只允许做全文收口，不再生成新的结论层动作
- official LongBench 对照、same-format compare、四个 regime 节点等 evidence anchor，只能被压缩成高层判断，不得重演第四章证据链

### 0.5 Draft / tex 回写纪律

- 当前阶段禁止直接改 `thesis/chapters/ch5_conclusion.tex`
- 只有在 `Chapter 5 Draft.md` 中被判为 `采用` 且进入“最终可回写正文块”的内容，才允许在后续独立任务中回写
- 本文件当前已可作为 `5.1–5.5` 的施工层接入与边界裁决 authority，但仍不替代 `Draft` 的逐段审核职责

---

## 1. 第五章冻结结构总览（5.1–5.5）

| 节次 | 冻结职责 | 当前状态 | 必须避免 |
|---|---|---|---|
| `5.1` | 面向 `RQ1–RQ3` 的全文级研究结论 | 施工层已冻结 | 不写成第四章长版复述；不与 `4.7` 抢职责 |
| `5.2` | 创新点与学术价值 | 施工层已冻结 | 不回放结果；不复制引言贡献清单；不抬高系统层 |
| `5.3` | 研究局限性 | 施工层已冻结 | 不撤回已成立结论；不把 future work 混进来 |
| `5.4` | 未来工作展望 | 施工层已冻结 | 不把 exploratory 内容写成当前结论 |
| `5.5` | 结语 | 施工层已冻结 | 不重讲 `RQ1–RQ3`；不补结果 |

---

## 2. 第五章 5.1 写作计划

### 2.1 小节标题

保持为：

**5.1 面向 RQ1–RQ3 的研究结论**

这个标题现在不能再回到旧稿的“主要发现”。它的职责不是罗列实验现象，而是把前四章已经完成的理论组织、方法实例和证据链，上收为对三个研究问题的全文级回答。

### 2.2 小节职责

`5.1` 在第五章中的唯一任务是：

> **按 `RQ1 → RQ2 → RQ3` 的顺序，以全文收束层口径给出本研究的最终结论。**

它回答的不是：

- 第四章做了哪些实验；
- 哪张表里谁高谁低；
- 哪个模型是哪一个 case。

它回答的是：

- `RQ1` 最终说明了什么；
- `RQ2` 最终说明了什么；
- `RQ3` 最终说明了什么；
- 这三条答案合起来，如何构成全文的结论层闭环。

### 2.3 本节不做什么

`5.1` 必须明确克制，不承担以下内容：

- 不写成第四章的“结果简报”或“图表回放”
- 不再沿用旧稿“发现一 / 发现二 / 发现三 / 发现四 / 发现五”的编号式结构
- 不重报表号、图号、模型名串联和 case 细节
- 不堆实验数字，尤其不把 `mean Δ=+0.02`、`PPL ≤ 0.15`、`3/5 tasks` 这类读数直接搬入正文主句
- 不把 `INT4 vs KIVI` 的 open question 写进结论层主句
- 不把贡献镜像 `C1 / C2 / C3` 原样复制到本节
- 不把 heuristic 强基线单独拉成一条“发现”；它只能作为 `RQ3` 结论中的结构性组成部分出现
- 不提前进入局限性、未来工作或结语口径

### 2.4 最优总结构

建议将 `5.1` 固定为：

- **1 个短导语**
- **3 个连续回答块**（`RQ1 / RQ2 / RQ3`）
- **1 个节末短收束**
- **0 张图**
- **0 张表**
- **0 个正文显示公式**

也就是说，正文主方案是 **5 个自然段**：

1. 导语段
2. `RQ1` 回答段
3. `RQ2` 回答段
4. `RQ3` 回答段
5. 节末收束段

### 2.5 节首导语

`5.1` 开头建议只写 **1 个短段**，控制在 **2–3 句**。

这一段只做两件事：

1. 说明前四章已经完成理论建构、方法实例化与实证验证；
2. 明确本节将回到第一章提出的 `RQ1–RQ3`，给出全文级答案，而不是重复第四章结果。

**推荐开头句：**

> 前四章已经分别完成问题界定、理论组织、方法实例化与实证验证。本节不再重复图表和模型个案，而是回到第一章提出的 `RQ1–RQ3`，以全文收束层的口径对本文的最终研究结论作集中回答。

### 2.6 公式与图表安排

#### 2.6.1 公式安排

**主方案：不放显示公式。**

第五章 `5.1` 的任务是回答研究问题，不是重新定义方法对象。即便第三章和第四章已经出现过关键公式，这里也不再回放。

#### 2.6.2 图表安排

**主方案：不放图，不放表。**

理由很简单：

- `5.1` 的承重件不是图表，而是三个问题级答案；
- 一旦重新放图表，读者会被拉回第四章的阅读模式；
- Chapter 5 的默认读感应是“抽象、压缩、收束”，而不是“再展示一轮证据”。

### 2.7 第一回答块：RQ1

#### 2.7.1 回答职责

这一回答块只承担一个任务：

> **把 `RQ1` 的答案压成一句全文级判断：behavior-guided calibration 已经在最保守、最可审计的路径上形成稳定、可复现的基础保真闭环。**

#### 2.7.2 本块应写什么

这一段应当包含三层意思：

1. 在规范路径层面，行为引导校准不是停留在理论设想，而是已经形成稳定、可复现的基础保真路径；
2. 这一结论的锚点是 `INT8 规范路径`，它承担的是“原则已闭环”的验证角色，而不是“最终压缩极限”的角色；
3. 受控外部对照只能被写成额外可信度补强，说明主文合成协议没有把方向读错，而不能被拔高为全面真实场景泛化证明。

#### 2.7.3 本块不应写什么

- 不重报 `Qwen2.5-1.5B`、三任务名称和具体分数
- 不直接写 `mean Δ=+0.02`
- 不把受控外部对照写成“真实场景全面泛化已证”
- 不在这里讨论为什么是 KL 而不是 MSE
- 不在这里转入低比特或 allocator

#### 2.7.4 推荐句序

本段建议按 **3 句骨架** 来写：

- **第 1 句：** 直接回答 `RQ1`，行为引导校准已经在规范路径上建立基础保真闭环
- **第 2 句：** 用“受控外部对照方向一致”作为可信度补强，而不是第二主矩阵
- **第 3 句：** 把这一回答收束成一句更高层判断：`RQ1` 证明的是该原则确实存在一个干净、可审计、可复现的落地实例

#### 2.7.5 相对当前稿件的删改动作

- **保留并压缩**：旧 `5.1` 中“INT8 canonical path 的闭环证据”这一块
- **吸收并降调**：把受控外部对照并入本段的第 2 句，只保留“方向一致”的作用
- **删掉**：旧稿围绕 `+0.02` 展开的数字性描述
- **删掉**：任何“INT8 量化很容易”式的解释性废话

#### 2.7.6 写完后的目标印象

读完这一段，评委脑子里应只留下这一句：

> **`RQ1` 的答案是：behavior-guided calibration 不是抽象口号，而是已经在最保守位宽上形成了一个干净、可审计、可复现的基础保真实例。**

### 2.8 第二回答块：RQ2

#### 2.8.1 回答职责

这一回答块只承担一个任务：

> **把 `RQ2` 的答案压成一句全文级判断：behavior-guided 原则不能机械地下推到对称 `INT4`，但在当前 role-aware 非对称格式与已验证设定下，可以恢复为一条可审计的低比特实例。**

#### 2.8.2 本块应写什么

这一段应当包含四层意思：

1. 对称 `INT4` 不是温和退化，而会出现架构依附性的阶跃失效；
2. 这种低比特失效的主导触发侧来自 Key 精度下降，而不是 `K/V` 均匀同步退化；
3. 在当前 role-aware 非对称格式与已验证设定下，low-bit path 可以从“崩塌”恢复为“可用”；
4. 本段只需把这一恢复写成低比特实例的成立，不承担更细的 format–calibration 解耦解释。

#### 2.8.3 本块不应写什么

- 不重报 `Needle 100/100`、`|ΔPPL|≤0.15` 这类读数
- 不把 `INT4 vs KIVI` 的 open question 写成这一段的主句
- 不重新讲 `MixedKV`、`K-only / V-only / K4V8` 的诊断配置
- 不重述 `RoleAlign` 的方法定义
- 不在这里展开 `format–calibration` 的彻底解耦要求，那属于局限性或未来工作

#### 2.8.4 推荐句序

本段建议按 **4 句骨架** 来写：

- **第 1 句：** 先回答低比特不能机械继承 `INT8` 成功
- **第 2 句：** 点出“架构依附性 + Key 主导触发”这两个核心判断
- **第 3 句：** 写出当前 role-aware 非对称格式与已验证设定下恢复了 low-bit path
- **第 4 句：** 用一句学术化判断收束：这里要成立的是一个可审计的低比特实例，而不是更广的低比特普遍规律

#### 2.8.5 相对当前稿件的删改动作

- **保留并重写**：从 `Chapter 4` 的 `RQ2` 结论与 discussion 中抽取“对称失效 → Key 触发 → RoleAlign 恢复”的正向结论链
- **整体移出**：旧 `5.1` 中“INT4 vs KIVI 的 open question”整块，不再作为 `5.1` 正式结论
- **删掉**：任何需要靠具体表格、具体对比数字才能成立的句子
- **删掉**：把“格式一阶、校准二阶组织增益”直接写成 `5.1` 主结论的句子；这类解释统一下放到讨论层或局限层

#### 2.8.6 写完后的目标印象

读完这一段，评委脑子里应只留下这一句：

> **`RQ2` 的答案是：behavior-guided 原则在对称 `INT4` 上会遭遇架构依附性的 Key 主导失效，但在当前 role-aware 非对称格式与已验证设定下可以被恢复为一条可审计的低比特实例。**

### 2.9 第三回答块：RQ3

#### 2.9.1 回答职责

这一回答块只承担一个任务：

> **把 `RQ3` 的答案压成一句全文级判断：由校准链路产出的行为敏感度画像不仅可以服务参数选择，也能为跨模型预算分配提供结构化读法，并在当前证据范围内揭示 family / scale / task 共同决定的策略适用区间。**

#### 2.9.2 本块应写什么

这一段应当包含四层意思：

1. behavior sensitivity profile 不只服务于 calibration，也确实能够上升为 allocator 的组织依据；
2. allocator 的真实贡献不是证明某个单一 policy 普适最优，而是揭示不同模型会落入不同的策略适用区间；
3. 这张图谱应以一句压缩长句概括多类代表性区间，包括早层保护回救区间、中等规模共识区间、高性能簇共存而无稳定最优，以及 AutoK 的最显著正向案例；
4. heuristic 作为强基线必须被正面写成结构性观察：它说明某些 regime 的可行层集本来就很窄，而不是对 behavior-guided 主线的否定。

#### 2.9.3 本块不应写什么

- 不把四个模型重新展开成四个 mini-case
- 不逐项重报 `k=1 / 11 / 7 / 3` 这类配置细节
- 不写 `same-order INT4 budget band` 的技术口径细节
- 不写成“某一个 allocator 胜出”的单一赢家叙事
- 不在这里长篇解释 heuristic 为什么强，那属于后面的学术价值提升和局限边界正式化

#### 2.9.4 推荐句序

本段建议按 **4 句骨架** 来写：

- **第 1 句：** 直接回答 `RQ3`，行为敏感度画像能够支撑预算分配
- **第 2 句：** 点出结论形式不是 universal winner，而是跨模型策略适用区间图谱
- **第 3 句：** 用一个压缩长句概括多类 regime node
- **第 4 句：** 正面承认 heuristic 强基线，并把它写成结构性观察而不是方法失败

#### 2.9.5 相对当前稿件的删改动作

- **保留并压缩**：旧 `5.1` 中关于 regime map 的主体材料
- **保留但降格重组**：旧稿中 heuristic 强基线的独立发现，不再单列，而改写为 `RQ3` 结论的一部分
- **删掉**：旧稿围绕 per-model winner 的逐点展开
- **删掉**：任何暗示严格同预算 formal winner 已被证明的句子

#### 2.9.6 写完后的目标印象

读完这一段，评委脑子里应只留下这一句：

> **`RQ3` 的答案是：行为敏感度画像不仅能驱动预算分配，而且能够把跨模型差异组织成一张可命名、可解释的策略适用区间图谱；heuristic 的强并不是例外，而是这张图谱的一部分。**

### 2.10 节末短收束

`5.1` 末尾建议再补 **1 个短段**，只做一件事：

> **把三个问题级答案收回到“全文已经完成什么”这一层，并自然转入 `5.2 创新点与学术价值`。**

**推荐节末引桥句：**

> 综上，本文对 `RQ1–RQ3` 的回答并不落在若干分散结果之上，而是共同支撑一个统一判断：attention behavior 可以作为贯通校准与分配的组织原则。基于这一结论，下一节将进一步讨论，这些研究结论为何具有独立的创新点与学术价值。

### 2.11 5.1 相对当前稿件的总手术动作

`5.1` 的总改法可以概括为 6 条：

1. **把旧标题“主要发现”正式改为“面向 RQ1–RQ3 的研究结论”。**
2. **把旧开头“给出 5 条经验规律”整段删除。** 新 `5.1` 不再按经验规律条数组织。
3. **把旧“发现四：INT8 canonical path”压缩改写为 `RQ1` 回答块。**
4. **把旧“发现三：INT4 vs KIVI 的 open question”整体移出 `5.1`。** 它属于局限性 / 未来工作接口，不属于正式结论。
5. **把旧“发现二 + 发现一”重组为 `RQ3` 回答块。** 其中 regime map 是主句，heuristic 强基线作为结构性观察并入其中，但不再逐模型点名展开。
6. **旧 `C1/C2/C3` 只允许拆解并重写吸收到 `5.2` 的“理论组织 + 方法实例 + 实证图谱”三层，不得镜像搬运。**

### 2.12 5.1 完成后的目标印象

读完整个 `5.1`，评委脑子里应该只留下这一句话：

> **本文已经对三个研究问题给出清晰而克制的最终答案：behavior-guided 原则先在规范路径上闭环，再在当前低比特设定下被恢复，并进一步为跨模型预算分配提供结构化读法；因此，第五章后续真正要做的，不再是重复证据，而是提升其创新点、边界与学术价值。**

---

## 3. 第五章 5.2 写作计划

### 3.1 小节标题

保持为：

**5.2 创新点与学术价值**

这个标题不用再改。它比“主要贡献”更成熟，因为这里不是把引言的贡献点照抄一遍，而是要在全文已经完成之后，回答：

> **这项研究的独立学术价值到底成立在什么层面。**

### 3.2 小节职责

`5.2` 在第五章中的职责只有一个：

> **把全文已经完成的工作，从“结论层”提升到“学术价值层”。**

它必须完成三层升维：

1. **理论组织层**
   `attention behavior` 不只是一个校准 trick，而是把 calibration 与 allocation 组织到同一主线下的统一原则。
2. **方法实例层**
   `INT8 规范路径`、`INT4-RoleAlign`、allocator / `AutoK` 不是彼此分散的小技巧，而是同一框架在不同 bit-width 与不同决策层上的实例化与扩展。
3. **实证图谱层**
   本文的经验价值不只在于“某个方法比某个 baseline 高几分”，而在于给出一张**跨模型策略适用区间图谱**，并把 heuristic 的强基线地位也解释为结构观察的一部分。

这节与 `5.1` 的区别必须非常清楚：

- `5.1` 回答：**最终得出了什么结论**
- `5.2` 回答：**这些结论为什么具有独立学术价值**

这节与 `5.3` 的边界也必须清楚：

- `5.2` 只谈价值成立的层次
- `5.3` 才正式谈这些价值的边界和限制

### 3.3 本节不做什么

`5.2` 必须明确克制，不承担以下内容：

- 不把 `1.5 研究内容、技术路线与主要贡献` 原封不动复制过来
- 不再按 `RQ1 → RQ2 → RQ3` 顺序重新回答问题，那是 `5.1` 的职责
- 不重述第四章各表各图，不重讲各模型 case
- 不堆实验数字，不写 `∆=+0.02`、`赢 3/5 task`、`top-3 within 3.5%` 这类结果章语言
- 不把 `Triton`、融合核、部署效率单独抬成与统一原则并列的“主创新”
- 不在这里引入局限性、未来工作或开放问题
- 不写成“本文做了很多工作”的流水账

一句话说，`5.2` 不是**贡献清单重放**，而是**贡献层级重构**。

### 3.4 最优总结构

建议将 `5.2` 固定为：

- **1 个短导语**
- **3 个核心价值段**
- **1 个短收束段**
- **0 张图**
- **0 张表**
- **0 个显示公式**

正文主方案建议采用：

> **连贯学术段落，不采用“创新点 1 / 2 / 3”列表式正文。**

原因很简单：

- latest story 已经把三层价值写死
- 当前 PDF 旧 `5.1` 里已有 `C1 / C2 / C3` 镜像总结，可作为原料，但正式正文若继续写成编号条目，会太像引言贡献节的重复版
- `5.2` 更适合写成**一条从组织原则 → 方法实例 → 实证图谱**的连续论证链

### 3.5 节首导语

这一节开头建议只写 **1 个短段**，作用只有一个：

> 把 `5.1` 的“研究结论”切换成 `5.2` 的“学术价值判断”。

推荐开头句可以直接按这个方向写：

> 如果说前一节回答的是本文围绕 `RQ1–RQ3` 最终得出了什么结论，那么本节进一步要回答的是：这些结论为何不应被理解为若干分散实验现象，而应被看作一个具有统一组织原则、分层实例链与跨模型解释能力的研究工作。

### 3.6 第 1 段：理论组织层的创新点与学术价值

#### 3.6.1 这一段要做什么

必须明确把本文的第一层价值写成：

> **attention behavior 作为 calibration 与 allocation 的统一组织对象。**

这层价值不能只写成“提出了一个 framework”，而要写清它到底把什么统一了：

- 第三章从注意力近似误差传播分析出发，把量化损伤的关注点从张量数值误差推到注意力行为；
- 在这个基础上，校准层与分配层不再是两个互不相关的优化问题，而是共享同一条 `behavior-guided` 主线；
- 因此，本文的理论贡献不在于再提出一个局部的低比特技巧，而在于给出一个能够同时组织 canonical calibration、low-bit path 与 allocation extension 的统一组织原则。

#### 3.6.2 这一段不该怎么写

- 不重讲 `3.1` 的 telescoping 分解公式
- 不再解释 KL 公式
- 不写成“本文首次发现 Key 比 Value 更重要”这种过强口号
- 不拿这一段去做文献综述式对比

#### 3.6.3 这一段最该留下的读感

评委读完这一段，应当感觉到：

> 这篇论文真正交付的不是一串 ad hoc tricks，而是一个把行为保持原则系统化的组织框架。

### 3.7 第 2 段：方法实例层的创新点与学术价值

#### 3.7.1 这一段要做什么

这一段应当把方法层价值压成三类实例：

1. **`INT8 规范路径`**
   它不是普通 baseline，而是统一原则在最保守位宽上的**干净验证实例**。
2. **`INT4-RoleAlign`**
   它不是从实验图里临时长出的技巧，而是统一原则在 low-bit 场景下对 `K/V` 角色差异的一条实例化路径。
3. **allocator / `AutoK`**
   它们不是与 calibration 并列争主轴的第二套方法，而是由同一 `behavior sensitivity profile` 自然延伸出的高层扩展。

这一段真正要抬起来的学术价值是：

- 本文不只提出“一个方法”，而是给出**从规范路径到激进路径、从校准层到预算层**的一条分层实例链；
- 这些实例共享离线校准产物、共享审计接口、共享行为组织原则，因此它们构成的是**统一方法族**，不是若干平行模块的堆叠。

这里若需要提及系统层，只能写成**可审计落地价值**，不能把系统层抬成与统一原则并列的主创新。

#### 3.7.2 这一段不该怎么写

- 不展开 `per-channel K + per-token V` 的公式
- 不细讲 `RoleAlign vs KIVI-style` 的公平比较细节
- 不重讲 `Triton` 核函数与部署实现
- 不给出具体任务分数

#### 3.7.3 这一段最该留下的读感

评委读完这一段，应当感觉到：

> 这项工作的方法层价值，在于它把统一原则做成了可落地、可审计、可扩展的路径家族，而不是只提出一个单点方案。

### 3.8 第 3 段：实证图谱层的创新点与学术价值

#### 3.8.1 这一段要做什么

这一段的中心句应该是：

> **本文的实证价值不只在于比较策略优劣，而在于识别并命名一张跨模型策略适用区间图谱。**

具体要压出四层意思：

1. allocator 的跨模型结果并没有导向单一 universal winner；
2. 相反，当前证据支持的是 `family / scale / task-dependent regimes`；
3. 因此，本文贡献了一种更高层的读数能力：不是只回答“谁最好”，而是回答“什么策略在什么范围内更容易落在高性能区间”；
4. heuristic 作为强基线，不应被写成本文失利，而应被解释为这张图谱本身的一部分，它说明某些 regime 的可行层集确实很窄，而本文的价值在于识别这些窄区间何时存在、何时失效。

#### 3.8.2 建议吸收的旧稿素材

当前 PDF 旧 `5.1` 里有一句特别承重的话，值得保留其逻辑、但必须重写为更学术的口气：

> behavior-guided allocator 的真实价值从“击败弱对手”升级为“揭示结构分区”。

这句话不要原封不动照搬，但它的逻辑应成为本段的收束核心。

#### 3.8.3 这一段不该怎么写

- 不逐个复述 Mistral / 3B / 8B / 14B case
- 不列每个 regime 的具体表号
- 不报任务分数
- 不把 heuristic 写成尴尬补充说明

#### 3.8.4 这一段最该留下的读感

评委读完这一段，应当感觉到：

> 这篇论文的经验价值，不在单点跑分，而在把原本杂乱的跨模型现象组织成了一张可命名、可解释、可讨论的策略适用区间图谱。

### 3.9 第 4 段：短收束

这一段只需要 **1 个短段**，作用是把前面三层压成一句总判断，并为 `5.3` 留出空间。

建议收束逻辑写成：

- 统一原则回答“为什么这不是零散技巧”
- 方法实例回答“为什么这不是只停留在概念层”
- 实证图谱回答“为什么这项工作不只是方法比较，而具备更高层解释力”

推荐收束句方向：

> 因而，本文的创新点与学术价值不应被理解为若干孤立方法结果的叠加，而应被理解为：以 attention behavior 为统一组织原则，在规范路径、低比特实例与跨模型策略适用区间图谱三个层面形成了一条从理论组织到经验解释的连续研究链。

### 3.10 公式安排

**结论：不放显示公式。**

原因很明确：

- `5.2` 讨论的是价值层级，不是方法定义
- 第三章已经完整承担公式职责
- 第五章再放公式，会把章节重心从“收束”拉回“推导”

最多允许的，也只是极少量行内专名：

- `INT8 canonical path`
- `INT4-RoleAlign`
- `AutoK`
- `behavior sensitivity profile`

### 3.11 图表安排

**主方案：0 图，0 表。**

我不建议在 `5.2` 放任何 summary table，原因有四个：

1. latest story 已经把 `5.2` 冻结为价值升维节，不是总览表节
2. 当前 PDF 旧 `5.1` 已有可拆用的 `C1 / C2 / C3` 文本材料，不缺信息密度
3. 一旦放表，很容易把本节写回“贡献清单”
4. Chapter 5 默认不放图表，这里应继续遵守

### 3.12 相对当前稿件的删改动作

这一节相对当前 PDF 旧 Chapter 5 的手术动作，建议固定成下面这组：

#### 3.12.1 保留

保留旧 `5.1` 里这块最有价值的原料：

- **“贡献总结（C1 / C2 / C3 镜像 §1.4）”** 这一整块结构
- **“从击败弱对手升级为揭示结构分区”** 的逻辑骨架
- **“Heuristic is a strong 基线（结构观察，非方法失败）”** 这层判断

#### 3.12.2 必须改写

以下内容不能原封不动进入 `5.2`，必须升维重写：

- 旧 `C1 / C2 / C3` 的条目式写法
- 旧稿里所有带“发现一 / 二 / 三 / 四 / 五”的口气
- 旧稿里所有明显属于结果章的词组，例如：
  - `mean ∆=+0.02`
  - `100% Needle`
  - `wins 3 of 5 tasks`
  - `top-3 policy 挤在 3.5% relative gap 内`

这些都要从“数字胜负句”改写成“学术价值句”。

#### 3.12.3 直接删掉

以下材料不应进入 `5.2`：

- 表号、图号、附录号
- provenance pin、`ddada19`、md5-locked 这类实验治理语言
- 逐模型 case 列举
- 部署效率结果
- 任何“未来可以……”的句子

#### 3.12.4 与 `5.1` 的拆流规则

旧 `5.1 主要发现` 里的内容，进入 `5.2` 时必须遵守下面的拆流：

- **属于问题回答的** → 留在新 `5.1`
- **属于价值升维的** → 进入新 `5.2`
- **属于条件边界的** → 移到新 `5.3`
- **属于未来方向的** → 移到新 `5.4`

这一步一定要干净，否则 `5.2` 会变成 `5.1` 的重复版。

### 3.13 完成后的目标印象

读完 `5.2`，评委脑子里应该只留下这一句话：

> **这篇论文的价值，不在于又提出了一个局部 INT4 技巧，而在于以 behavior-guided 原则把规范路径、低比特实例与跨模型策略适用区间图谱组织成了一条可解释、可审计、可扩展的研究路径。**

---

## 4. 第五章 5.3 写作计划

### 4.1 小节标题

保持为：

**5.3 研究局限性**

这个标题已经冻结，不再改。它比旧 PDF 里的“局限性”更正式，因为这里不是简单列“不足”，而是对全文结论的**适用范围**与**外推边界**做正式界定。

### 4.2 小节职责

`5.3` 的唯一任务是：

> **把第四章已经被证明成立、同时也已经被限定边界的结论，统一转写为全文层面的研究局限性说明。**

它必须做到三件事：

1. **承认边界，但不自我拆台。**
   这里要写“本文的结论成立到哪里为止”，而不是写成“本文其实什么都没证明”。
2. **承接第四章，而不是另起一套。**
   这节以 `4.6.4` 已冻结的边界为基础，并在全文收束层重组为本节的四组研究限制。
3. **为 `5.4` 留接口，但不抢写未来工作。**
   也就是说，`5.3` 只负责说明“哪里仍未被完全回答”，不负责展开“以后要做什么”。

这节与 `5.1`、`5.2` 的区别必须很清楚：

- `5.1` 回答：**本文得出了什么结论**
- `5.2` 回答：**这些结论为什么有学术价值**
- `5.3` 回答：**这些结论目前成立在什么边界内**

### 4.3 本节不做什么

这一节必须明确克制，不承担以下内容：

- 不把第四章结果再复述一遍
- 不重新比较 `INT4-RoleAlign`、`KIVI-style`、`AutoK` 谁强谁弱
- 不把 `5.3` 写成“未来工作展望”的前置长清单
- 不引入新文献、新实验、新 appendix 细节
- 不继续沿用旧稿里偏 memo 化的表达，例如 `Open Question 1`、脚本名或实验目录名、大段 cost/benefit 数字堆叠
- 不把 low-bit 结果重新写回“INT4 整体失败”的旧叙事；freeze-ready Chapter 4 已经把它收束为：**对称 `INT4` 有 cliff，但 role-aware 路径可恢复；其结论是条件性的，不是整体否定性的**

一句话说，`5.3` 不是**道歉节**，而是**边界节**。

### 4.4 最优总结构

建议将 `5.3` 固定为：

- **1 个短导语**
- **4 个核心限制段**
- **1 个短收束段**
- **0 张图**
- **0 张表**
- **0 个正文显示公式**

正文主方案建议采用：

> **4 个连贯自然段来承载 4 组主限制，不采用“局限性 1/2/3/4”的条目化正文。**

但在施工文档内部，仍按 4 组限制分别冻结，方便后续审校与最小替换。

### 4.5 节首导语

这一节开头建议只写 **1 个短段**，功能只有一个：

> 把 `5.2` 的“学术价值判断”切换成 `5.3` 的“适用域与边界说明”。

推荐开头逻辑：

> 如果说前一节强调的是本文在理论组织、方法实例与实证图谱层面的创新点与学术价值，那么本节需要进一步说明的是：这些价值并非脱离评测协议、比较口径、系统环境与数据溯源条件而无条件成立，而是被限定在一个当前已清楚界定的适用域之内。

这个导语要短，不能提前把四条局限全写完。

### 4.6 第 1 段：评测协议与真实外推边界

这一段应当承接 `4.6.4` 的第一类边界，同时吸收旧 PDF `5.2` 的第一条局限原料。

#### 4.6.1 这一段要做什么

必须写清两层意思：

1. **正文主矩阵的评测协议边界**
   正文主结果主要建立在 LongBench 风格合成基准任务上，官方 LongBench 真实数据对照只是在 `INT8 canonical path` 上提供了一块范围受控的 sanity check，因此它支持的是“方向一致”，而不是“真实场景全面泛化已被充分证明”。
2. **质量度量本身的边界**
   PPL 不能被当成长上下文性能的唯一代表性指标，因此本文才并列使用了 Needle、RULER 与任务级指标。换句话说，这一章已经尽量避免单指标偏差，但真实任务分布与更广场景覆盖仍然有限。

#### 4.6.2 这一段不该怎么写

- 不要把这段写成“本文数据不真实，所以结论不可靠”
- 不要再出现旧稿那种大段 benchmark 列举
- 不要把它写成“我们未来还要测很多东西”的半 future-work 段

#### 4.6.3 这一段最该留下的读感

评委读完应当感觉到：

> 本文已经给出可信的协议内证据，也做了有限真实数据补强；但它对真实场景外推的口径是克制而清楚的。

### 4.7 第 2 段：比较口径与预算可比性边界

这一段必须成为 `5.3` 的承重段之一，因为它关系到 allocator 线结论能否被过度解释。

#### 4.7.1 这一段要做什么

这一段只需要把一个边界锁死：

> **本文关于 allocator 与跨模型策略适用区间图谱的主要结论，建立在同量级 INT4 预算带内的经验可比性上，而不是严格 matched-budget formal compare 上。**

这里要进一步展开两层意思：

1. `4.4` 可以支持：
   - regime map exists
   - per-model structural footprint
   - heuristic 是强基线这一结构判断
2. `4.4` 不能支持：
   - 某个单一 policy 在严格同预算下被形式化证明更优
   - allocator 相对 `KIVI-style` 已完成 matched-budget 的正式胜负裁决

也就是说，旧 PDF `5.2` 中关于 matched-budget formal compare 仍待完成的诚实边界，必须被保留，但要从“future work note”升级成“全文主张的比较口径限制”。

#### 4.7.2 这一段不该怎么写

- 不要把 matched-budget 缺口写成“本文 allocator 线不成立”
- 不要把探索性对比里的具体比率、百分比、脚本名搬到正文里
- 不要把这段写成技术细节备忘录

#### 4.7.3 这一段最该留下的读感

评委读完应当感觉到：

> allocator 线的结构发现是成立的，但其“形式化严格可比性”仍需要后续更干净的 matched-budget 设计来补全。

### 4.8 第 3 段：结论条件性与机制解耦边界

这一段要把“论文不是 universal winner 叙事”写清楚，同时把 old `INT4 vs KIVI` 的耦合边界正式学术化。

#### 4.8.1 这一段要做什么

必须压出两层边界：

1. **family / scale / task-dependent 的条件性边界**
   本文的经验图谱明确表明，不存在 across-model 的 universal best strategy；因此，许多结论本来就是条件性的，必须读成“在什么模型、什么任务、什么预算带内更容易成立”。
2. **机制解耦仍不充分的边界**
   `INT4-RoleAlign` 与 `KIVI-style` 的比较是 same-format compare，这使得我们能够较干净地比较 calibration philosophy，但还不能把“格式贡献”与“校准贡献”彻底做成完全正交的 isolate。当前证据支持的是 **同格式条件下的组织增益与实例化价值**，而不是 **已经完全解耦的一阶因果归因**。

#### 4.8.2 这一段不该怎么写

- 不要再讲每个模型的 case
- 不要写成“RoleAlign 优势很小，所以贡献有限”
- 不要写成“heuristic 强基线说明 behavior-guided 没价值”

#### 4.8.3 这一段最该留下的读感

评委读完应当感觉到：

> 本文已经识别出稳定的条件性结构，但并不把这些条件性发现夸张成普适规律或单因素定论。

### 4.9 第 4 段：系统实现、动态决策与时间有效性边界

这一段建议把旧稿剩余两类局限合并写成一组更成熟的“实现与适用域边界”。

#### 4.9.1 这一段要做什么

建议写三层意思：

1. **系统环境绑定**
   部署效率与性能交叉边界结论严格绑定于当前 `NVIDIA H20`、固定软件栈、`batch=1`、贪婪解码与当前 backend 组合，因此它们是经验边界，不是硬件无关定律。
2. **动态决策层仍未成熟**
   当前 `AutoK` 主要还是 model-level 的静态 proposer；更细粒度的 prompt-adaptive routing 仍只提供弱改善或混合信号，尚不足以被写成主结论层成果。
3. **数据与架构时间有效性**
   clean-provenance pin 保证的是当前结果的可审计性，而不是对未来架构或新长上下文注意力变体的自动泛化；当架构空间变化时，适用区间图谱需要重新验证，而不是被默认继承。

#### 4.9.2 这一段不该怎么写

- 不要把 appendix 编号、实验目录名、具体 CSV 路径搬进正文
- 不要把探索性单任务数值保留在局限性正文里
- 不要在这里提前展开 `5.4` 的未来方向细节

#### 4.9.3 这一段最该留下的读感

评委读完应当感觉到：

> 本文的方法与图谱已经形成可审计的当前版本，但更细粒度的动态决策与跨平台部署规律仍处在边界之外。

### 4.10 节末短收束

这节最后建议只用 **1 个短段** 收住，作用是：

- 不重复四条局限
- 只把四条局限统一解释为“适用域说明”
- 轻轻桥到 `5.4`

推荐收束句逻辑：

> 因而，本文的研究局限性不应被理解为对既有结论的撤回，而应被理解为：当前关于规范路径保真、低比特恢复与跨模型策略适用区间图谱的结论，已经在评测协议、比较口径、系统环境与时间外推范围上被清楚限定；下一节只沿其中可直接转写为研究路线的三类方法性边界继续扩展其适用域，其余边界在本节保留为适用域说明。

### 4.11 公式安排

**结论：不放显示公式。**

原因很简单：

- 这一节是边界说明节，不是方法定义节
- 第三章已经完成公式职责
- 第五章再放公式，只会把“收束”拉回“推导”

最多允许少量行内专名：

- `INT8 canonical path`
- `INT4-RoleAlign`
- `KIVI-style`
- `AutoK`

### 4.12 图表安排

**主方案：0 图，0 表。**

这一节不需要 summary table。因为这里最重要的是**口径清晰**，不是再做一张“limitations checklist”。一旦加表，很容易把 `5.3` 写成研究笔记式附录，而不是成熟的 conclusion chapter 小节。

### 4.13 相对当前稿件的删改动作

这一节相对当前 PDF 旧 `5.2 局限性`，建议做下面这组手术动作。当前旧稿确实已经有 5 条可用原料，但需要重组，不应直接照抄。

#### 4.13.1 保留

保留旧稿中的这 5 类核心材料：

- 实证与真实外推范围有限
- `INT4-RoleAlign` vs `KIVI-style` 的解耦仍不充分
- `AutoK` 仍以静态 proposer 为主
- clean-provenance 的可审计性不等于未来架构泛化
- matched-budget formal compare 仍未充分完成

#### 4.13.2 压缩重组

把旧 `5.2` 的 5 条压成新的 **4 组学术边界**：

- **评测协议与真实外推边界**
- **比较口径与预算可比性边界**
- **结论条件性与机制解耦边界**
- **系统实现、动态决策与时间有效性边界**

也就是说，新稿不再机械保留“5 条 limitations”的外观，而保留其真正承重内容。

#### 4.13.3 必须改写

以下内容必须升格改写：

- `诚实地列出本文研究的 5 条 limitations`
- `Open Question 1`
- `future work 见 §5.3`
- 附录编号、脚本名、实验目录名
- 探索性对比中的具体数值比率与百分比堆叠

这些都要从“笔记口吻”改成“学术边界口吻”。

#### 4.13.4 直接删掉

默认删掉这些材料，不进 `5.3` 正文：

- appendix 编号串联
- `rolealign_allocator_auto_eqmem` 这类实现名
- `1.5–1.8×`、`1–3%`、`16% cell` 这类探索性数字
- prompt-adaptive 的 task 细部表现

这些可以作为后面判断 `5.4` 是否要吸收的备料，但不应该留在 `5.3` 主文。

### 4.14 完成后的目标印象

读完 `5.3`，评委脑子里应该只留下这一句话：

> **这篇论文的结论是成立的，但作者非常清楚它们成立在何种协议、预算、系统与外推边界之内；这种边界意识强化了论文可信度，而不是削弱它。**

---

## 5. 第五章 5.4 写作计划

### 5.1 小节标题

保持为：

**5.4 未来工作展望**

这个标题已经冻结，不再改。它和 `5.3 研究局限性` 是一前一后的关系：

- `5.3` 说明：**当前结论成立到哪里为止**
- `5.4` 说明：**沿这些已知边界，下一步最值得往哪里推进**

### 5.2 小节职责

`5.4` 的唯一任务是：

> **把 `5.3` 已经正式承认的边界，转写成三条最有研究价值的后续方向。**

它必须完成三件事：

1. **未来方向必须直接长在当前主线之上。**
   也就是继续围绕 `behavior-guided framework`、`INT4-RoleAlign`、allocator / `AutoK` 与 regime map 往前推，而不是突然跳到另一套问题。
2. **未来方向应从 `5.3` 已识别边界中，择其最值得继续推进的三类方法性边界展开，而非对全部限制做逐条镜像。**
   也就是：
   - prompt 级信号已有弱提示，但未形成成熟 routing
   - `role-aware allocator` 有接口、有局部信号，但尚未成熟
   - allocator 线仍缺 matched-budget formal compare
3. **未来方向必须少而重。**
   latest story 已经把本节重点锁为 3 条，所以主方案就只保留这 3 条，不再自行扩展成 5 条、6 条。

这节与 `5.3` 的边界必须非常清楚：

- `5.3` 是**边界说明**
- `5.4` 是**沿边界外推的研究路线**

这节与 `5.5` 的边界也必须清楚：

- `5.4` 仍要展开方向
- `5.5` 只做一段收束，不再展开任何方向

### 5.3 本节不做什么

这一节必须明确克制，不承担以下内容：

- 不写成长清单式“未来可以研究很多很多东西”
- 不重新定义新方法主线
- 不把 appendix 里的 exploratory branch 重新抬成第四主贡献
- 不写新的实验设计细节、脚本名、目录名、附录号
- 不保留旧稿中的具体 exploratory 数字，如单任务 `+0.40`、`1.5–1.8×`、`16% cell` 之类
- 不在这里重新做局限性陈述，那些已经属于 `5.3`
- 不把本节写成“对工业实践的启示”或“开放问题总表”

一句话说，`5.4` 不是**研究愿望池**，而是**主线的受控延伸节**。

### 5.4 最优总结构

建议将 `5.4` 固定为：

- **1 个短导语**
- **3 个核心方向段**
- **1 个短收束段**
- **0 张图**
- **0 张表**
- **0 个正文显示公式**

正文主方案建议采用：

> **连贯自然段，不再使用“未来工作一 / 二 / 三”的条目式标题。**

也就是说，施工文档里我们把它拆成 3 个方向来冻结；但正式正文里，更建议写成 3 个连续学术段，每段首句显式点出该方向即可。这样会比旧 PDF 那种“未来工作一、二、三”更成熟，也更像 conclusion chapter 的正式收束。

### 5.5 节首导语

这一节开头建议只写 **1 个短段**，作用只有一个：

> 把 `5.3` 的“边界说明”切换成 `5.4` 的“延伸方向”。

推荐开头逻辑写成：

> 前一节已经说明，本文当前关于规范路径保真、低比特恢复与跨模型策略适用区间图谱的结论，仍受到评测协议、比较口径与系统成熟度的边界约束。基于 `5.3` 中最值得继续推进的三类方法性边界，后续研究较值得优先推进的方向并不是另起新的问题域，而是沿 prompt 级策略选择、角色感知预算机制与严格预算可比性三个层面，继续扩展 behavior-guided 框架的适用域。

这个导语要短，不要提前把三条方向写成摘要。

### 5.6 第 1 段：Per-prompt routing

这一段是新 `5.4` 的第一个方向，也最容易被写偏。

#### 5.6.1 这一段要做什么

必须把这一方向写成：

> **从 model-level 固定 proposer，推进到 prompt-level 条件化策略选择。**

这里真正要压出的逻辑有三层：

1. 当前 `AutoK` 主要还是 **model-level / profile-guided proposer**
2. 现有 prompt-adaptive 线索只呈现 **弱改善或 mixed signal**，说明 prompt 级 routing signal 可能存在，但还远未成熟到能写成本文主结论
3. 因而，当前 prompt-level signals 仅提供了弱改善或 mixed signal，因此 per-prompt routing 更适合作为值得优先检验的开放方向，而不是当前策略图谱的直接下推

#### 5.6.2 这一段最适合怎么写

建议把它写成：

- 先承认：当前固定 `k` / model-level proposer 已能给出有价值的 regime readout
- 再指出：不同 prompt 之间的 task-domain 差异，提示 static policy 可能仍过于粗糙
- 最后落到：未来可优先检验将输入 prompt 的表示与当前策略适用区间图谱联系起来，是否能够形成稳定、可迁移的 per-prompt routing 机制

#### 5.6.3 这一段不该怎么写

- 不要保留旧稿里具体的 appendix 编号
- 不要保留单任务的具体分数
- 不要写成“prompt-adaptive 已经证明更优，只是还没做完”
- 不要把 selector 实现细节写进正文

#### 5.6.4 这一段最该留下的读感

评委读完应当感觉到：

> 本文已经识别出策略适用区间的结构信号，而一个优先检验的下一步方向，是把这种结构读数从模型级推进到 prompt 级，而不是停留在固定预算建议上。

### 5.7 第 2 段：更成熟的 role-aware allocator

这一段是主线内部延伸，不是另开新方向。

#### 5.7.1 这一段要做什么

必须把它写成：

> **把当前仅具接口形态的 K/V 角色感知预算扩展，推进为更成熟的联合分配机制。**

这里要压出的逻辑是：

1. 第三章其实已经给出了 `role-aware allocator` 的结构接口
2. 但当前证据只支持说：K/V 非对称分配**可行但未熟**，有局部改善，却没有稳定跨模型占优信号
3. 因此未来工作不是“再发明一个 allocator 名字”，而是把 `K/V` 两侧的预算语言、耦合决策与 profile readout 真正做成熟

#### 5.7.2 这一段最适合怎么写

建议写成三层推进：

- 第一层：当前 allocator 仍以逐层 budget language 为主
- 第二层：角色感知扩展已经显示出接口层面的可表达性，但经验上尚未成熟
- 第三层：下一步更值得做的是把 `K/V` 预算从独立槽位推进到更耦合的 pair-wise / joint allocation 机制

#### 5.7.3 这一段不该怎么写

- 不要写目录名、实验批次名、CSV 数量
- 不要写旧稿里的局部 exploratory 数字
- 不要把它写成“当前已经几乎证明 role-aware allocator 更强”

#### 5.7.4 这一段最该留下的读感

评委读完应当感觉到：

> 当前论文已经暴露出 role-aware allocator 可继续推进的结构入口，但真正成熟的角色感知预算机制仍是下一阶段值得专门推进的核心问题。

### 5.8 第 3 段：Matched-budget formal compare

这一段必须写稳，因为它直接对应 `5.3` 中最重要的比较口径边界。

#### 5.8.1 这一段要做什么

必须把它写成：

> **用更干净的严格预算可比性设计，补全 allocator 线当前仍未完成的形式化比较。**

这一段背后的事实基础有两层：

1. Chapter 4 已经把 `4.4` 的结论严格限定在 **same-order INT4 budget band** 下的 regime readout，而不是 strict matched-budget formal compare
2. 当前旧稿也已经诚实承认：allocator-enabled 路径相对 `KIVI-style` 的 matched-budget formal compare 仍未完成，现有探索性结果还不足以支撑 systematic claim

#### 5.8.2 这一段最适合怎么写

建议分三层写：

- 先承认：本文已经证明了 strategy regime map 的存在，但这不等于完成严格预算可比性的形式化裁决
- 再指出：未来工作需要的不是再跑更多同口径表，而是设计更干净的 matched-budget protocol
- 最后说明：未来工作需要设计更干净的严格预算可比性协议，以补全 allocator 线当前尚未完成的 **formal compare**

#### 5.8.3 这一段不该怎么写

- 不要保留旧稿里的具体 cost/benefit 数字
- 不要保留脚本名或 compare setup 名称
- 不要写成“只要再做一轮实验就一定能证明 allocator 更优”

#### 5.8.4 这一段最该留下的读感

评委读完应当感觉到：

> 作者很清楚当前 allocator 线最需要补的，不是更多 case，而是更严格的比较设计；这会直接决定其比较性主张能否进一步收紧。

### 5.9 节末短收束

这一节最后建议只用 **1 个短段** 收住，作用是：

- 不再重复三条方向的细节
- 只把它们压成一条主线上的递进关系
- 轻轻桥到 `5.5`

推荐收束句逻辑：

> 综上，本文后续最值得推进的并不是平行扩张更多方法分支，而是沿同一 behavior-guided 主线继续向三个层面深化：在策略调用层面推进到 per-prompt routing，在预算机制层面推进到更成熟的 role-aware allocator，在比较口径层面推进到严格 matched-budget formal compare。这样，未来工作的重点仍然不是寻找一个脱离条件的“全局最优”，而是持续扩大这一框架的可解释性、可比较性与适用域。

### 5.10 公式安排

**结论：不放显示公式。**

原因非常明确：

- 这一节是 future-work 节，不是方法定义节
- 公式只会把读者重新拉回第三章
- 这里最重要的是方向收束，而不是符号展开

### 5.11 图表安排

**主方案：0 图，0 表。**

这一节不需要 roadmap 图，也不需要“future work summary table”。原因有三点：

1. Chapter 5 默认不放图表的总纪律在这里最该坚持
2. 三条方向本身已经足够清楚，不需要再图示
3. 一旦做表，很容易把 `5.4` 写成 planning memo，而不是正式论文收束节

### 5.12 相对当前稿件的删改动作

这一节相对当前 PDF 旧 `5.3 未来工作展望`，建议做下面这组手术动作。当前旧稿的优势是：三条方向已经基本对齐最新 story；问题在于写法还偏 exploratory memo。

#### 5.12.1 保留

保留旧稿中的三条骨架：

- `Per-Prompt Routing`
- 更成熟的 `Role-Aware Allocator`
- `Allocator-vs-KIVI Matched-Budget Formal Compare`

这三条本身就是新 `5.4` 的主骨架，不需要推翻。

#### 5.12.2 必须改写

以下内容必须整体升格改写：

- `未来工作一 / 二 / 三` 的条目式口吻
- `prompt_adaptive` 的单任务具体分数
- `task-bucket fallback` 的实现口气
- `results/l2_kv_asymmetric/` 这类实验目录名
- `bit dictionary {4,8,16}`、`1.5–1.8×`、`16%` 等探索性数字堆叠

它们在旧 `5.3` 里作为备忘录是有价值的，但在新 `5.4` 正文里必须改写成**正式学术方向句**。

#### 5.12.3 直接删掉

默认删掉这些材料，不进新 `5.4` 主文：

- appendix 编号
- 脚本名、实验名
- 单任务具体改变量
- “哪一个 cell 反向劣于谁”的细粒度 compare

这些内容顶多作为后面写 `Draft` 时的内部备料，不应出现在未来工作主文中。

#### 5.12.4 与 `5.3` 的接口重写

旧稿里有些 future work 是从局限性条目直接平移出来的。新稿要把这个关系写得更正式：

- **prompt 级弱信号** → `per-prompt routing`
- **role-aware 接口已存在但未成熟** → 更成熟的 `role-aware allocator`
- **same-order 而非 matched-budget** → `matched-budget formal compare`

这三条是从 `5.3` 抽出的主延伸接口，并非对全部边界的逐条镜像。

这样 `5.4` 才会像 `5.3` 的自然延伸，而不是另开清单。

#### 5.12.5 与新 `5.5` 的顺延关系

当前 PDF 还是旧结构：`5.3 未来工作展望`、`5.4 结语`。在冻结版目录中，这意味着：

- 旧 `5.3` → 新 `5.4`
- 旧 `5.4` → 新 `5.5`

所以这里的施工动作必须明确：新 `5.4` 只吸收旧 `5.3`，不要把旧 `5.4` 的收束话语混进来。

### 5.13 完成后的目标印象

读完 `5.4`，评委脑子里应该只留下这一句话：

> **这篇论文不是以“当前做完了”为终点，而是已经归纳出三条值得优先检验的推进方向：把策略选择推进到 prompt 级、把预算机制推进到更成熟的角色感知层、把比较主张推进到严格预算可比性层。**

---

## 6. 第五章 5.5 写作计划

### 6.1 小节标题

保持为：

**5.5 结语**

这个标题已经冻结，不再改。它在第五章中的功能不是“再给一个总结节”，而是：

> **在 `5.1–5.4` 已经完成研究结论、学术价值、局限性与未来工作的前提下，用最后一段把整篇论文收干净。**

### 6.2 小节职责

`5.5` 的职责只有一个，而且非常纯：

> **把整篇论文从“研究问题—方法框架—实例路径—实证图谱—边界与展望”这条主线收成一句成熟的全文判断。**

这节必须完成三件事：

1. **把全文主句收回来。**
   也就是把 `behavior-guided framework` 作为唯一主线重新压回最后一段，而不是再分散成 `INT8`、`INT4`、allocator 三件独立工作。
2. **把贡献的最终形态收回来。**
   这里不能再按 `RQ1–RQ3` 展开，但要让评委在最后一段里再次感到：
   - 这篇论文不只是在做一个局部低比特技巧
   - 它真正交付的是一个统一组织原则、一组路径实例，以及一张跨模型策略适用区间图谱
3. **把全文语气收稳。**
   既不夸张，也不泄气。要呈现的是：这项工作已经给出了清楚、可信、边界明确的推进，而不是“问题从此被彻底解决”。

这一节和 `4.7`、`5.1` 的区别必须非常清楚：

- `4.7` 回答：**第四章已经证明了什么**
- `5.1` 回答：**全文对 `RQ1–RQ3` 的最终答案是什么**
- `5.5` 回答：**整篇论文最终应被怎样理解**

### 6.3 本节不做什么

这一节必须比前面任何一节都更克制，不承担以下内容：

- 不再按 `RQ1–RQ3` 顺序重答一遍
- 不再复述 `5.2` 的三层创新点
- 不再列 `5.3` 的局限性
- 不再展开 `5.4` 的未来方向
- 不再点名具体模型案例，如 `Qwen2.5-3B`、`Qwen2.5-14B`、`Mistral-7B`
- 不再写 `heuristic`、`AutoK`、`INT4-RoleAlign` 的局部比较口气
- 不再出现任何数字、表号、图号、附录号
- 不再引入新结论、新局限、新展望

一句话说，`5.5` 不是**第五章小结**，而是**全文收口句**。

### 6.4 最优总结构

建议将 `5.5` 固定为：

- **1 个自然段**
- **4–5 句**
- **0 张图**
- **0 张表**
- **0 个正文显示公式**

这是主方案，不建议再切成两段。原因很简单：

- latest story 已明确要求“简洁收束，不再扩写”
- `4.7` 已经做过章级桥接
- `5.1–5.4` 已经把该展开的内容全部展开过了
- `5.5` 再拉长，只会把全文重新写散

### 6.5 唯一正文段的最佳推进顺序

这一段最稳的内部顺序，我建议固定为 **5 句式**：

#### 6.5.1 第 1 句：把问题提升回全文层

先把论文对象重新拉高，不从某个方法名开头，而从问题性质开头。

推荐逻辑：

> 面向长上下文高效推理，KV Cache 量化并不是一个单纯寻找更低 bit-width 的局部优化问题，而是一个需要统一组织原则的系统设计问题。

这一句可以直接吸收旧 `5.4` 的第一性判断，但必须把英文口气与旧目录口气清干净。

#### 6.5.2 第 2 句：收回统一原则与方法主线

这一句要把全文方法主线收回来，但不能展开成方法回顾。

推荐逻辑：

> 本文以 attention behavior 为统一组织对象，把 calibration 与 allocation 两层决策纳入同一 `behavior-guided` 框架，并在规范实例、低比特路径与预算扩展中形成了可相互印证的分层实例链。

这一句里可以保留少量专名，但不要写成方法清单。

#### 6.5.3 第 3 句：收回实证价值的最终形态

这一句必须把全文经验贡献收成“图谱价值”，而不是“谁赢了谁”。

推荐逻辑：

> 更重要的是，本文并未将跨模型实验收束为一个脱离条件的“全局最优”策略，而是识别并命名了跨模型策略适用区间图谱，使不同 family、scale 与 task 条件下的结构差异能够被读出、被解释，并为后续研究提供可承接的结构读法。

这一句是 `5.5` 的承重句。它要把旧 `5.4` 中“regime map 的价值不在通用最优，而在命名结构差异”完整继承下来，但删去所有具体 regime 枚举。

#### 6.5.4 第 4 句：轻带边界意识，但不重开局限性

这里不能再重讲 `5.3`，只允许用一句轻量判断，把全文可信度收稳。

推荐逻辑：

> 在此基础上，本文也对这些结论的协议边界、比较口径与系统适用域作出了克制而明确的限定，从而使全文结论既保持解释力，也保持可信度。

注意，这一句只允许轻触“边界”，不能重新列 limitation 清单。它的作用是让结尾显得成熟，而不是单纯抒情。

#### 6.5.5 第 5 句：最后的全文收束句

最后一句应当回到论文意义，而不是回到具体实验。

推荐逻辑：

> 因而，本文的最终价值不在于提出一个永远最优的局部量化技巧，而在于为面向长上下文高效推理的 KV Cache 量化提供了一条以 behavior 为中心、可审计、可扩展且具备结构解释力的研究路径。

这句就是全文最后一句的目标形态。

### 6.6 公式安排

**结论：不放公式。**

这一节已经处在全文最后，不应再出现任何显示公式。即使是行内公式，也应尽量少到只剩必要专名。

最多允许出现的，也只是：

- `INT8`
- `INT4`
- `behavior-guided`

但能不用就不用。

### 6.7 图表安排

**结论：0 图，0 表。**

这一节绝对不建议再放任何 summary figure 或 final table。原因有四个：

1. 它只有 1 段
2. 它的目标是语义收束，不是信息再组织
3. Chapter 5 默认不放图表的纪律，在这里最该坚持
4. 一旦加图表，全文会在最后一页重新变成结果说明节

### 6.8 相对当前稿件的删改动作

这一节相对当前 PDF 旧 `5.4 结语`，建议做下面这组手术动作。旧稿不是不能用，而是**只能取其骨，不保其形**。

#### 6.8.1 保留

保留旧 `5.4` 里这两层最高价值判断：

- **KV Cache 量化不是“找最佳 bit-width”的局部问题，而是需要组织原则的系统设计问题**
- **适用区间图谱的价值不在通用最优，而在命名结构差异**

这两句的逻辑完全值得进入新 `5.5`。

#### 6.8.2 必须改写

以下内容不能原样保留，必须压缩改写：

- `first principle`
- `operating 适用区间地图`
- 旧稿里明显偏摘要式的“完成了 calibration 与 allocation 两层的统一组织”
- 旧稿的“可读数”口气

这些都要改成更成熟的中文学术结尾句。

#### 6.8.3 直接删掉

以下内容不应再进入新 `5.5`：

- `3B early-layer rescue`
- `14B top-tier wide-spread`
- `Mistral AutoK cov80`
- `heuristic` 与 `behavior-guided` 重合集合的具体点名
- 任何“4 条命名规律构成操作化入口”的枚举式写法

这些内容在旧 `5.4` 中有意义，是因为旧结构里前文还不够分层；但在新结构下，它们已经属于 `5.1` 与 `5.2` 的已完成内容，再留在 `5.5` 就会显得回头重讲。

#### 6.8.4 顺延关系要写死

当前 PDF 还是旧结构：旧 `5.4` = 结语。在冻结版目录中，它对应：新 `5.5` = 结语。也就是说，新 `5.5` 主要就是在**旧 `5.4` 的基础上做压缩升级**，而不是从别处抽一段文字来替代它。

### 6.9 完成后的目标印象

读完 `5.5`，评委脑子里应该只留下这一句话：

> **这篇论文真正完成的，不是找到一个永远最优的 KV Cache 量化技巧，而是以 attention behavior 为中心，把规范路径、低比特实例与跨模型策略适用区间图谱组织成了一条可信、可解释、可扩展的研究路径。**

---

## 7. 第五章接入与回写纪律

### 7.1 外部正文接入前置条件

后续任何外部 Chapter 5 草稿进入前，必须先做四层核对：

1. 与 `objective.md` 是否一致
2. 与 `thesis_story_20260420.md` 是否一致
3. 与本文件的章级职责和小节边界是否一致
4. 与 Chapter 3 / 4 已冻结结论是否一致

### 7.2 Draft 到 tex 的桥接纪律

- 未在 [Chapter 5 Draft.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/Chapter 5 Draft.md>) 中被判为 `采用` 的正文，禁止进入 `tex`
- 被判为 `需改写` 的正文，必须保留原判定、冲突原因与推荐改法
- 当前线程阶段只维护 source-of-truth，不做 `tex` 回写

### 7.3 Draft 审批门槛（总则）

#### 7.3.1 通用检查清单

任何 Chapter 5 正文草稿在进入 `Draft` 之前，至少要通过以下检查：

- `职责检查`：草稿必须能被一句话概括为“本节唯一在回答什么”
- `边界检查`：不得触碰该节“本节不做什么”中的任一项
- `结构检查`：必须服从已冻结的段落数、回答块数、图表/公式纪律
- `证据口径检查`：只能上收 Chapter 3/4 已冻结结论，不能增强 claim
- `承重检查`：每段只承载一个动作；整节必须能压成一个明确的目标印象
- `引桥检查`：节首说明承接什么，节末说明转向什么
- `术语检查`：不得回退到旧“主要发现 / 五条规律 / universal winner”叙事

#### 7.3.2 判定标准

- `拒绝`：
  - 章节职责错位
  - 与 Chapter 3/4 冻结结论冲突
  - 重新拉回结果章写法
  - 引入新 claim 或新泛化
  - 破坏固定结构
- `需改写`：
  - 大方向对，但仍然太像第四章摘要
  - 数字 / case 过多
  - 边界泄漏
  - 段落承重混乱
  - 目标印象不够锋利
- `可接入 Draft`：
  - 所有 hard gate 满足
  - 整节可以被压成预设的一句话结论
  - 仅剩措辞级微调

`Writing` 层的 `可接入 Draft`，对应 `Draft` 层的：

- `采用`
- `需改写`
- `拒绝`

#### 7.3.3 分节硬门槛

| 节 | 当前硬门槛 |
|---|---|
| `5.1` | 必须是 `1+3+1` 段，按 `RQ1→RQ2→RQ3`，`0` 图表 / 表格 / 显示公式；一旦出现 finding 列表、表号、读数主句、case 回放，直接拒绝 |
| `5.2` | 必须是 `1+3+1` 段；正文保持连贯学术段落，不使用“创新点一 / 二 / 三”编号体；三段分别只承载统一原则、方法实例、实证图谱；`0` 图、`0` 表、`0` 显示公式；一旦出现结果数字主句、逐模型 case、贡献条目镜像 §1.5 或把系统层抬成并列主创新，直接判为 `需改写` 或 `拒绝` |
| `5.3` | 必须是 `1+4+1` 段，四段只承载四类边界：协议与真实外推、比较口径与预算可比性、结论条件性与机制解耦、系统实现与时间有效性；不得把局限写成自我否定或 future work 清单 |
| `5.4` | 必须是 `1+3+1` 段，三条方向应从 `5.3` 已识别边界中择其最值得继续推进的三类方法性边界展开，而非对全部限制的逐条镜像；不得扩成 wish list，不得保留 exploratory 数字、脚本名或 appendix 编号，不得把未来方向写成已成立结论 |
| `5.5` | 必须是单段 `4–5` 句，且只做全文收口；不得重答 `RQ1–RQ3`、不得重列三层创新点、不得列具体模型 / 数字 / future work；一旦拉长成摘要复刻或结果补段，直接拒绝 |
