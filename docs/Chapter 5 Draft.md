# Chapter 5 Draft

## 文档身份与当前状态

- 文档身份：Chapter 5 正文整合、审校与回写前中间层 source-of-truth。
- 当前状态：
  - `5.1–5.5` 已完成施工层冻结；Draft 层仍待接入正文草稿。
  - [This is Chapter 5 Writing.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/This is Chapter 5 Writing.md>) 已建立，当前作为 Chapter 5 `5.1–5.5` 施工层 authority。
  - 当前处于“source-of-truth 初始化完成、等待外部正文接入”的阶段。
  - 尚未进入 `thesis/chapters/ch5_conclusion.tex` 回写阶段。
- 维护边界：
  - 本文档服务于 Chapter 5 正文整合与审核。
  - 本文档不是 `objective.md`。
  - 本文档不是 `docs/thesis_story_20260420.md`。
  - 本文档不是 `docs/thesis_upgrade_live_plan.md`。
  - 本文档不是最终 `tex` 成稿。

## 当前审校基线

- 基线时间：`2026-04-23 03:21`
- 当前线程角色：Chapter 5 正文整合与审核线程
- 当前默认回写目标：`thesis/chapters/ch5_conclusion.tex`
- 当前 worktree 状态摘要：
  - 共享写作文件已存在未提交改动。
  - 本线程初始化本文件时，不覆盖现有 `story`、Chapter 3/4 source-of-truth 与 `tex`。
  - 当前阶段仅建立 Chapter 5 的审校容器，不回写任何 `tex`。
- 当前只读输入基线：
  - `AGENTS.md`
  - `CLAUDE.md`
  - `objective.md`
  - `docs/thesis_story_20260420.md`
  - `docs/This is Chapter 5 Writing.md`
  - `docs/This is Chapter 3 Writing.md`
  - `docs/This is Chapter 4 Writing.md`
  - `docs/Chapter 3 Draft.md`
  - `docs/Chapter 4 Draft.md`
  - `docs/thesis_upgrade_live_plan.md`
  - `docs/mainline_execution_queue.md`
  - `thesis/chapters/ch5_conclusion.tex`

## Source-of-Truth 文件列表

| 层级 | 文件 | 本文档使用方式 |
|---|---|---|
| 项目协作入口 | `AGENTS.md` | 约束多 Agent 分工、共享文件职责、主工作流 |
| 项目级规范 | `CLAUDE.md` | 约束 Plan 纪律、可写边界、论文工作方式 |
| 高层目标 | `objective.md` | 冻结全文 mission、方向分层与主线边界 |
| 冻结叙事 | `docs/thesis_story_20260420.md` | 冻结 Chapter 5 的章级职责、目录结构与术语边界 |
| Chapter 5 施工文档 | `docs/This is Chapter 5 Writing.md` | 当前冻结 `5.1–5.5` 的施工规则、边界、图表纪律与接入门槛 |
| 上游方法冻结 | `docs/This is Chapter 3 Writing.md` + `docs/Chapter 3 Draft.md` | 提供 Chapter 3 已冻结的方法主线、实例层级与系统落地边界 |
| 上游证据冻结 | `docs/This is Chapter 4 Writing.md` + `docs/Chapter 4 Draft.md` | 提供 `RQ1–RQ3` 证据链、部署边界与效度边界 |
| 实时工作台 | `docs/thesis_upgrade_live_plan.md` | 只用于补充 runtime 真相与历史草稿来源，不作为正文 authority |
| 执行清单 | `docs/mainline_execution_queue.md` | 只用于确认当前已进入论文写作阶段，不承接正文裁决职责 |
| 被审对象 | `thesis/chapters/ch5_conclusion.tex` | 记录当前旧结构与冻结口径的不一致项 |

### 当前 active source-of-truth 三件套

- `docs/Chapter 5 Draft.md`
- `docs/This is Chapter 5 Writing.md`
- `docs/thesis_story_20260420.md`

说明：

- 上述三份文件是当前有效裁决来源。
- `objective.md` 提供更高层 mission 与边界，但不负责 Chapter 5 逐段裁决。
- `thesis_upgrade_live_plan.md` 中的旧草稿块只作历史参考，不直接参与当前正文裁决。

## 文档分工与边界

| 文件 | 负责什么 | 不负责什么 |
|---|---|---|
| `objective.md` | 高层 mission、边界、成功标准、方向分层 | 不负责 Chapter 5 小节施工 |
| `docs/thesis_story_20260420.md` | 论文唯一主线、章节职责、迁移规则 | 不负责逐段正文审核 |
| `docs/This is Chapter 5 Writing.md` | 当前负责 `5.1–5.5` 写作施工、边界、图表与回写纪律 | 不负责外部正文整合 |
| `docs/This is Chapter 3 Writing.md` + `docs/Chapter 3 Draft.md`；`docs/This is Chapter 4 Writing.md` + `docs/Chapter 4 Draft.md` | 提供上游冻结的方法/证据结论、结构边界与章际职责切割 | 不负责 Chapter 5 正文裁决 |
| `docs/thesis_upgrade_live_plan.md` | 当前论文升级 live status | 不负责正文裁决 |
| `docs/Chapter 5 Draft.md` | Chapter 5 正文整合、逐段审核、回写前 source-of-truth | 不替代上述任一权威文件 |

## Chapter 5 冻结结构总览（5.1–5.5）

| 节次 | 冻结职责 | 必须避免 |
|---|---|---|
| `5.1` | 面向 `RQ1–RQ3` 的全文级研究结论 | 不写成“主要发现”式结果罗列 |
| `5.2` | 创新点与学术价值 | 不生成新 claim；不夸大贡献 |
| `5.3` | 研究局限性 | 不撤回已成立结论；不把 future work 混进来 |
| `5.4` | 未来工作展望 | 不把 exploratory 内容写成当前结论 |
| `5.5` | 结语 | 不扩成摘要复刻或结果补段 |

## 上游冻结结论核对清单

后续接收任何 Chapter 5 草稿时，至少要逐项核对以下内容。

### 1. `RQ1` 的冻结回答

- 是否保持“behavior-guided calibration 在 `INT8 canonical path` 上形成稳定、可复现的基础保真路径”
- 是否保持官方 LongBench 真实数据对照只是协议一致性 sanity check
- 是否避免把 `competitive parity` 误写成“普适击败基线”

### 2. `RQ2` 的冻结回答

- 是否保持“对称 `INT4` 的架构依附性失效”
- 是否保持“Key 精度下降是主导触发侧”
- 是否保持 `RoleAlign vs KIVI-style` 为 same-format compare

### 3. `RQ3` 的冻结回答

- 是否保持 allocation / `AutoK` 是统一框架扩展层
- 是否保持 regime-based interpretation，而非 universal law
- 是否显式保留 heuristic 是强基线这一结构结论

### 4. 必须一并保留的边界

- 协议边界
- 比较口径边界
- 系统与硬件边界
- 数据溯源边界

如果正文只讲高层结论、不讲这些边界，则默认视为口径不完整。

### 5. `4.7` 与 Chapter 5 的职责切割

后续正文必须保持：

- `4.7` 回答：第四章已经证明了什么
- Chapter 5 回答：整篇论文最终说明了什么

若草稿仍在长段解释第四章表图、case 与读数，默认判为职责越界。

## 当前 `Draft ↔ ch5_conclusion.tex` 的不一致问题清单

说明：

- 本区块记录当前旧 `tex` 与冻结结构之间的显式冲突。
- 这些问题当前只在 Draft 层记录，不在本轮直接回写 `tex`。

### [需改写] 旧 `ch5_conclusion.tex` 仍沿用“主要发现 / 局限性 / 未来工作 / 结语”结构

- 与 `story` 冻结的 `5.1–5.5` 结构不一致。
- 后续回写时必须重排为：
  - `5.1 面向 RQ1–RQ3 的研究结论`
  - `5.2 创新点与学术价值`
  - `5.3 研究局限性`
  - `5.4 未来工作展望`
  - `5.5 结语`

### [需改写] 旧 `主要发现` 节仍是结果罗列式写法

- 当前旧稿中“发现一到发现五”的组织方式容易回到第四章结果复述。
- 新版 `5.1` 必须改为按 `RQ1–RQ3` 组织，而不是按 finding 列表组织。

### [需改写] 旧稿把“贡献总结”塞在结果节内部

- `story` 已冻结 `5.2 创新点与学术价值` 为独立小节。
- 后续不得继续把贡献总结混在 `5.1` 的结果归总里。

### [需改写] 旧稿对局限 / future work 的分层仍需重新对齐

- limitation 应对应当前已冻结边界与 open questions
- future work 应只保留：
  - per-prompt routing
  - 更成熟的 role-aware allocator
  - matched-budget formal compare

## 外部线程草稿接入规则

后续每次收到 Chapter 5 草稿时，统一按以下流程处理。

### A. 归属判断

- 先判断该草稿属于 `5.1–5.5` 的哪一节
- 若一段同时跨两节，必须拆分后再审核
- 不允许将“创新点 / 局限 / future work”混写在同一审校单元中

### B. 对齐审查

每段至少核对四层一致性：

1. 与 `This is Chapter 5 Writing.md` 是否一致
2. 与 `thesis_story_20260420.md` 是否一致
3. 与 Chapter 3 / 4 已冻结结论是否一致
4. 与整篇 thesis 的章节职责边界是否一致

### C. 审核结论

每个草稿块只能判为以下三类之一：

- `采用`
- `需改写`
- `拒绝`

### D. 冲突处理

若判为 `需改写` 或 `拒绝`，必须明确写出：

- 冲突位置
- 冲突原因
- 推荐改法

不允许静默重写后直接吸收。

### E. 整合动作

对每个草稿块都必须说明：

- 是否写入 `Chapter 5 Draft.md`
- 写入到哪一节
- 是否生成清洗后的可用正文
- 是否进入“最终可回写正文块”

## 审核协议

### 判定标签

- `采用`：与四层 authority 均一致，可直接进入 Draft 冻结基线
- `需改写`：主方向可用，但存在越界、口径冲突或边界遗漏
- `拒绝`：与冻结主线或章节职责根本冲突，不应吸收

### 合法理由分类

每条判定至少归入以下理由之一：

- 与 `story` 主线不符
- 与 Chapter 5 施工文档不符
- 与 Chapter 3 / 4 已冻结结论不符
- 与整篇 thesis 的章节职责边界不符

### 审核纪律

- 正文必须逐段审核，不整体放行
- 未审核正文不得进入“最终可回写正文块”
- 未形成 `采用` 判定前，不得回写 `tex`
- 当前线程只初始化 source-of-truth 与接收容器，不做 `tex` 落稿

## GPT / 外部线程正文整合区

说明：

- 本区从后续外部正文接入开始维护。
- 当前 `5.1–5.5` 已完成首轮接入与审核。

### 5.1 正文草稿（已接收，`2026-04-23 03:55`）

- 当前状态：已接收 / 已审核 / `采用（可接入 Draft）`
- 归属判断：`5.1 面向 RQ1–RQ3 的研究结论`
- 来源说明：本轮外部正文初稿；正文主体 5 段，附带一段对话内 `自审` 说明

#### 草稿原文摘要

- 采用 `1+3+1` 自然段结构：
  - 导语段
  - `RQ1` 回答段
  - `RQ2` 回答段
  - `RQ3` 回答段
  - 节末收束段
- `RQ1` 将行为引导校准收束为 `INT8 canonical path` 上的基础保真路径，并保留 official LongBench 仅作 sanity check 的边界。
- `RQ2` 将低比特结论收束为“对称 `INT4` 不能机械继承成功，但在 `K/V` 角色差异与 same-format 条件下可恢复为可用路径”。
- `RQ3` 将 allocator / `AutoK` 收束为扩展层，并把 regime-based interpretation 统一表述为“跨模型策略适用区间图谱”，同时保留 heuristic 为强基线。
- 末段将三条研究问题的回答上收为 `attention behavior` 作为贯通 calibration 与 allocation 的统一组织原则，并桥接到 `5.2`。

#### 审核表

| 维度 | 结论 | 说明 |
|---|---|---|
| 归属判断 | `通过` | 内容明确属于 `5.1 面向 RQ1–RQ3 的研究结论` |
| 与 `This is Chapter 5 Writing.md` 对齐 | `通过` | 符合 `RQ1 → RQ2 → RQ3` 的 `1+3+1` 结构，不回到旧 finding-list |
| 与 `story` 对齐 | `通过` | 保持 `behavior-guided framework` 主线，不与 `5.2–5.5` 抢职责 |
| 与 Chapter 3 / 4 冻结结论对齐 | `通过` | `RQ1` 保留 sanity check 边界，`RQ2` 保留 same-format / Key 主导触发，`RQ3` 保留 regime-based interpretation 与 heuristic 强基线 |
| 章节职责边界 | `通过` | 不重报图表、模型 case、数字主句，不重演第四章结果章 |
| 格式 gate | `通过` | `1+3+1` 自然段；`0` 图、`0` 表、`0` 显示公式 |
| 是否可接入 Draft | `通过` | 可直接形成 `5.1` 的 freeze-ready baseline |

#### 审核结论

- 审核结论：`采用（可接入 Draft）`
- 接入说明：
  - 本轮 prose polish 只做句级与段内节奏调整，不改变结构、口径与判定
  - 对话中的 `## 自审` 不属于论文正文，已转写为本区审核记录，不进入“最终可回写正文块”
- 本轮 prose polish：
  - 轻调 `RQ3` 段的枚举感
  - 收顺节末引桥句
- 非阻塞建议：
  - `RQ2` 段中的“正式路径”若后续要再收紧，可改成“可审计的恢复路径”或“可用恢复路径”
  - 节末最后一句若后续要与 `5.2` 引桥完全统一，可收成“下一节将进一步说明这些研究结论的创新点、学术价值与适用边界”

#### 清洗后的可用正文

```markdown
## 5.1 面向 RQ1–RQ3 的研究结论

前四章已经完成问题界定、理论组织、方法实例化与实证验证。本节不再回放图表或模型个案，而是回到第一章提出的 `RQ1–RQ3`，从全文收束的角度概括本文的最终研究结论。

对于 `RQ1`，本文的回答是：以 attention behavior 为中心的行为引导校准已经不只是一个理论设想，而是在 `INT8 canonical path` 上落实为一条稳定、可复现且可审计的基础保真路径。第四章的规范路径验证表明，在最保守的位宽和受控协议下，这一原则能够在不依赖额外在线自适应的前提下保持与全精度高度一致的任务行为；官方 LongBench 真实数据对照则只承担评测协议一致性的 sanity check，进一步说明主文的合成评测并未系统性误判这一方向，但这并不构成对真实场景全面泛化的证明。换句话说，`RQ1` 真正回答的不是“INT8 本身容易做好”，而是 behavior-guided 原则已经在最干净的验证实例上落成了一条可信的基础保真路径。

对于 `RQ2`，本文的回答是：behavior-guided 原则不能机械地下推到对称 `INT4`，但它能够在角色感知的非对称路径中恢复为一条可用且可审计的低比特实例。第四章已经表明，对称 `INT4` 面对的并不是温和退化，而是具有明显架构依附性的阶跃失稳，其中 Key 精度下降是更主要的触发侧；相应地，`INT4-RoleAlign` 的意义也不在于宣称低比特问题已经普遍解决，而在于在当前已验证的同格式条件和 role-aware 设定下，给出了一条从对称失效走向可用恢复的清晰路径。因此，`RQ2` 的答案不是“INT4 必然成功”，而是只有在承认 `K/V` 角色差异、并据此组织格式与离线校准纪律时，低比特路径才可能稳定建立起来。

对于 `RQ3`，本文的回答是：由校准链路产出的 behavior sensitivity profile 不仅能够服务参数选择，也可以上升为跨模型预算分配的组织依据，并在当前已验证的预算范围内揭示一张由 family、scale 与 task 共同决定的跨模型策略适用区间图谱。allocator 与 `AutoK` 的实证结果并未导向一个脱离条件的 universal winner，更稳定的事实是，不同模型会落入不同的高性能区间：有的更依赖早层保护，有的呈现中等规模的共识解，也有的表现为多个高性能簇并存而没有稳定最优。与此同时，heuristic 作为强基线并不是与本文主线相冲突的例外；它恰恰说明，在某些区间里，可行层集本身就很窄。behavior-guided 方法的价值，正在于识别这些区间何时出现、如何区分，并把原本分散的跨模型现象整理成可命名、可解释的结构读数，而不是把单一策略写成普适法则。

综上，对 `RQ1–RQ3` 的回答共同指向一个更高层的判断：attention behavior 可以作为贯通 calibration 与 allocation 的统一组织原则。它先在 `INT8 canonical path` 上建立了最干净的基础保真路径，随后在低比特场景下具体化为一条承认 `K/V` 角色差异的恢复路径，并进一步扩展成能够读出跨模型策略适用区间的结构化工具。因而，第五章后续已经无须再重复第四章的证据，而应转向说明这些研究结论的创新点、学术价值与适用边界。
```

### 5.2 正文草稿（已接收，`2026-04-23 04:44`）

- 当前状态：已接收 / 已审核 / `采用（可接入 Draft）`
- 归属判断：`5.2 创新点与学术价值`
- 来源说明：本轮外部 LaTeX-ready 正文；原稿带 `\section/\label` 包装

#### 草稿原文摘要

- 采用 `1+3+1` 自然段结构：
  - 导语段
  - 理论组织层
  - 方法实例层
  - 实证图谱层
  - 节末收束段
- 第一段将 `attention behavior` 提升为同时组织 calibration 与 allocation 的统一对象。
- 第二段将 `INT8 canonical path`、`INT4-RoleAlign`、allocator / `AutoK` 组织成分层实例链，而不把系统层抬成主创新。
- 第三段将经验层价值收束为“跨模型策略适用区间图谱”，并把 heuristic 解释为结构观察的一部分。
- 末段把三层价值收成研究路径，不回到 `RQ1–RQ3` 或贡献条目镜像。

#### 审核表

| 维度 | 结论 | 说明 |
|---|---|---|
| 归属判断 | `通过` | 内容明确属于 `5.2 创新点与学术价值` |
| 与 `This is Chapter 5 Writing.md` 对齐 | `通过` | 符合 `1+3+1` 结构，三段只承载统一原则、方法实例、实证图谱 |
| 与 `story` 对齐 | `通过` | 回答“为何具有独立学术价值”，未重答 `RQ1–RQ3` |
| 与 Chapter 3 / 4 冻结结论对齐 | `通过` | 保持 `INT8 canonical path` / `INT4-RoleAlign` / allocator / heuristic / 图谱口径 |
| 章节职责边界 | `通过` | 无结果数字主句、无逐模型 case、无部署效率主创新 |
| 格式 gate | `通过` | `1+3+1`；`0` 图、`0` 表、`0` 显示公式 |
| 是否可接入 Draft | `通过` | 可形成 `5.2` 的 freeze-ready baseline |

#### 审核结论

- 审核结论：`采用（可接入 Draft）`
- 清洗动作：
  - 去除 `\section{创新点与学术价值}` 与 `\label{sec:ch5-innovation-value}` 包装
  - 本轮 prose polish 只做句级与段内节奏调整，不改变三层结构与判定
- 本轮 prose polish：
  - 去掉导语与三段中的模板腔
  - 压低抽象名词堆叠和 regime 枚举感
- 本轮 prose polish（微调）：
  - 收掉“从理论组织层看 / 在方法层面 / 从经验层面看”的模板化推进
  - 缓和段末“价值不在于 A，而在于 B”的重复判断句
- 本轮终稿微调：
  - 进一步收顺三段开头的推进语气
  - 把末段判断句压得更像 thesis prose
- 文风判断：`自然`
- 非阻塞建议：
  - 若后续还要统一终稿文风，第三段可以再略收一点术语密度

#### 清洗后的可用正文

```latex
上一节已经概括了本文的最终结论。本节进一步要回答的是，这些结论为何不能只被理解为若干分散的实验现象，而应被看作一项具有统一组织原则、分层实例链和跨模型解释能力的研究。

在理论层面，本文并没有把 KV Cache 量化继续拆成几个彼此独立的局部优化问题，而是把 attention behavior 提升为同时组织 calibration 与 allocation 的统一对象。第三章从注意力近似误差传播分析出发，说明量化损伤真正被下游感知的，并不是孤立的张量数值偏差，而是由注意力分布偏移与聚合输出偏移共同构成的行为失真。由此，校准层与分配层不再只是两个并列的工程步骤，而被收束到同一条 behavior-guided 主线之下。本文在理论层面的推进，也因此不在于为既有量化流程再补一个局部校准目标，而在于为 KV Cache 量化提供了一种能够同时容纳规范验证、低比特实例和预算扩展的解释框架。

在方法层面，本文的价值也不在几项零散技巧的叠加，而在于同一原则下形成了一条层次分明的实例链。作为最保守也最干净的验证实例，\texttt{INT8 canonical path} 说明 behavior-guided 校准能够首先在规范位宽上建立稳定、可复现的基础保真路径；在与 \texttt{KIVI-style} 共享非对称格式的前提下，\texttt{INT4-RoleAlign} 的意义也不在经验上的一边倒优势，而在于它把低比特路径组织成了一条可固化、可审计、并能继续向后扩展的实例化路径。allocator 与 \texttt{AutoK} 同样不应被理解为脱离校准层另起炉灶的第二套方法，而应被视为由同一 behavior sensitivity profile 延伸出的预算决策扩展。与其说本文提出了若干彼此并置的技巧，不如说它把规范验证、低比特恢复与预算扩展组织成了一条前后衔接的分层实例链。

在经验层面，本文的创新同样不在于把某一种策略写成跨模型、跨任务、跨预算条件下的单一最优，而在于识别并命名了一张跨模型策略适用区间图谱。第四章的跨模型结果并没有把比较收束成一个脱离条件的普适赢家，更稳定的事实是，模型家族、参数规模与任务会共同塑造不同策略的高性能区间。本文真正提供的，也不是某个 allocator 在若干读数上赢了多少，而是一种用来辨认这些区间何时出现、如何分化以及为何分化的结构化视角。heuristic 在多个区间中表现为强基线，也并不削弱本文主线；相反，它提醒我们，在某些模型条件下，可行层集本来就很窄，而 behavior-guided 方法的学术价值，正在于把“强基线何时成立、何时失效”这类现象从经验杂讯提升为可解释的结构读数。

因此，本文的创新点与学术价值，并不在于孤立地提出某一个新的量化技巧，而在于以 attention behavior 为统一组织原则，把规范路径、低比特实例与跨模型策略适用区间图谱组织成一条具有解释力、可审计性与可扩展性的研究路径。也正是在这一意义上，本文不仅回答了 KV Cache 量化“如何压缩”的问题，也把这一问题从局部技巧比较推进到了组织原则与结构解释的层面。
```

### 5.3 正文草稿（已接收，`2026-04-23 04:44`）

- 当前状态：已接收 / 已审核 / `采用（可接入 Draft）`
- 归属判断：`5.3 研究局限性`
- 来源说明：本轮外部 LaTeX-ready 正文；原稿带 `\section/\label` 包装

#### 草稿原文摘要

- 采用 `1+4+1` 自然段结构：
  - 导语段
  - 评测协议与真实外推边界
  - 比较口径与预算可比性边界
  - 结论条件性与机制解耦边界
  - 系统实现、动态决策与时间有效性边界
  - 节末收束段
- 第一段把局限性写成适用域说明，而非对前文结论的撤回。
- 第二、三段分别锁定 allocator 线的预算可比性边界，以及 low-bit / same-format compare 的条件性边界。
- 第四段把系统环境绑定、`AutoK` 的静态 proposer 属性与 clean-provenance 的时间有效性收进同一组边界。
- 末段将四类边界统一收成正式限定，并桥接 `5.4`。

#### 审核表

| 维度 | 结论 | 说明 |
|---|---|---|
| 归属判断 | `通过` | 内容明确属于 `5.3 研究局限性` |
| 与 `This is Chapter 5 Writing.md` 对齐 | `通过` | 符合 `1+4+1` 结构，四段只承载四类边界 |
| 与 `story` 对齐 | `通过` | 写的是适用域说明，不是 future work 清单 |
| 与 Chapter 4 冻结边界对齐 | `通过` | 保留 official LongBench 补充检验、same-order budget band、same-format compare、静态 proposer、clean-provenance 边界 |
| 章节职责边界 | `通过` | 不重报结果，不自我否定，不抢写 `5.4` |
| 格式 gate | `通过` | `1+4+1`；`0` 图、`0` 表、`0` 显示公式 |
| 是否可接入 Draft | `通过` | 可形成 `5.3` 的 freeze-ready baseline |

#### 审核结论

- 审核结论：`采用（可接入 Draft）`
- 清洗动作：
  - 去除 `\section{研究局限性}` 与 `\label{sec:ch5-limitations}` 包装
  - 本轮 prose polish 只做句级与段内节奏调整，不改变四组边界与桥接关系
- 本轮 prose polish：
  - 压低首段和末段的提纲腔
  - 缓和系统边界段的总结口气
- 本轮 prose polish（微调）：
  - 收短首段元话语，减少“本节进一步要说明的是”式提纲感
  - 放松末段“总的来看”式总结腔
- 本轮终稿微调：
  - 进一步压低首段的说明腔
  - 收顺节末总结句的落点
- 文风判断：`自然`
- 非阻塞建议：
  - 若后续还要统一终稿文风，首段仍可再轻一点信息密度

#### 清洗后的可用正文

```latex
上一节讨论的是本文在理论组织、方法实例与实证图谱层面的学术价值。本节转而说明这些价值成立在怎样的范围之内。它们并不能脱离评测协议、比较口径、系统环境和时间外推条件而无条件成立；讨论局限性，也不是撤回前面的结论，而是把这些结论的适用域交代清楚。

从评测协议上看，本文的主要实证证据建立在统一协议下的 LongBench 风格合成基准任务上，官方 LongBench 真实数据对照仅在 \texttt{INT8 canonical path} 上承担评测协议一致性的补充检验。这说明本文已经能够在协议内给出稳定证据，也通过一块受控的真实数据对照排除了主文评测方向被系统性读错的可能，但这还不足以把相关结论外推为对更广泛真实应用场景的充分证明。尤其当输入分布、任务结构、提示风格与真实部署流程发生变化时，不同量化路径的相对表现及其适用边界仍可能随之改变。与此同时，本文虽然并列使用了 PPL、Needle 与任务级指标以降低单一度量带来的偏差，但这并不意味着已经穷尽了长上下文质量的全部观察维度。

在比较口径上，本文关于逐层预算分配与跨模型策略适用区间图谱的主要结论，建立在同量级 \texttt{INT4} 预算带内的经验可比性上，而不是严格的同预算比较。现有证据足以支持不同模型存在不同结构足迹、模型族/规模/任务共同塑造高性能区间，以及 heuristic 作为强基线所具有的结构性含义；但这些证据还不足以形式化证明某一单一策略在严格同预算条件下具有普适更优性，也不足以完成 allocator 相对 \texttt{KIVI-style} 的正式胜负裁决。因此，本文在 allocator 线上的主要贡献，应被理解为对跨模型策略适用区间的识别与组织，而不是对严格预算可比性问题的最终完成。

此外，本文不少结论本来就是条件性的，其解释力更多体现为在特定模型族、参数规模与任务类型下识别稳定结构，而不是给出脱离条件的普适规律。尤其在低比特部分，\texttt{INT4-RoleAlign} 与 \texttt{KIVI-style} 的比较虽然已被限制在同格式条件下，从而能够较干净地呈现校准理念的差异，但格式贡献、校准贡献与架构因素之间的作用仍未被完全正交解耦。因此，当前证据更支持将 \texttt{INT4-RoleAlign} 理解为一条在既定格式与已验证设定下成立的低比特恢复路径，而不是一条已经对所有模型族、规模与任务条件给出统一因果归因的普适方案。类似地，跨模型策略适用区间图谱揭示的也是条件性结构，而不是一个脱离模型与任务语境的普适规律。

系统层与动态决策层同样有明确的适用域限制。部署效率、性能交叉边界与显存收益的读数严格绑定于当前的 \texttt{NVIDIA H20}、固定软件栈、当前 backend 组合以及统一解码设定，因此不能被直接推广为跨硬件、跨 runtime 的一般定律。与此同时，\texttt{AutoK} 当前主要仍是模型级的静态 proposer，prompt 级自适应路由只呈现出弱改善或混合信号，尚不足以被写成正式结论；而结果溯源链保证的是结果的可追溯与可审计，而不是对未来 Transformer 变体或新长上下文注意力架构的自动泛化。换句话说，本文给出的，是一个在当前架构、协议和系统环境下可信、可审计的工作版本，而不是可以不经复核就直接迁移到未来环境的答案。

因此，本节讨论的局限性并不意味着前述结论失效，而是把它们成立的条件正式限定下来。当前关于规范路径保真、低比特恢复与跨模型策略适用区间图谱的结论，已经在评测协议、比较口径、条件适用域以及系统与时间外推边界上被清楚界定。下一节将在此基础上，只沿其中可直接转写为研究路线的三类方法性边界，进一步展开未来工作展望。
```

### 5.4 正文草稿（已接收，`2026-04-23 04:44`）

- 当前状态：已接收 / 已审核 / `采用（可接入 Draft）`
- 归属判断：`5.4 未来工作展望`
- 来源说明：本轮外部 LaTeX-ready 正文；原稿带 `\section/\label` 包装

#### 草稿原文摘要

- 采用 `1+3+1` 自然段结构：
  - 导语段
  - `per-prompt routing`
  - 更成熟的 `role-aware allocator`
  - `matched-budget formal compare`
  - 节末收束段
- 第一段将 `per-prompt routing` 降成值得优先检验的开放方向，而非自然成熟下一步。
- 第二段把 `role-aware allocator` 写成主线内部延伸，而非另开新方法。
- 第三段把 future work 收束到比较口径补全，而不是扩成 case 列表。
- 末段将三条方向统一收成 behavior-guided 主线的受控延伸。

#### 审核表

| 维度 | 结论 | 说明 |
|---|---|---|
| 归属判断 | `通过` | 内容明确属于 `5.4 未来工作展望` |
| 与 `This is Chapter 5 Writing.md` 对齐 | `通过` | 符合 `1+3+1` 结构，三段只承载三条 future work |
| 与 `story` 对齐 | `通过` | 只保留三条主方向，不扩成愿望清单 |
| 与 `5.3` 接口对齐 | `通过` | 写的是从已识别边界中择三类方法性边界继续推进，而非逐条镜像 |
| 章节职责边界 | `通过` | 不把 future work 写成已成立结论，不保留 exploratory 数字或脚本名 |
| 格式 gate | `通过` | `1+3+1`；`0` 图、`0` 表、`0` 显示公式 |
| 是否可接入 Draft | `通过` | 可形成 `5.4` 的 freeze-ready baseline |

#### 审核结论

- 审核结论：`采用（可接入 Draft）`
- 清洗动作：
  - 去除 `\section{未来工作展望}` 与 `\label{sec:ch5-future-work}` 包装
  - 本轮 prose polish 只做句级与段内节奏调整，不改变三条方向与边界口径
- 本轮 prose polish：
  - 去掉未来工作段落中的计划备忘录口气
  - 收顺第三段与结尾段的推进节奏
- 文风判断：`偏自然，轻微模板腔`
- 非阻塞建议：
  - 第三段最后一句略书面化，后续终稿 polish 时可再自然化

#### 清洗后的可用正文

```latex
上一节已经说明，本文关于规范路径保真、低比特恢复和跨模型策略适用区间图谱的结论，仍受评测协议、比较口径和系统成熟度等边界约束。顺着这些已经识别出的边界往前走，后续最值得推进的，并不是另起新的问题域，而是继续沿 prompt 级策略选择、角色感知预算机制和严格预算可比性三个方向扩展 behavior-guided 框架的适用域。

一条值得优先检验的路线，是将当前以模型级为主的预算建议机制进一步推进到 prompt 级的条件化策略选择。现阶段的 \texttt{AutoK} 主要仍根据离线行为敏感度画像给出模型级建议，而 prompt-adaptive 的探索只呈现出弱改善或混合信号，尚不足以被写成本文的正式结论。这意味着，现有跨模型策略适用区间图谱虽然已经能够为固定预算提供结构化读数，但它对输入层差异的响应仍然比较粗粒度。后续工作可以继续检验：如果把输入提示的结构表征与当前图谱中的高性能区间联系起来，是否能够形成稳定、可迁移的 prompt 级路由机制，使策略调用从“按模型给建议”进一步推进到“按输入做条件化选择”。

沿着同一主线往前走，另一个重要方向，是把当前只具接口形态的角色感知预算扩展推进为更成熟的 role-aware allocator。本文已经表明，K/V 两侧在低比特下并不对称，角色感知的预算语言也因此具备明确的结构基础；但从现有证据看，这种扩展更多体现为“可表达、可试验、局部可受益”，而不是已经形成稳定跨模型占优的成熟机制。也正因此，后续工作更重要的，并不是再提出新的分配名称，而是进一步把 Key 与 Value 两侧的预算描述、耦合关系和 profile 读数结合起来，使预算决策从当前较粗的逐层配置，推进为更细致、也更一致的联合分配机制。只有当这种角色差异真正进入 allocator 的核心决策中，behavior-guided 框架在分配层的解释力与可复用性才可能进一步加强。

在比较口径层面，allocator 线后续最需要补全的，也不是更多零散个案，而是更严格的同预算比较。第四章已经证明，在当前同量级 INT4 预算带下，不同模型确实会落入不同的策略适用区间，这一点支撑了图谱层面的结构判断；但这并不等于某一单一策略已经在严格同预算条件下完成了形式化胜负裁决。因此，未来工作的重点应放在设计更干净的严格预算可比性协议，以补全 allocator 相对现有基线在正式比较意义上的证据链。比较口径真正收紧之后，本文关于策略适用区间的解释力，才能继续向更可比较的结构主张推进，而不只是停留在经验上同量级预算的读数组织。

总之，本文后续最值得推进的，并不是平行扩张更多方法分支，而是沿同一 behavior-guided 主线继续向三个层面深化：在策略调用层面优先检验 prompt 级路由，在预算机制层面推进更成熟的 role-aware allocator，在比较口径层面补全严格同预算比较。未来工作的重点因此也不在于寻找一个脱离条件的“全局最优”策略，而在于持续扩大这一框架的可解释性、可比较性与适用域。
```

### 5.5 正文草稿（已接收，`2026-04-23 04:44`）

- 当前状态：已接收 / 已审核 / `采用（可接入 Draft）`
- 归属判断：`5.5 结语`
- 来源说明：本轮外部 LaTeX-ready 正文；原稿带 `\section/\label` 包装

#### 草稿原文摘要

- 采用单段 `5` 句结构。
- 第一句把问题重新提升到“KV Cache 量化是系统设计问题”。
- 第二、三句收回统一原则、分层实例链与跨模型策略适用区间图谱。
- 第四句轻触边界意识，不重开 `5.3`。
- 第五句将全文价值落在以 behavior 为中心的研究路径上。

#### 审核表

| 维度 | 结论 | 说明 |
|---|---|---|
| 归属判断 | `通过` | 内容明确属于 `5.5 结语` |
| 与 `This is Chapter 5 Writing.md` 对齐 | `通过` | 符合单段 `4–5` 句收口结构 |
| 与 `story` 对齐 | `通过` | 回答“整篇论文最终应被怎样理解”，不扩写 |
| 章节职责边界 | `通过` | 不重答 `RQ1–RQ3`，不重列三层创新点，不补新局限或 future work |
| 冻结口径 | `通过` | 保留“系统设计问题”与“图谱价值不在通用最优”两层最高判断 |
| 格式 gate | `通过` | 单段 `5` 句；`0` 图、`0` 表、`0` 显示公式 |
| 是否可接入 Draft | `通过` | 可形成 `5.5` 的 freeze-ready baseline |

#### 审核结论

- 审核结论：`采用（可接入 Draft）`
- 清洗动作：
  - 去除 `\section{结语}` 与 `\label{sec:ch5-closing}` 包装
  - 本轮 prose polish 只做轻量自然化，不改变单段 `5` 句结构
- 本轮 prose polish：
  - 轻调第三句末尾的人工打磨痕迹
- 文风判断：`自然`
- 非阻塞建议：
  - 第三句末尾“可承接的结构读法”略有人工打磨痕迹，后续终稿 polish 时可再收自然一点

#### 清洗后的可用正文

```latex
面向长上下文高效推理，KV Cache 量化并不是单纯寻找更低 bit-width 的局部优化问题，而是一个需要统一组织原则的系统设计问题。本文以 attention behavior 为统一组织对象，将 calibration 与 allocation 两层决策纳入同一 behavior-guided 框架，并在规范实例、低比特路径与预算扩展之间建立了可相互印证的分层实例链。更重要的是，本文并未把跨模型实验压成一个脱离条件的“全局最优”策略，而是识别并命名了跨模型策略适用区间图谱，使不同 family、scale 与 task 条件下的结构差异能够被读出、被解释，也为后续研究留下了一种可以继续承接的结构读法。在此基础上，本文也对这些结论的协议边界、比较口径与系统适用域作出了克制而明确的限定，从而使全文结论既保持解释力，也保持可信度。因而，本文的最终价值不在于提出一个永远最优的局部量化技巧，而在于为面向长上下文高效推理的 KV Cache 量化提供了一条以 behavior 为中心、可审计、可扩展并具备结构解释力的研究路径。
```

## 最终可回写正文块

### 5.1

```markdown
前四章已经完成问题界定、理论组织、方法实例化与实证验证。本节不再回放图表或模型个案，而是回到第一章提出的 `RQ1–RQ3`，从全文收束的角度概括本文的最终研究结论。

对于 `RQ1`，本文的回答是：以 attention behavior 为中心的行为引导校准已经不只是一个理论设想，而是在 `INT8 canonical path` 上落实为一条稳定、可复现且可审计的基础保真路径。第四章的规范路径验证表明，在最保守的位宽和受控协议下，这一原则能够在不依赖额外在线自适应的前提下保持与全精度高度一致的任务行为；官方 LongBench 真实数据对照则只承担评测协议一致性的 sanity check，进一步说明主文的合成评测并未系统性误判这一方向，但这并不构成对真实场景全面泛化的证明。换句话说，`RQ1` 真正回答的不是“INT8 本身容易做好”，而是 behavior-guided 原则已经在最干净的验证实例上落成了一条可信的基础保真路径。

对于 `RQ2`，本文的回答是：behavior-guided 原则不能机械地下推到对称 `INT4`，但它能够在角色感知的非对称路径中恢复为一条可用且可审计的低比特实例。第四章已经表明，对称 `INT4` 面对的并不是温和退化，而是具有明显架构依附性的阶跃失稳，其中 Key 精度下降是更主要的触发侧；相应地，`INT4-RoleAlign` 的意义也不在于宣称低比特问题已经普遍解决，而在于在当前已验证的同格式条件和 role-aware 设定下，给出了一条从对称失效走向可用恢复的清晰路径。因此，`RQ2` 的答案不是“INT4 必然成功”，而是只有在承认 `K/V` 角色差异、并据此组织格式与离线校准纪律时，低比特路径才可能稳定建立起来。

对于 `RQ3`，本文的回答是：由校准链路产出的 behavior sensitivity profile 不仅能够服务参数选择，也可以上升为跨模型预算分配的组织依据，并在当前已验证的预算范围内揭示一张由模型族、规模与任务共同决定的跨模型策略适用区间图谱。allocator 与 `AutoK` 的实证结果并未导向一个脱离条件的 universal winner，更稳定的事实是，不同模型会落入不同的高性能区间：有的更依赖早层保护，有的呈现中等规模的共识解，也有的表现为多个高性能簇并存而没有稳定最优。与此同时，heuristic 作为强基线并不是与本文主线相冲突的例外；它恰恰说明，在某些区间里，可行层集本身就很窄。behavior-guided 方法的价值，正在于识别这些区间何时出现、如何区分，并把原本分散的跨模型现象整理成可命名、可解释的结构读数，而不是把单一策略写成普适法则。

综上，对 `RQ1–RQ3` 的回答共同指向一个更高层的判断：attention behavior 可以作为贯通 calibration 与 allocation 的统一组织原则。它先在 `INT8 canonical path` 上建立了最干净的基础保真路径，随后在低比特场景下具体化为一条承认 `K/V` 角色差异的恢复路径，并进一步扩展成能够读出跨模型策略适用区间的结构化工具。因而，第五章后续已经无须再重复第四章的证据，而应转向说明这些研究结论的创新点、学术价值与适用边界。
```

`（当前阶段禁止回写 tex；后续若出现新一轮 `5.1` 草稿，必须先追加审核记录，再决定是否覆盖本 baseline）`

### 5.2

```latex
上一节已经概括了本文的最终结论。本节进一步要回答的是，这些结论为何不能只被理解为若干分散的实验现象，而应被看作一项具有统一组织原则、分层实例链和跨模型解释能力的研究。

在理论层面，本文并没有把 KV Cache 量化继续拆成几个彼此独立的局部优化问题，而是把 attention behavior 提升为同时组织 calibration 与 allocation 的统一对象。第三章从注意力近似误差传播分析出发，说明量化损伤真正被下游感知的，并不是孤立的张量数值偏差，而是由注意力分布偏移与聚合输出偏移共同构成的行为失真。由此，校准层与分配层不再只是两个并列的工程步骤，而被收束到同一条 behavior-guided 主线之下。本文在理论层面的推进，也因此不在于为既有量化流程再补一个局部校准目标，而在于为 KV Cache 量化提供了一种能够同时容纳规范验证、低比特实例和预算扩展的解释框架。

在方法层面，本文的价值也不在几项零散技巧的叠加，而在于同一原则下形成了一条层次分明的实例链。作为最保守也最干净的验证实例，\texttt{INT8 canonical path} 说明 behavior-guided 校准能够首先在规范位宽上建立稳定、可复现的基础保真路径；在与 \texttt{KIVI-style} 共享非对称格式的前提下，\texttt{INT4-RoleAlign} 的意义也不在经验上的一边倒优势，而在于它把低比特路径组织成了一条可固化、可审计、并能继续向后扩展的实例化路径。allocator 与 \texttt{AutoK} 同样不应被理解为脱离校准层另起炉灶的第二套方法，而应被视为由同一 behavior sensitivity profile 延伸出的预算决策扩展。与其说本文提出了若干彼此并置的技巧，不如说它把规范验证、低比特恢复与预算扩展组织成了一条前后衔接的分层实例链。

在经验层面，本文的创新同样不在于把某一种策略写成跨模型、跨任务、跨预算条件下的单一最优，而在于识别并命名了一张跨模型策略适用区间图谱。第四章的跨模型结果并没有把比较收束成一个脱离条件的普适赢家，更稳定的事实是，模型家族、参数规模与任务会共同塑造不同策略的高性能区间。本文真正提供的，也不是某个 allocator 在若干读数上赢了多少，而是一种用来辨认这些区间何时出现、如何分化以及为何分化的结构化视角。heuristic 在多个区间中表现为强基线，也并不削弱本文主线；相反，它提醒我们，在某些模型条件下，可行层集本来就很窄，而 behavior-guided 方法的学术价值，正在于把“强基线何时成立、何时失效”这类现象从经验杂讯提升为可解释的结构读数。

因此，本文的创新点与学术价值，并不在于孤立地提出某一个新的量化技巧，而在于以 attention behavior 为统一组织原则，把规范路径、低比特实例与跨模型策略适用区间图谱组织成一条具有解释力、可审计性与可扩展性的研究路径。也正是在这一意义上，本文不仅回答了 KV Cache 量化“如何压缩”的问题，也把这一问题从局部技巧比较推进到了组织原则与结构解释的层面。
```

`（当前阶段禁止回写 tex；后续若出现新一轮 `5.2` 草稿，必须先追加审核记录，再决定是否覆盖本 baseline）`

### 5.3

```latex
上一节讨论的是本文在理论组织、方法实例与实证图谱层面的学术价值。本节转而说明这些价值成立在怎样的范围之内。它们并不能脱离评测协议、比较口径、系统环境和时间外推条件而无条件成立；讨论局限性，也不是撤回前面的结论，而是把这些结论的适用域交代清楚。

从评测协议上看，本文的主要实证证据建立在统一协议下的 LongBench 风格合成基准任务上，官方 LongBench 真实数据对照仅在 \texttt{INT8 canonical path} 上承担评测协议一致性的补充检验。这说明本文已经能够在协议内给出稳定证据，也通过一块受控的真实数据对照排除了主文评测方向被系统性读错的可能，但这还不足以把相关结论外推为对更广泛真实应用场景的充分证明。尤其当输入分布、任务结构、提示风格与真实部署流程发生变化时，不同量化路径的相对表现及其适用边界仍可能随之改变。与此同时，本文虽然并列使用了 PPL、Needle 与任务级指标以降低单一度量带来的偏差，但这并不意味着已经穷尽了长上下文质量的全部观察维度。

在比较口径上，本文关于逐层预算分配与跨模型策略适用区间图谱的主要结论，建立在同量级 \texttt{INT4} 预算带内的经验可比性上，而不是严格的同预算比较。现有证据足以支持不同模型存在不同结构足迹、模型族/规模/任务共同塑造高性能区间，以及 heuristic 作为强基线所具有的结构性含义；但这些证据还不足以形式化证明某一单一策略在严格同预算条件下具有普适更优性，也不足以完成 allocator 相对 \texttt{KIVI-style} 的正式胜负裁决。因此，本文在 allocator 线上的主要贡献，应被理解为对跨模型策略适用区间的识别与组织，而不是对严格预算可比性问题的最终完成。

此外，本文不少结论本来就是条件性的，其解释力更多体现为在特定模型族、参数规模与任务类型下识别稳定结构，而不是给出脱离条件的普适规律。尤其在低比特部分，\texttt{INT4-RoleAlign} 与 \texttt{KIVI-style} 的比较虽然已被限制在同格式条件下，从而能够较干净地呈现校准理念的差异，但格式贡献、校准贡献与架构因素之间的作用仍未被完全正交解耦。因此，当前证据更支持将 \texttt{INT4-RoleAlign} 理解为一条在既定格式与已验证设定下成立的低比特恢复路径，而不是一条已经对所有模型族、规模与任务条件给出统一因果归因的普适方案。类似地，跨模型策略适用区间图谱揭示的也是条件性结构，而不是一个脱离模型与任务语境的普适规律。

系统层与动态决策层同样有明确的适用域限制。部署效率、性能交叉边界与显存收益的读数严格绑定于当前的 \texttt{NVIDIA H20}、固定软件栈、当前 backend 组合以及统一解码设定，因此不能被直接推广为跨硬件、跨 runtime 的一般定律。与此同时，\texttt{AutoK} 当前主要仍是模型级的静态 proposer，prompt 级自适应路由只呈现出弱改善或混合信号，尚不足以被写成正式结论；而结果溯源链保证的是结果的可追溯与可审计，而不是对未来 Transformer 变体或新长上下文注意力架构的自动泛化。换句话说，本文给出的，是一个在当前架构、协议和系统环境下可信、可审计的工作版本，而不是可以不经复核就直接迁移到未来环境的答案。

因此，本节讨论的局限性并不意味着前述结论失效，而是把它们成立的条件正式限定下来。当前关于规范路径保真、低比特恢复与跨模型策略适用区间图谱的结论，已经在评测协议、比较口径、条件适用域以及系统与时间外推边界上被清楚界定。下一节将在此基础上，只沿其中可直接转写为研究路线的三类方法性边界，进一步展开未来工作展望。
```

`（当前阶段禁止回写 tex；后续若出现新一轮 `5.3` 草稿，必须先追加审核记录，再决定是否覆盖本 baseline）`

### 5.4

```latex
上一节已经说明，本文关于规范路径保真、低比特恢复和跨模型策略适用区间图谱的结论，仍受评测协议、比较口径和系统成熟度等边界约束。顺着这些已经识别出的边界往前走，后续最值得推进的，并不是另起新的问题域，而是继续沿 prompt 级策略选择、角色感知预算机制和严格预算可比性三个方向扩展 behavior-guided 框架的适用域。

一条值得优先检验的路线，是将当前以模型级为主的预算建议机制进一步推进到 prompt 级的条件化策略选择。现阶段的 \texttt{AutoK} 主要仍根据离线行为敏感度画像给出模型级建议，而 prompt 级自适应方案的探索只呈现出弱改善或混合信号，尚不足以被写成本文的正式结论。这意味着，现有跨模型策略适用区间图谱虽然已经能够为固定预算提供结构化读数，但它对输入层差异的响应仍然比较粗粒度。后续工作可以继续检验：如果把输入提示的结构表征与当前图谱中的高性能区间联系起来，是否能够形成稳定、可迁移的 prompt 级路由机制，使策略调用从“按模型给建议”进一步推进到“按输入做条件化选择”。

沿着同一主线往前走，另一个重要方向，是把当前只具接口形态的角色感知预算扩展推进为更成熟的 role-aware allocator。本文已经表明，K/V 两侧在低比特下并不对称，角色感知的预算语言也因此具备明确的结构基础；但从现有证据看，这种扩展更多体现为“可表达、可试验、局部可受益”，而不是已经形成稳定跨模型占优的成熟机制。也正因此，后续工作更重要的，并不是再提出新的分配名称，而是进一步把 Key 与 Value 两侧的预算描述、耦合关系和 profile 读数结合起来，使预算决策从当前较粗的逐层配置，推进为更细致、也更一致的联合分配机制。只有当这种角色差异真正进入 allocator 的核心决策中，behavior-guided 框架在分配层的解释力与可复用性才可能进一步加强。

在比较口径层面，allocator 线后续最需要补全的，也不是更多零散个案，而是更严格的同预算比较。第四章已经证明，在当前同量级 INT4 预算带下，不同模型确实会落入不同的策略适用区间，这一点支撑了图谱层面的结构判断；但这并不等于某一单一策略已经在严格同预算条件下完成了形式化胜负裁决。因此，未来工作的重点应放在设计更干净的严格预算可比性协议，以补全 allocator 相对现有基线在正式比较意义上的证据链。比较口径真正收紧之后，本文关于策略适用区间的解释力，才能继续向更可比较的结构主张推进，而不只是停留在经验上同量级预算的读数组织。

总之，本文后续最值得推进的，并不是平行扩张更多方法分支，而是沿同一 behavior-guided 主线继续向三个层面深化：在策略调用层面优先检验 prompt 级路由，在预算机制层面推进更成熟的 role-aware allocator，在比较口径层面补全严格同预算比较。未来工作的重点因此也不在于寻找一个脱离条件的“全局最优”策略，而在于持续扩大这一框架的可解释性、可比较性与适用域。
```

`（当前阶段禁止回写 tex；后续若出现新一轮 `5.4` 草稿，必须先追加审核记录，再决定是否覆盖本 baseline）`

### 5.5

```latex
面向长上下文高效推理，KV Cache 量化并不是单纯寻找更低 bit-width 的局部优化问题，而是一个需要统一组织原则的系统设计问题。本文以 attention behavior 为统一组织对象，将 calibration 与 allocation 两层决策纳入同一 behavior-guided 框架，并在规范实例、低比特路径与预算扩展之间建立了可相互印证的分层实例链。更重要的是，本文并未把跨模型实验压成一个脱离条件的“全局最优”策略，而是识别并命名了跨模型策略适用区间图谱，使不同 family、scale 与 task 条件下的结构差异能够被读出、被解释，也为后续研究留下了一种可以继续承接的结构读法。在此基础上，本文也对这些结论的协议边界、比较口径与系统适用域作出了克制而明确的限定，从而使全文结论既保持解释力，也保持可信度。因而，本文的最终价值不在于提出一个永远最优的局部量化技巧，而在于为面向长上下文高效推理的 KV Cache 量化提供了一条以 behavior 为中心、可审计、可扩展并具备结构解释力的研究路径。
```

`（当前阶段禁止回写 tex；后续若出现新一轮 `5.5` 草稿，必须先追加审核记录，再决定是否覆盖本 baseline）`

## 下一轮增量维护规则

- 新草稿进入时，先追加审核记录，再决定是否更新“最终可回写正文块”
- 若新草稿与既有 `采用` 版本冲突，不覆盖旧记录，必须新增一轮审核结论
- 在 Chapter 5 真正进入回写任务前，本文档继续作为唯一正文整合容器
