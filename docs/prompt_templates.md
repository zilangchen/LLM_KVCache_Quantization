目标：<一句话描述你要什么>
上下文：当前仓库是 <项目>，相关模块/路径是 <...>
约束：

- 不改 public API（或：允许 breaking change，但要写迁移说明）
- 保持兼容 <版本/平台>
- 代码风格：<lint/format>
  验收标准：
- 新增/更新单元测试（说明覆盖点）
- 运行命令并通过：<例如 npm test / pytest -q / cargo test>
- 输出：给我一段变更摘要 + 关键 diff/文件列表
  执行偏好：
- 先给一个简短计划（可选）
- 然后直接动手改代码并跑测试
