# This is Chapter 4 Writing

**文档身份**：逐小节写作施工文档  
**当前用途**：记录第四章各小节的写作计划、段落职责、公式安排、图表落位与删改动作  
**当前状态**：第四章 `4.1–4.7` 已完整锁定。

---

## 0. 与 story 文档的分工

本文件与 [thesis_story_20260420.md](/Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/thesis_story_20260420.md) 的分工如下：

- `story` 文档负责冻结高层主线、章节目录、迁移规则与术语边界。
- 本文件负责第四章逐小节写作施工，粒度更细，直接服务于后续 `thesis/chapters/*.tex` 的改写。
- 本文件负责维护第四章 `4.1–4.7` 的逐小节写作施工与图表总表，但不承担高层主线裁决职责。

### 0.1 维护规则

- 默认按“小节”为单位追加，不做大范围重写。
- 每个小节至少写清：
  - 小节职责
  - 应做与不应做
  - 段落结构
  - 公式安排
  - 图表安排
  - 相对当前稿件的删改动作
- 本文件是施工说明，不直接替代正文。

### 0.2 术语冻结与正文用语纪律

本文件后续统一遵守 story 文档已冻结的中文术语规则。除必要专名外，正文与图表标题均以中文学术表达为主。

固定保留的专名包括：

- `KV Cache`
- `Transformer`
- `GQA`
- `Triton`
- `AutoK`
- `KIVI-style`
- `LongBench`
- `MixedKV`
- `TPOT`

正文、图表与标题优先使用以下中文表述：

- `canonical path` -> **规范路径**
- `operational proxy` -> **工程代理**
- `role-aware calibration` -> **角色感知校准**
- `Regime Map` -> **跨模型策略适用区间图谱**
- `Phase Boundary` -> **性能交叉边界**
- `LongBench tasks` -> **LongBench 风格合成基准任务**

说明：

- 本文件中若保留少量英文表达，仅作为施工提示，不代表正文最终表述。
- “最显著正向案例”“高性能簇共存而无稳定最优”等固定表述，必须与 story 文档保持一致。

### 0.3 跨章引用纪律

本文件内部统一使用冻结后的第四章编号体系：

- `4.1`：实验设置与可信性声明
- `4.1.1`：模型、硬件与实现环境
- `4.1.2`：评测任务、数据与指标
- `4.1.3`：比较方法与配置
- `4.1.4`：统计检验、显著性与数据溯源
- `4.2`：RQ1：行为引导校准的保真度验证
- `4.2.1`：INT8 规范路径保真度
- `4.2.2`：评测协议一致性检验：官方 LongBench 真实数据对照
- `4.3`：RQ2：低比特路径从对称失效到 RoleAlign 恢复
- `4.3.1`：对称 INT4 的架构依附性失效与阶跃崩塌
- `4.3.2`：K/V 角色敏感性与 MixedKV 完整诊断
- `4.3.3`：INT4-RoleAlign 与 KIVI-style 跨模型对比
- `4.4`：RQ3：跨模型预算分配与策略适用区间图谱
- `4.4.1`：同量级预算下的跨模型主表
- `4.4.2`：Mistral-7B：AutoK 的最显著正向案例
- `4.4.3`：Qwen2.5-3B：早层保护回救区间
- `4.4.4`：LLaMA-3.1-8B：中等规模共识区间
- `4.4.5`：Qwen2.5-14B：高性能簇共存而无稳定最优
- `4.5`：部署效率评估
- `4.5.1`：4K 序列下的 TPOT 对比
- `4.5.2`：长序列 TPOT 扩展规律
- `4.5.3`：性能交叉边界与 GQA 影响
- `4.5.4`：KV Cache 显存压缩率与部署建议
- `4.6`：实验结果讨论与效度威胁
- `4.6.1`：KL 与 MSE 校准目标的规模依赖性机制辨析
- `4.6.2`：INT4 结果的分层机制辨析
- `4.6.3`：Heuristic 强基线的结构性含义
- `4.6.4`：威胁效度与外推边界
- `4.7`：本章小结

规则：

- 在 `tex` 真正回写前，先建立旧编号到新编号的 label mapping
- 本文件内不得混用旧的第四章目录体系与冻结后的新体系
- 需要引用第三章时，以 [This is Chapter 3 Writing.md](</Users/chenzilang/Desktop/LLM_KVCache_Quantization/docs/This is Chapter 3 Writing.md>) 与 `story` 文档中已冻结的编号和职责为准

### 0.4 图表承重测试与数据门禁

第四章图表同样采用“承重测试”原则，而不是机械按数量增减。判断标准是：

> 去掉该图或该表后，该节的证据链是否在对应位置断裂。

当前已冻结的 `4.1` 规则如下：

- `4.1` 只承担“实验设置与可信性声明”
- `4.1` 不混入任何结果性句子
- `4.1` 不放图
- `4.1` 只保留 2 张正式表：表 `4-1` 与表 `4-2`

数据门禁：

- 官方 LongBench 真实数据对照不属于 `4.1.2` 的主评测矩阵，而在 `4.2.2` 中单独承担评测协议一致性检验
- `4.1.4` 中的 clean-provenance 说明必须与后续主表、主图的 provenance 规则一致
- 若 supporting rows 使用 legacy / backport 数据，必须在对应表注中显式说明

---

## 64. 第四章 4.1 写作计划

### 64.1 小节标题

保持为：

**4.1 实验设置与可信性声明**

这个标题现在是对的，不需要改。它比旧的“实验设置”多了一层 **credibility** 含义，能把统计、显著性、数据溯源、clean-provenance 一并合法地收进来。`story` 已经把这一节冻结为“只放设置，不放结果”，所以这里不能再容纳任何 `RQ1/RQ2/RQ3` 的实证结论。

### 64.2 小节职责

`4.1` 在第四章中的职责只有两个：

1. **给后续所有证据搭一个干净、可信、可审计的入口。**
2. **把“你测了谁、测了什么、怎么比、统计上能不能信、数据从哪里来”一次交代清楚。**

换句话说，`4.1` 回答的不是“你发现了什么”，而是：

> **为什么评委可以相信你接下来在 `4.2–4.6` 里给出的所有证据。**

### 64.3 本节不做什么

`4.1` 必须明确克制，不承担以下内容：

- 不出现任何结果性句子；
- 不提前写 `INT8` 的 mean \( \Delta = +0.02 \)；
- 不提前写 `INT4-RoleAlign` 与 `KIVI-style` 的比较结论；
- 不提前写 allocator 的跨模型适用区间；
- 不提前写 heuristic 是强基线；
- 不提前写 TPOT 谁更快、交叉边界在哪里；
- 不把官方 LongBench 真实数据对照当成主评测矩阵，它只属于 `4.2.2` 的**评测协议一致性检验**。

### 64.4 最优总结构

建议将 `4.1` 固定为：

- **1 个短导语**
- **4 个三级小节**
- **0 张图**
- **2 张正式表**
- **0 个正文显示公式**

正式表只保留：

- **表 4-1**：模型、家族、规模、GQA 配置与实验角色
- **表 4-2**：量化模式、校准策略、量化轴与默认执行后端

不建议再新增正式表。当前主稿本来就已经用 **表 4-1 + 表 4-2 + `4.1.2` 的分项文字说明 + `4.1.4` 的统计段** 构成了很稳定的设置层；一旦在 `4.1.2` 再加任务汇总表，后续表号会整体顺延，反而打乱 `4.2.1` 的规范路径表号节奏。

### 64.5 节首导语

`4.1` 开头建议只写 **2 段**。

**第 1 段**只做一件事：说清本章后续实证共享一套统一协议，且本节负责集中交代模型、任务、比较配置、统计检验和数据溯源。

**第 2 段**只做一件事：告诉读者 `4.1.1–4.1.4` 的功能顺序，别提前预告任何结果。

推荐开头句：

> 为保证后续各节证据能够在同一协议下被直接比较，本节集中说明实验模型与实现环境、评测任务与指标、比较方法与配置，以及统计检验与数据溯源规则。除必要的协议说明外，本节不报告任何结果，所有性能与质量读数统一留到后续 `RQ1–RQ3`、部署效率与讨论小节中展开。

### 64.6 图表总策略

`4.1` 的图表策略应当冻结为：

- **图：不放**
- **正式表：只保留 2 张**
- **任务说明：用分项文字，不新开表**
- **统一解码参数：放脚注或段内括注，不新开表**

原因很简单：`4.1` 的目标是“干净地交代设置”，不是“再做一轮协议可视化”。一旦放图，读者会误以为这里已经开始读结果。

---

## 65. 第四章 4.1.1 写作计划

### 65.1 小节标题

保持为：

**4.1.1 模型、硬件与实现环境**

### 65.2 小节职责

这一小节只做三件事：

1. 交代**模型覆盖面**：三个模型族、六个模型、从 1.5B 到 14B。
2. 交代**结构差异**：尤其是 `GQA/H_{kv}` 与层数差异。
3. 交代**实验环境**：单张 `NVIDIA H20`、软件栈、依赖冻结方式。

它的功能不是“介绍模型优缺点”，而是告诉读者：

> **后续跨模型证据为什么有资格被读成 family / scale / GQA 三维异质性，而不是偶然选了几台模型。**

### 65.3 本节不做什么

- 不在正文里提前讲谁赢谁输；
- 不在句子里塞入 `+4 分回救`、`最显著正向案例`、`top-tier` 这类结果性措辞；
- 不在这里展开任何任务读数；
- 不在这里讨论 `H_{kv}` 对低比特鲁棒性或融合核效率的具体作用，那些都属于后文证据层。

### 65.4 最优段落结构

建议 **3 个自然段 + 1 张表**。

**第 1 段：模型覆盖逻辑。**  
先说明为什么选这六个模型：它们覆盖 `Qwen2.5 / LLaMA-3.1 / Mistral` 三个家族，覆盖 `1.5B–14B` 尺度区间，且 `H_{kv}` 从 2、4 到 8 全部出现。

**第 2 段：引出表 4-1。**  
表 4-1 是本小节的承重件。正文只需要一句话说清：模型名、家族、参数规模、`(H_q,H_{kv})`、层数和实验角色见表。不要在段内把表内容抄一遍。

**第 3 段：硬件与软件环境。**  
单张 `NVIDIA H20 (98 GB HBM)`，`Python 3.12`、`PyTorch 2.8.0 (CUDA 12.8)`、`Transformers`、`Triton`，并说明依赖版本已经冻结。这里顺手交代统一贪婪解码设置即可，但只用脚注或行内括注，不单开表。当前主稿这部分材料已经很完整，基本保留即可。

### 65.5 公式安排

**不放显示公式。**

这一小节的对象是“实验矩阵与环境”，不是方法定义。最多只允许保留行内记号：

- `GQA`
- `(H_q, H_{kv})`
- `H_{kv}=8`

### 65.6 图表安排

**保留表 4-1，不放图。**

表 4-1 的结构建议延续现稿，但把最后一列“实验角色”改得更符合更新后的 `story`：

- `Qwen2.5-1.5B-Instruct`：**INT8 规范路径与真实数据对照锚点**
- `Qwen2.5-3B-Instruct`：**小模型早层保护回救区间**
- `Qwen2.5-7B-Instruct`：**same-family supporting case**
- `LLaMA-3.1-8B-Instruct`：**中等规模共识区间与 `H_{kv}=8` 架构对照**
- `Mistral-7B-Instruct-v0.3`：**AutoK 典型正向剖面**
- `Qwen2.5-14B-Instruct`：**高性能簇共存而无稳定最优**

这里要注意：这些“角色”可以写成**后续剖面的承载功能**，但不要写成已经成立的结果结论。`story` 对模型角色已经冻结，尤其 `1.5B`、`LLaMA-8B`、`Mistral-7B` 和 `14B` 的定位都已经更新过，所以表 4-1 的角色列必须按新版本改。

### 65.7 相对当前稿件的删改动作

- **保留**：现稿关于六个模型、`H_{kv}`、层数、硬件和软件环境的主体材料；
- **改写**：表 4-1 的“实验角色”列，使其与新 `story` 完全对齐；
- **删掉或降格**：句内直接使用 `AutoK strongest positive case`、`top-tier` 这类结果化表述，尽量改成“后续典型剖面承载对象”；
- **保留但压缩**：统一贪婪解码参数，只保留为脚注或行内说明。

### 65.8 完成后的目标印象

读完 `4.1.1`，评委脑子里应该只留下这一句：

> 这套实验矩阵不是随意挑的，而是有意识地覆盖了 family、scale 和 `GQA/H_{kv}` 三个结构维度，因此后文的跨模型证据具备可读性。

---

## 66. 第四章 4.1.2 写作计划

### 66.1 小节标题

保持为：

**4.1.2 评测任务、数据与指标**

### 66.2 小节职责

这一小节只做两件事：

1. 交代**主评测矩阵**到底由哪些任务、数据和指标构成；
2. 交代**每类评测对应回答什么问题**。

它最关键的作用，是把后文所有质量与效率读数统一到一套可理解的协议里。

### 66.3 本节不做什么

- 不讨论具体任务结果；
- 不在这里讲“哪种任务更难”；
- 不把官方 LongBench 真实数据对照写成主矩阵的一部分；
- 不在这里解释为什么 `INT8` 能保真或为什么 `INT4` 会崩；
- 不让 `4.1.2` 滑成 benchmark survey。

### 66.4 最优段落结构

建议 **6 个自然段，0 张图，0 张正式表**。

**第 1 段：总览。**  
先说本章评测覆盖五个维度：语言建模质量、精确检索、长文档理解、合成推理压测、系统效率。

**第 2 段：PPL。**  
只写数据集、评测窗口、`chunk_size=128`、它测的是什么。当前主稿里对 `chunk_size=128` 的说明很重要，要保留，因为它直接影响 KV 量化在 PPL 协议里的时间语义。

**第 3 段：Needle-in-a-Haystack。**  
写 4K/8K/16K/32K、20 个深度级别、exact match pass rate 即可。

**第 4 段：LongBench 风格合成基准任务。**  
这是本小节的关键段。必须明确：

- 主矩阵统一使用 **LongBench 风格合成基准任务**；
- 主表抽取的任务集合是什么；
- 各任务指标类型是什么；
- 由于使用合成数据，**绝对值不与官方榜单对比**，但方法间相对差异仍有意义。

**第 5 段：RULER。**  
只说明 4 个子任务、4 个长度点、宏平均得分和它与 Needle 的评分粒度差异。

**第 6 段：系统效率指标。**  
写清 `TPOT`、KV Cache memory、peak memory 即可。然后用一句话收束：

> 官方 LongBench 真实数据对照不属于本节主评测矩阵，而在 `4.2.2` 中作为评测协议一致性检验单独报告。

这一点必须显式写出来。更新后的 `story` 已经把 `4.2.2` 冻结为“评测协议一致性检验：官方 LongBench 真实数据对照”，所以 `4.1.2` 不能再像旧稿那样把 `A.18` 当成附录随口一提，而应该把它明确后移到 `4.2.2`。

### 66.5 公式安排

**不放显示公式。**

指标定义全部用自然语言说明即可。最多允许保留极少量行内符号，如：

- `TPOT`
- `F1`
- `Rouge-L`
- `Edit Similarity`

### 66.6 图表安排

**不放图，不新增正式表。**

原因有三点：

1. 当前主稿的任务说明已经可以用分项自然段清楚讲完；
2. 如果在这里新开任务汇总表，会整体推迟后面主结果表号；
3. `4.1.2` 的核心不是并列比较任务，而是建立协议入口。

如果导师强烈要求“设置节必须见一张任务表”，才考虑加一张**默认关闭**的摘要表，但不是主方案。

### 66.7 相对当前稿件的删改动作

- **保留**：当前 `PPL / Needle / LongBench-style / RULER / Efficiency` 五分法；
- **保留但收紧**：LongBench-style 合成任务的类别、指标与“不可直接对齐官方榜单”的诚实边界；
- **改写**：把末尾对 `A.18` 的附录式提及，改成一句明确的 forward reference：官方 LongBench 真实数据对照见 `4.2.2`；
- **删掉**：任何让读者误以为“官方 LongBench 也属于主矩阵”的表述。

### 66.8 完成后的目标印象

读完 `4.1.2`，评委脑子里应该只留下这一句：

> 这章不是只看一个 benchmark，而是用一组各司其职的协议来读出质量、检索、长文档、合成压测和效率；其中主矩阵是合成 LongBench 风格任务，官方 LongBench 真实数据对照另有专节承担 sanity check。

---

## 67. 第四章 4.1.3 写作计划

### 67.1 小节标题

保持为：

**4.1.3 比较方法与配置**

### 67.2 小节职责

这一小节要完成的唯一任务是：

> **把后文要比较的所有方法和配置放在同一个“正交设计维度”框架里说清楚。**

也就是说，它不是简单列 baseline，而是要让读者明白，后文比较主要沿三个维度展开：

1. **Calibration philosophy**
2. **量化轴**
3. **Scale 时间语义**

当前主稿这一节已经非常接近理想状态。真正要做的不是重写，而是**去掉其中偷带结果判断的句子**。

### 67.3 本节不做什么

- 不在这里开始比较 `RoleAlign` 和 `KIVI-style` 谁更强；
- 不在这里写“差异是小而有方向性的”这种结果性判断；
- 不在这里讲 `K/V` 独立消融结论；
- 不在这里讨论 allocator 的适用区间。

### 67.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：比较逻辑。**  
先说本章比较的是一组量化模式，不是单一 baseline；然后说明为什么要把它们按 calibration philosophy / quantization axis / scale time semantics 来组织。

**第 2 段：引出表 4-2。**  
表 4-2 是本小节承重件，保留现稿的形式最好。正文只需一两句话介绍：比较对象分成三类：`INT8/INT4 baseline` 作为 percentile 数值分布参考路径，`INT8-Canonical / 对称 INT4（behavior-guided）` 作为离线行为引导对称路径，`INT4-RoleAlign / KIVI-style` 作为 same-format 的 role-aware 低比特路径，其中前者是离线角色感知校准、后者是 runtime absmax/min 基线；`MixedKV` 仅作为诊断配置。

**第 3 段：解释三条正交维度。**  
这是本小节最重要的一段：

- calibration philosophy：`percentile` vs `KL` vs `runtime absmax/min`
- 量化轴：对称 per-group vs 非对称 per-channel/per-token
- scale 时间语义：离线固定、逐 token 动态、K/V 混合时间语义

**第 4 段：边界声明。**  
只保留两句话：

- `MixedKV` 仅用于 `4.3.2` 的 K/V 诊断，不作主线方法；
- `\tau^{-1}` 已降级为附录观察，本章主线不启用。

### 67.5 公式安排

**不放显示公式。**

这一小节只保留必要的行内配置记号：

- `K@INT8+V@INT4`
- `per-channel K + per-token V`
- `INT8-Canonical`
- `INT4-RoleAlign`
- `KIVI-style`

### 67.6 图表安排

**保留表 4-2，不放图。**

表 4-2 的列结构基本可以不动：

- 量化模式
- 校准策略
- 量化轴
- 比特宽度
- 默认 decode kernel

表下注应保留两条硬边界：

1. `INT4-RoleAlign` 与 `KIVI-style` 共享同一非对称格式，差异集中在 calibration philosophy；
2. `MixedKV` 为诊断配置，不作主线比较。

### 67.7 相对当前稿件的删改动作

- **保留**：当前表 4-2 及“三个设计维度”的整体框架；
- **删掉**：当前 `4.1.3` 末尾那句带结果口气的话——“本文立场（第 4.2 节）是：calibration philosophy 差异在同 format 下对 quality / PPL / Needle 的影响是小而有方向性的，不是 winner-take-all 的缺口。”
  这句话虽然学术上诚实，但它已经是结果判断，不应留在设置节。
- **保留**：`MixedKV` 仅用于 `4.3.2` 诊断这一声明；
- **保留**：`\tau^{-1}` 不启用，只作附录诊断。

### 67.8 完成后的目标印象

读完 `4.1.3`，评委脑子里应该只留下这一句：

> 后文所有比较都放在一套清楚的设计坐标系里：校准哲学、量化轴和 scale 时间语义是彼此正交的，因此比较是可解释的，不是杂糅的。

---

## 68. 第四章 4.1.4 写作计划

### 68.1 小节标题

保持为：

**4.1.4 统计检验、显著性与数据溯源**

### 68.2 小节职责

这一小节的任务非常关键：

> **把第四章从“看起来像一堆数字”提升为“有统计纪律、有 provenance 纪律的一章证据”。**

因此它必须同时承担两层职责：

1. **统计可靠性声明**
2. **数据来源与 clean-provenance 声明**

### 68.3 本节不做什么

- 不再解释 benchmark 本身；
- 不再列模型硬件；
- 不在这里重讲任何方法；
- 不在这里评论结果；
- 不在这里长篇讨论威胁效度，那是 `4.6.4`。

### 68.4 最优段落结构

建议 **5 个自然段，0 图，0 表**。

**第 1 段：数据溯源与 clean-provenance 入口。**  
当前主稿把 “Clean-provenance 数据基线” 放在了第四章开头、`4.1` 之前。新的结构里，这段内容应整体并入 `4.1.4` 的开头，因为它本质上就是可信性声明的一部分，而不是章首导语。这里要说明：

- 正文哪些结果来自 canonical clean rerun；
- provenance pin 如何冻结；
- 哪些 supporting rows 若使用 legacy/backport，会在对应表注中显式说明，而不是在这里扩写。

这一步非常重要，因为更新后的 `story` 已经把“数据溯源”正式写进 `4.1.4` 了。

**第 2 段：随机性控制与 seed 设计。**  
写清质量评测 5 个固定 seeds，吞吐量测试 8 个固定 seeds，统一贪婪解码，以此消除 sampling 随机性。

**第 3 段：Bootstrap、sign-flip、BH-FDR。**  
这里保留当前主稿的三件套：

- 95% Bootstrap CI（10,000 次）
- 符号翻转置换检验
- Benjamini-Hochberg FDR

但语言要更整洁：这是 protocol，不是讨论。

**第 4 段：非劣性读法与 PPL 确定性。**  
这里有两点必须保留：

- non-inferiority 在本文里是 “CI bound vs margin” 的 informal operational reading；
- 贪婪解码下 PPL 在同一硬件与同一软件栈上是确定的，因此 PPL 不读 p-value，而读 effect size。

当前主稿这部分写得很扎实，可以基本保留。

**第 5 段：收束句。**  
用一句话说明：后文各主表的统计解释，都应在上述协议和边界下理解。

### 68.5 公式安排

**原则上不放显示公式。**

最多允许保留一个行内表达式，例如：

- `n = 5`
- \( p_{\min} = 3/33 \approx 0.091 \)

但不要开新公式环境。`4.1.4` 是 protocol section，不是数学 section。

### 68.6 图表安排

**不放图，不放表。**

这一节的信息更适合做成紧凑的自然段，而不是再开一张统计表。如果真把它做成表，会严重破坏 `4.1` 的阅读流。

### 68.7 相对当前稿件的删改动作

- **整体迁入**：把当前第四章开头那段 `Clean-provenance 数据基线` 移到这里；
- **保留**：当前关于 Bootstrap、sign-flip、BH-FDR、PPL determinism 的主体材料；
- **压缩**：尽量少举具体模型 PPL 数值例子，除非导师要求保留一个示例来证明 determinism；
- **保留但收紧**：sign-flip 下界、non-inferiority 的非 TOST 说明；
- **明确**：统计说明服务于后文表格解读，而不是在这里开始讨论方法差异。

### 68.8 完成后的目标印象

读完 `4.1.4`，评委脑子里应该只留下这一句：

> 这章后面的数字不是“凭感觉报出来的”，而是在固定 seed、固定解码、Bootstrap、sign-flip、BH-FDR 和 clean-provenance 约束下生成并解释的。

---

## 69. 4.1 相对当前稿件的总手术动作

这一节的总改法可以概括为 6 条：

1. **把旧 `4.1.5` 整节剥离出去，升格为新 `4.2.1`。**  
   这是 `4.1` 纯设置化的第一刀。

2. **把章首游离的 clean-provenance 说明吸收到 `4.1.4`。**  
   这样 `4.1` 才真正配得上“可信性声明”这五个字。

3. **保留现有两张正式表，不新增图。**  
   即：只保留表 4-1 和表 4-2。

4. **把 `4.1.2` 里的官方 LongBench 提及改成对 `4.2.2` 的明确前向引用。**  
   不再继续写成附录式顺带一提。

5. **把 `4.1.3` 里所有偷带结果判断的话删掉。**  
   设置节只定义比较对象和配置，不预判谁强谁弱。

6. **把 `4.1.1` 的模型角色列改成与更新后 story 一致。**  
   尤其是 `Qwen2.5-1.5B` 的锚点角色、`LLaMA-3.1-8B` 的中等规模共识角色、`Mistral-7B` 的 AutoK 典型剖面角色，以及 `Qwen2.5-7B` 的 supporting role。

---

## 70. 4.1 完成后的目标印象

读完整个 `4.1`，评委脑子里应该只留下这一句话：

> **第四章接下来的所有结果都建立在一套干净、可复现、可比较、可审计的实验协议之上；因此后文真正要讨论的，不再是“你到底怎么测的”，而是“这些证据是否成立、边界在哪里”。**

---

## 71. 第四章 4.2 写作计划

### 71.1 小节标题

保持为：

**4.2 RQ1：行为引导校准的保真度验证**

这个标题现在是对的，不需要再改。它准确说明：这一节不是在做低比特恢复，也不是在做 allocator 图谱，而是在回答一个更基础的问题——**behavior-guided calibration 本身是否先能站住**。

### 71.2 小节职责

`4.2` 在第四章中的职责只有一个：

> **先用最保守、最干净的 `INT8 canonical path` 证明校准层能够闭环；再用一块范围受控的官方 LongBench 真实数据对照，证明这个结论不只停留在本文的合成主协议内部。**

因此，`4.2` 必须形成一个非常清晰的两步证据链：

第一步是 **internal fidelity proof**：`4.2.1` 证明 canonical path 相对 `FP16` 保持 empirical indistinguishability。  
第二步是 **protocol consistency check**：`4.2.2` 证明在官方真实数据上，结论方向没有反转，从而初步排除“合成协议系统性高估保真度”的担忧。

### 71.3 本节不做什么

`4.2` 必须明确克制，不承担以下内容：

- 不讨论对称 `INT4` 的崩塌；
- 不讨论 `K/V` 完整诊断；
- 不比较 `INT4-RoleAlign` 与 `KIVI-style`；
- 不提前进入 allocator / `AutoK`；
- 不在这里展开 `KL vs MSE` 的规模依赖性，那必须留到 `4.6.1`；
- 不把 `4.2.2` 写成“全面真实场景泛化证明”。

换句话说，`4.2` 必须是 **INT8-only、RQ1-only、校准层-only** 的一节。

### 71.4 最优总结构

建议将 `4.2` 固定为：

- **1 个短导语**
- **2 个三级小节**
- **0 张图**
- **2 张正式表**
- **0 个正文显示公式**

这一节要做成 **table-led section**，而不是 figure-led section。原因很简单：`RQ1` 的强项是“读数非常干净”，不是“机制可视化很多”。

### 71.5 节首导语

`4.2` 开头建议只写 **2 段**。

**第 1 段**只做一件事：把 `4.1` 的协议层与 `4.2` 的证据层切开。  
推荐开头句：

> 第 4.1 节已经交代统一实验协议。本节转而回答 `RQ1`：behavior-guided calibration 是否能够在最保守位宽上形成稳定、可复现的基础保真路径。

**第 2 段**只做一件事：告诉读者这节证据的顺序。  
建议落成：

> 为避免在低比特与跨模型比较中混入额外变量，本节首先在 `INT8 canonical path` 上建立最干净的规范路径证据，随后再以一块范围受控的官方 LongBench 真实数据对照，对主评测协议做一致性检验；低比特恢复与跨模型结构问题统一留到后续 `4.3–4.4` 展开。

### 71.6 图表总策略

`4.2` 的图表策略应冻结为：

- **图：不放**
- **正式表：2 张**
- **主表 1**：`4.2.1` 的 canonical fidelity 表
- **主表 2**：`4.2.2` 的 official LongBench sanity-check 表

其中最关键的一刀是：

> **旧 `4.1.5` 那张表必须被“净化”为纯 `FP16 vs INT8-Canonical` 表，不再让 `INT4-RoleAlign / KIVI-style` 两列提前侵入 `RQ1`。**

这是 `4.2` 写干净的关键动作。旧稿之所以能这么写，是因为当时 `4.1.5` 还混在设置节里；现在既然 `4.2` 被正式冻结成 `RQ1` 证据节，这张表就必须只服务 `RQ1`。

---

## 72. 第四章 4.2.1 写作计划

### 72.1 小节标题

保持为：

**4.2.1 INT8 规范路径保真度**

### 72.2 小节职责

这一小节只承担一个任务：

> **证明 behavior-guided calibration 至少已经在一个最干净、最保守、最可复现的位宽设置上闭环。**

这不是在证明“INT8 很强”，而是在证明：

- 框架先在最容易审计的 setting 上成立；
- 成立的对象是 **calibration layer itself**；
- 这个成立与 Triton 的工程加速是两回事。

当前主稿旧 `4.1.5` 的数据已经完全够用：Qwen2.5-1.5B、三任务、相对 `FP16` 的 mean \( \Delta = +0.02 \)，这正是 canonical path 该承担的那种“干净而克制”的证据。

### 72.3 本节不做什么

- 不讨论 `INT4`；
- 不把 `INT4-RoleAlign` 与 `KIVI-style` 放成主列；
- 不展开“为什么是 Qwen2.5-1.5B”这种模型角色讨论，那在 `4.1.1` 已交代；
- 不把 fidelity 归功于 Triton fused kernel；
- 不在这里谈“真实数据泛化”，那是 `4.2.2` 的事。

### 72.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：canonical path 的角色声明。**  
第一段必须把 `INT8 canonical path` 的地位讲清楚：它是框架的规范验证实例，不是最终压缩极限，也不是工程效率展示。

**第 2 段：引出主表。**  
这里直接引出一张“净化后”的主表：三任务 `FP16 vs INT8-Canonical vs \Delta`。不要再把 `INT4-RoleAlign / KIVI-style` 塞进表里。

**第 3 段：读数解释。**  
这一段只读三件事：
- mean \( \Delta = +0.02 \)
- 三个任务的符号方向不一致
- 每个任务的变化量都很小，因此不能读成系统偏移

**第 4 段：校准层闭环，不是 kernel 结论。**  
这一段必须单独写出来，作用是切断评委的误读：

> 这里证明的是校准产物本身在 canonical scale 下能够保持保真，而不是 fused kernel 让质量变好了；kernel 层的价值只在 `4.5` 的部署效率中讨论。

### 72.5 公式安排

**不放显示公式。**

这一小节是干净读数节，不需要公式。最多允许少量行内符号：

- `FP16`
- `INT8-Canonical`
- mean \( \Delta \)

### 72.6 图表安排

**只保留 1 张正式表，不放图。**

这张表建议直接沿用旧 `4.1.5` 的数据，但必须改瘦为：

- 任务
- `FP16`
- `INT8-Canonical`
- \( \Delta \)

也就是说：

> **旧表里作为“format context”的 `INT4-RoleAlign / KIVI-style` 两列，应删掉或降格为一句脚注，不再以同权列出现。**

**固定回写纪律：**

> 正文回写到 `tex` 时，`4.2.1` 的 canonical 主表必须被净化为纯 `FP16 vs INT8-Canonical` 表；旧正文资产中若仍保留 `INT4-RoleAlign / KIVI-style` 参考列，只能降为脚注或旁注，不得以同权主列出现。

这样做有两个好处：

1. `4.2.1` 会变得极其干净，只服务 `RQ1`；  
2. `4.3` 的 `RQ2` 主证据不会提前泄露到 `4.2` 里。

如果全章顺排，建议这张表继续保留为 **表 4-3**；这样它与旧稿的 canonical 主表延续性最强。

### 72.7 相对当前稿件的删改动作

- **整体上移并重编号**：把旧 `4.1.5` 原封不动提升为新 `4.2.1`；
- **保留**：Qwen2.5-1.5B 的三任务 canonical matrix 与 mean \( \Delta = +0.02 \)；
- **删掉**：旧表中 `INT4-RoleAlign / KIVI-style` 的并列主列；
- **改写**：旧小节的首段与尾段，使其明确服务 `RQ1`，而不是旧式“结果插在设置后”；
- **保留但强化**：那句“保真度不依赖 Triton，Triton 只影响 TPOT”的边界声明，把它独立写成结尾段，而不是顺手提一句。

### 72.8 完成后的目标印象

读完 `4.2.1`，评委脑子里应该只留下这一句：

> behavior-guided calibration 至少已经在一个最干净的 `INT8 canonical path` 上闭环了；因此，后文真正的问题不再是“这个原则是否存在”，而是“把位宽继续压低时，它还能否成立”。

---

## 73. 第四章 4.2.2 写作计划

### 73.1 小节标题

保持为：

**4.2.2 评测协议一致性检验：官方 LongBench 真实数据对照**

这个标题现在必须锁死，不能再回到“真实场景泛化”或“官方 LongBench 泛化验证”那种高调标题。因为 story 已经明确要求这里的定位是 **sanity check / credibility supplement**，不是 full generalization proof。

### 73.2 小节职责

这一小节只承担一个任务：

> **用一块范围受控、目标很窄的官方真实数据对照，验证主文合成评测协议没有把 canonical fidelity 结论系统性地带偏。**

也就是说，`4.2.2` 的真正问题不是“模型在真实世界上表现多好”，而是：

- 主文 synthetic LongBench-style protocol 的方向性结论，是否会在官方数据上翻车；
- 至少在最保守的 `INT8 canonical path` 上，答案是不是“没有翻车”。

当前附录 `A.18` 已经把这个 scope 写得很清楚：Qwen2.5-1.5B 单模型、3 个任务、单 seed、每任务最多 50 样本，宏平均绝对差仅 \(0.01\) pp，所有单任务差异都小于 \(0.5\) pp。这个尺度非常适合写成 sanity check，但绝不适合写成全面泛化证明。

### 73.3 本节不做什么

- 不声称“真实场景泛化已经被充分证明”；
- 不把它写成主评测矩阵的一部分；
- 不在这里比较 `INT4`；
- 不做正式统计显著性宣称；
- 不用这块单 seed 小样本数据去支撑跨模型结论；
- 不在这里展开 `KL vs MSE` 的讨论，只能埋 forward pointer 到 `4.6.1`。

### 73.4 最优段落结构

建议 **4 个自然段 + 1 张小表**。

**第 1 段：为什么要补这一节。**  
先承认主文主矩阵是 LongBench 风格合成基准任务，因此需要一块官方真实数据对照来检查协议一致性。

**第 2 段：把 scope 坦白写满。**  
这一段必须一次写全：
- `Qwen2.5-1.5B-Instruct`
- 32K 截断
- seed = 1234
- 每任务最多 50 样本
- 三个官方子任务：NarrativeQA、HotpotQA、GovReport
- 只测 `INT8 canonical path`

这段的语气必须像实验边界声明，而不是成果预告。

**第 3 段：读小表。**  
这里直接读三组单任务差：
- NarrativeQA \( -0.43 \) pp
- HotpotQA \( +0.31 \) pp
- GovReport \( +0.08 \) pp
- 宏平均绝对差 \(0.01\) pp

然后马上落到一句更稳的结论：

> 差异方向在任务间有正有负，幅度均很小，因此更像量化噪声下的微小波动，而不是协议切换导致的系统偏移。

**第 4 段：结论降调 + 前向引桥。**  
这一段必须同时完成两件事：
1. 结论只落到：**方向一致，初步排除系统性偏差**
2. 末尾加 forward pointer：
   **关于为何采用 KL 而非 MSE，以及该目标在不同规模上的必要性差异，将在 `4.6.1` 节讨论。**

**固定前向引桥句（正文建议直接采用）：**

> 关于为何采用 KL 而非 MSE，以及该目标在不同规模上的必要性差异，将在 `4.6.1` 节讨论。

### 73.5 公式安排

**不放显示公式。**

这一节只读小表，不需要任何公式。  
最多允许行内记号：

- \( \Delta \) (pp)
- `seed=1234`
- `n \le 50`

### 73.6 图表安排

**只放 1 张小表，不放图。**

这张表建议直接由附录 `A.18` 转入正文，结构保持最简：

- 任务
- `FP16`
- `INT8-Canonical`
- \( \Delta \)（pp）

表注必须明确写出：

- 单模型
- 单种子
- 小样本
- 用途是 protocol sanity check，不构成全面统计检验

如果全章顺排，这张表应接在 canonical 主表后，作为新的 **表 4-4**；这样后面的 `RQ2` 表号整体顺延即可。

### 73.7 相对当前稿件的删改动作

- **整体前移**：把附录 `A.18` 正式前移到正文 `4.2.2`；
- **改题**：从任何“泛化验证 / 真实场景泛化”口气，统一降为“评测协议一致性检验：官方 LongBench 真实数据对照”；
- **保留**：现有三任务官方表及其小幅差异读数；
- **强化**：把“局限：仅 1.5B、3 任务、单种子、不构成全面统计检验”前移到正文，而不是只留在附录；
- **新增**：一条指向 `4.6.1` 的 forward pointer；
- **删掉**：任何暗示“真实世界全面泛化已证”的措辞。

### 73.8 完成后的目标印象

读完 `4.2.2`，评委脑子里应该只留下这一句：

> 主文的 synthetic LongBench-style protocol 至少没有在最保守的 `INT8 canonical path` 上把 fidelity 方向读错；官方真实数据给出的结论方向与主文一致，因此这是一块很值钱的 credibility supplement，但还不是全面泛化总证。

---

## 74. 4.2 相对当前稿件的总手术动作

`4.2` 的总改法可以概括为 6 条：

1. **把旧 `4.1.5` 纯重编号为新 `4.2.1`。**  
   这是一次低风险迁移，核心数据不用重做。

2. **把旧 canonical 主表净化成纯 `FP16 vs INT8-Canonical` 表。**  
   `INT4-RoleAlign / KIVI-style` 两列退出 `RQ1` 主表。

3. **把附录 `A.18` 前移为新 `4.2.2`。**  
   它不再是附录里顺带一提，而是 `RQ1` 的第二步证据。

4. **把 `4.2.2` 的语气整体降为 sanity check。**  
   标题、首段、结论段都必须显式写出 scope 限制。

5. **在 `4.2.2` 末尾埋 forward pointer 到 `4.6.1`。**  
   这样读者在看到 `INT8` 成功后，不会一直带着“那为什么是 KL？”的悬念往后拖。

6. **在 `4.2` 结尾加一句桥到 `4.3`。**  
   建议固定成这种语气：
   > 至此，behavior-guided calibration 已在 canonical 位宽上建立基础保真路径；接下来的问题，不再是这一原则是否成立，而是当位宽继续压低到 `INT4` 时，它会在哪里失效、又如何被恢复。

---

## 75. 4.2 完成后的目标印象

读完整个 `4.2`，评委脑子里应该只留下这一句话：

> **RQ1 已经被两步回答：一方面，behavior-guided calibration 在 `INT8 canonical path` 上能够形成干净、可复现的基础保真路径；另一方面，官方 LongBench 的小范围真实数据对照表明，这个结论至少没有被主文的合成协议系统性带偏。**

再往下读 `4.3` 时，读者的心态就会自然切换成：

> 原则已经立住了，现在真正值得看的，是它在低比特压力下如何失效、如何恢复。

---

## 76. 第四章 4.3 写作计划

### 76.1 小节标题

保持为：

**4.3 RQ2：低比特路径从对称失效到 RoleAlign 恢复**

这个标题现在是对的，不需要再改。它比旧稿的 “INT4 推进：从对称 INT4 失效到 RoleAlign vs KIVI 对比” 更学术，也更符合当前章职能：这里不是讲一个“INT4 story”，而是在回答一个明确研究问题——**当位宽压到低比特后，behavior-guided 路径是如何先失效、再被恢复的。**

### 76.2 小节职责

`4.3` 在第四章中的唯一任务是：

> **把 low-bit path 的证据链做完整。**

这条证据链必须严格分三步：

第一步，在 `4.3.1` 先立住：对称 `INT4` 不是“轻微退化”，而是会在一部分模型上出现**阶跃崩塌**；但这种崩塌又不是绝对无例外的，因此它必须被写成**架构依附性失效**。  
第二步，在 `4.3.2` 再证明：这个崩塌不是平均发生在 `K/V` 两侧，而是**Key 侧主导触发**，`MixedKV` 则提供“保护 Key 后可明显恢复”的诊断性证据。  
第三步，在 `4.3.3` 最后证明：当格式升级到 `per-channel K + per-token V` 后，`INT4-RoleAlign` 可以在与 `KIVI-style` **同 format、差 calibration philosophy** 的设置下闭合 low-bit 恢复路径；而它的独立价值，更多体现在**离线可审计产物、可扩展接口与与 allocator 的自然衔接**，而不是 winner-take-all 的大幅质量差距。

### 76.3 本节不做什么

`4.3` 必须明确克制，不承担以下内容：

- 不进入 allocator / `AutoK` 的跨模型图谱，那是 `4.4`；
- 不讲部署效率，那是 `4.5`；
- 不展开 `KL vs MSE` 的规模依赖，那是 `4.6.1`；
- 不在这里放大段 “三层诚实分析”，那一部分已被 story 正式后移到 `4.6.2`；
- 不把 `4.3.3` 写成 “RoleAlign 完胜 KIVI-style” 的结论节。

换句话说，`4.3` 只能完成 **low-bit failure chain**，不能提前进入更高层的 regime 解释或讨论节。

### 76.4 最优总结构

建议将 `4.3` 固定为：

- **1 个短导语**
- **3 个三级小节**
- **1 段节末短收束**
- **2 张主图**
- **4 张正式表**

主方案里，图表职责这样分：

- `4.3.1`：**1 张小型汇总表**
- `4.3.2`：**2 张图 + 2 张主结果表**
- `4.3.3`：**1 张主结果表**

这里我明确给出一个施工判断：

> **`4.3.1` 最好用“小型汇总表”承重，而不是新开图。**

原因不是美观，而是结构稳定性：第三章 `3.2` 现在已经固定为从 `4.3.2` 同源结果中抽取出的 bit-layout 压缩诊断视图；如果现在在 `4.3.1` 新开一张图，会把原来的 `Figure 4-1 / 4-2` 全部顺延，导致第三章与第四章之间的图号联动更麻烦。用一张小表承重，最稳。

### 76.5 节首导语

`4.3` 开头建议只写 **2 段**。

**第 1 段**只做一件事：承接 `4.2`。  
推荐开头句：

> 第 4.2 节已经在 `INT8 canonical path` 上建立了 behavior-guided calibration 的基础保真路径。本节进一步回答 `RQ2`：当位宽继续压低到 `INT4` 时，这一路径会在何处失效，又如何被恢复。

**第 2 段**只做一件事：告诉读者这节的三步证据顺序。  
建议固定为：

> 为避免把低比特结果写成并列实验堆叠，本节先用一块简洁证据立住对称 `INT4` 的架构依附性失效，再通过完整的 `K/V` 诊断定位 Key 主导退化，最后在同一非对称格式下比较 `INT4-RoleAlign` 与 `KIVI-style`，完成从 failure 到 recovery 的闭环证据链。

### 76.6 图表总策略

`4.3` 的图表策略应冻结为：

- **`4.3.1` 用小表，不用新图**
- **`4.3.2` 继续保留旧 `Figure 4-1` 和 `Figure 4-2`**
- **`4.3.3` 只保留 1 张跨模型主对比表，不强行再加比较图**
- **原旧 `4.2.3` 的“三层诚实分析”不再留在这里，整体后移到 `4.6.2`**

若全章顺排，推荐的新表号逻辑是：

- `4.2` 已占 **表 4-3 / 表 4-4**
- `4.3.1` 小型汇总表 = **表 4-5**
- `4.3.2` 的 PPL 表与多任务双 panel 表 = **表 4-6 / 表 4-7**
- `4.3.3` 的同 format 跨模型主对比 = **表 4-8**

图号则尽量保持旧序：

- 旧 `Figure 4-1`、`Figure 4-2` 继续作为 `4.3.2` 的主图，不改为别的编号。  
  这会让后续 `3.2 ↔ 4.3.2` 的互文最稳。

---

## 77. 第四章 4.3.1 写作计划

### 77.1 小节标题

保持为：

**4.3.1 对称 INT4 的架构依附性失效与阶跃崩塌**

这个标题必须锁死。不能退回“对称 INT4 的系统性失效”，因为最新 story 已经明确要求：**必须显式容纳 `LLaMA-3.1-8B (Hkv=8)` 的架构例外**。当前主稿旧 `4.2` 开头其实也已经诚实写了这一点：Qwen2.5-1.5B / 7B 的对称 `INT4` 会从 Needle 100% 直接跌到 0%，但 `LLaMA-3.1-8B` 仍可维持 98%，因此标题必须带上“架构依附性”这个限定。

### 77.2 小节职责

这一小节只承担一个任务：

> **把 symmetric INT4 的“cliff pattern”先立住。**

它不是完整机制诊断，也不是恢复路径比较，而是整个 `RQ2` 的门槛证据：

- `INT8` 的成功不等于把位宽机械降到 `INT4` 仍然成立；
- 真正发生的不是渐进变差，而是在部分模型上出现**100% → 0%** 的检索阶跃；
- 但这种阶跃又不是绝对普适，因此必须被写成**架构依附性**而非无条件“系统性失效”。

### 77.3 本节不做什么

- 不展开 `K/V` 独立消融细节；
- 不解释 `MixedKV` 如何恢复；
- 不比较 `RoleAlign` 与 `KIVI-style`；
- 不在这里重讲 softmax 理论，那些第三章已经讲过；
- 不在这里做长版讨论。

它的目标只是把读者从 `4.2` 的 canonical success，送进 `4.3` 的 low-bit problem space。

### 77.4 最优段落结构

建议 **4 个自然段 + 1 张小型汇总表**。

**第 1 段：从 `INT8` 走向 `INT4` 不是线性降级。**  
直接点出：对称 `INT8` 的可行，不代表对称 `INT4` 仍是温和退化；量化级别从 255 个有效等级骤降到 15 个有效等级之后，low-bit behaviour 会发生质变。这个段落要短，不要抢机制分析的内容。

**第 2 段：立住 cliff pattern。**  
这里直接写现有旧稿里最强的一组现象：Qwen2.5-1.5B / 7B 的对称 `INT4` Needle 从 100% 直接掉到 0%，且 PPL 退化超过 15%；这说明它不是“稍差一点”，而是已经越过功能临界点。

**第 3 段：主动写出 LLaMA 例外。**  
这一段必须单独写：`LLaMA-3.1-8B (Hkv=8)` 是唯一显式例外，INT4 Needle 仍可维持 98%，提示这里不仅有位宽问题，还有架构层面的噪声稀释效应。注意，这里只写“提示”即可，不展开长机制分析。

**第 4 段：把问题送到 `4.3.2`。**  
结尾固定落成：

> 因此，本节所确认的不是“INT4 普遍不可用”，而是“对称 `INT4` 在一类模型上会出现架构依附性的阶跃崩塌”；接下来的关键问题，是这一崩塌究竟由 `K` 侧还是 `V` 侧首先触发。

### 77.5 公式安排

**不放显示公式。**

这一小节是 pattern-establishing section，不需要公式。最多允许行内符号：

- `INT8`
- `INT4`
- `H_{kv}=8`

### 77.6 图表安排

**主方案：只放 1 张小型汇总表，不放图。**

这张表建议设计成：

- 模型
- `H_{kv}`
- `FP16 Needle`
- 对称 `INT4 Needle`
- 对称 `INT4` 相对 `FP16` 的 PPL 变化
- 结构标签（崩塌 / 架构例外）

这里不追求多指标完整，只追求一眼能读出的 **cliff summary**。  
你完全可以用旧 `4.2` 导语中的现成锚点数值来做这张表：Qwen2.5-1.5B / 7B 的 Needle 100%→0%，`LLaMA-3.1-8B` 仍 98%，以及“PPL > 15%”的 cliff 口径。

### 77.7 相对当前稿件的删改动作

- **抽出**：把旧 `4.2` 开头的“对称 INT4 的阶跃崩塌”那段，从大节导语提升为独立的 `4.3.1`；
- **改题**：从“系统性失效”升级为“架构依附性失效与阶跃崩塌”；
- **新增**：一张小型 summary table；
- **强化**：把 `LLaMA-3.1-8B` 的例外从句内提示提升到首段/标题层的显式限定；
- **克制**：不在这里塞入 `K/V` 机制解释和恢复细节。

### 77.8 完成后的目标印象

读完 `4.3.1`，评委脑子里应该只留下这一句：

> **low-bit 问题是真问题，而且不是温和退化；但它也不是一条对所有架构完全相同的失效曲线。**

---

## 78. 第四章 4.3.2 写作计划

### 78.1 小节标题

保持为：

**4.3.2 K/V 角色敏感性与 MixedKV 完整诊断**

这个标题现在已经很准。它同时覆盖两件事：

- 完整版 `K/V` role diagnosis
- `MixedKV` 作为恢复性诊断配置的证据

而且它与第三章 `3.2` 的压缩版动机诊断刚好形成前后呼应。

### 78.2 小节职责

这一小节要完成的唯一任务是：

> **把“Key 主导退化”从第三章的动机判断，升级成第四章的完整版证据。**

它必须回答三层问题：

1. `K-only / V-only / K4V8 / MixedKV` 这四种配置各自隔离了什么；
2. 为什么说 low-bit cliff 主要由 **Key 精度下降**触发；
3. 为什么说 `MixedKV` 的恢复，不是“碰巧好一点”，而是支持了“优先保护 Key”这条设计方向。

### 78.3 本节不做什么

- 不重新讲 `3.1` 的理论分解；
- 不把这一节写成第三章 `3.2` 的重复；
- 不提前进入 `RoleAlign vs KIVI-style` 的同 format 比较；
- 不在这里做长版机制讨论，那些要留给 `4.6.2`。

### 78.4 最优段落结构

建议 **6 个自然段 + 2 张图 + 2 张主结果表**。

**第 1 段：与 `3.2` 的关系。**  
明确写：第三章 `3.2` 已经给出从同源结果中抽取出的 **bit-layout 压缩诊断视图**，本节报告其完整版证据。这样读者会知道这里不是新问题，而是完整数据落位；同时也要说清，`3.2` 与本节共享结论方向，但不是逐配置复刻。

**第 2 段：先说明两层诊断结构。**  
这一段必须先写清：本节不是只摆一组配置，而是分成两层证据。第一层是 PPL 的单侧极端干预锚点，用来回答“单独压 Key 或单独压 Value 时，谁先触发 cliff”；第二层才是在 RULER / LongBench 上使用统一的四配置矩阵，回答“这种角色敏感性是否能跨任务、跨模型保持一致方向”。同时要补一句：第三章 `3.2` 中的 `K8V4` 对应这里的恢复性诊断配置 `MixedKV`，这是章际桥接，而不是术语冲突。

**第 3 段：先读 PPL 表，立住单侧干预锚点。**  
这里必须把旧 `表 4-4` 放到最前面读，因为它是整节最锋利的一刀。正文要明确：这张表不是四配置矩阵本身，而是单侧干预的 PPL 锚点，分别比较 `K@INT8+V@FP16`、`K@FP16+V@INT8`、`K@INT4+V@FP16` 和 `K@FP16+V@INT4`。在 Qwen2.5-1.5B 上，`K@INT4 + V@FP16` 的 PPL 直接变成 **1290.9**，相对 FP16 为 **+13,774%**；而 `K@FP16 + V@INT4` 只有 **+0.4%**，`K@INT8 + V@FP16` 也只有 **+0.2%**。这一组读数几乎是“Key 触发 cliff”的最强单表证据。

**第 4 段：再切到统一四配置矩阵，建立跨任务一致方向。**  
这一段接着读旧 `表 4-5 / 4-6`：在 Qwen2.5-1.5B / 7B 上，`K4V8` 会把 RULER 直接打到 **0%**；而在 LLaMA-3.1-8B 上，`K4V8` 仍有 **31.12** 的 RULER 与 **8.92** 的 LongBench 综合分。这里的写法必须很克制：  
不是“LLaMA 没问题”，而是“共同方向依然是 Key 主导退化，但强度受 family / scale / `H_{kv}` 调制”。

**第 5 段：再读 `MixedKV` 恢复，并顺手吸收 supporting 证据。**  
这一段要把两层信息收住：一是 `MixedKV` 明显优于均匀低比特 Key 的诊断意义；二是 14B 的 supporting row 只作为正文补强，用来说明这种不对称性并不只出现在小模型。这里不再给 14B 额外开第四章正式表，而是在多任务结论之后用 supporting note 吸收。

**第 6 段：把诊断结论收束成一句设计启示。**  
固定写成：

> 这些结果共同说明，在本文考察的低比特场景中，真正首先触发行为崩塌的是 Key 精度下降；而 `MixedKV` 的恢复则表明，低比特路径的首要动作不是继续平均压缩 `K/V`，而是优先保护 Key，并在此基础上升级量化格式。

### 78.5 公式安排

**不放显示公式。**

这一小节是实验诊断节，不需要公式。最多允许行内配置记号：

- `K-only`
- `V-only`
- `K4V8`
- `MixedKV`
- `H_{kv}`

### 78.6 图表安排

这一小节建议固定为：

- **图 4-1**：继续保留旧 `Figure 4-1` 的完整版本
- **图 4-2**：继续保留旧 `Figure 4-2` 的配对误差热力图
- **表 4-6**：旧 `表 4-4`，PPL 消融
- **表 4-7**：RULER / LongBench-style 多任务双 panel 结果表

这里的核心纪律是：

> **`4.3.2` 必须承担 third-chapter compressed diagnosis 的 same-source full-evidence role。**

也就是说，第三章 `3.2` 用的压缩版动机图，必须被视为这里完整证据中抽取出的 bit-layout 压缩视图；它不要求与本节逐配置同构，但结论方向、来源数据与设计启示必须严格一致，不能口径漂移。

### 78.7 相对当前稿件的删改动作

- **整体重编号**：把旧 `4.2.1 K/V Role Mechanism` 提升为新 `4.3.2`；
- **保留**：旧 `Figure 4-1`、`Figure 4-2`，以及旧 `表 4-4` 到 `表 4-6` 的主体数据；
- **改写首段**：从“INT4 大节内部桥梁段”改成“第三章压缩诊断的完整版证据段”；
- **强化**：把 “Key 主导退化 + 架构依附性” 写成双层结论，而不是只写 “Key 比 Value 更敏感”；
- **下沉 supporting role**：14B 证据保留为正文补强，不再硬抬成第四章正式表。

### 78.8 完成后的目标印象

读完 `4.3.2`，评委脑子里应该只留下这一句：

> **low-bit cliff 的主导触发侧是 Key，而不是 Value；`MixedKV` 的恢复则证明了“Key-first protection”不是直觉，而是有完整实证支撑的设计方向。**

---

## 79. 第四章 4.3.3 写作计划

### 79.1 小节标题

保持为：

**4.3.3 INT4-RoleAlign 与 KIVI-style 的跨模型对比**

这个标题也不需要再改。它已经把比较对象和边界写得很清楚：这里比较的是 **RoleAlign vs KIVI-style**，而不是 RoleAlign vs 世界上一切 `INT4` 方法。

### 79.2 小节职责

这一小节只承担一个任务：

> **在“同 format、差 calibration philosophy”的公平设置下，完成 low-bit recovery path 的最后一环。**

它要让读者看懂三件事：

1. 这场比较的公平性在哪里；
2. empirical 结果到底支持到什么程度；
3. `RoleAlign` 的真正价值是什么，不是什么。

### 79.3 本节不做什么

- 不重讲 `K/V` 诊断；
- 不讨论 allocator；
- 不做大段三层诚实分析，那已后移到 `4.6.2`；
- 不在这里讲工程速度，因为两者 decode 路径相同，部署层问题统一放到 `4.5`。

### 79.4 最优段落结构

建议 **5 个自然段 + 1 张表**。

**第 1 段：先把比较边界讲清。**  
这一段必须非常重要地写清：

- `INT4-RoleAlign` 与 `KIVI-style` 共享同一非对称格式：`per-channel K + per-token V`
- 解码路径相同：`torch_ref`
- 唯一差异集中在 calibration philosophy：`INT4-RoleAlign` 采用 offline role-aware calibration，其中 `K-path` 以 attention-distribution KL 为主代理，并在离线搜索纪律下确定参数，`V-path` 由独立输出扰动代理主导；`KIVI-style` 则采用 runtime `absmax/min`

这一步不写清，后面所有表都容易被误读成“格式竞争”。当前主稿旧 `表 4-8` 已经把这个边界写得很清楚，必须保留。

**第 2 段：先把方法身份讲清。**  
这一段不要读很多数字，而是用正文直接讲清：`RoleAlign` 多出来的不是另一种 cache format，而是离线可审计 calibration artifact、只读的 scale 固化方式，以及可扩展到 allocator 的接口形态。第四章不再重复设计差异表；这部分正式归宿已经冻结在第三章 `3.5.4`，本节只保留和 same-format 比较直接相关的最小解释。

**第 3 段：读跨模型主表。**  
这里直接读旧 `表 4-9`：  
在 1.5B / 7B / 8B 上，`RoleAlign` 与 `KIVI-style` 的 PPL 差距分别是 **+0.15 / +0.05 / +0.00**；而 Needle 在对比模型上都恢复到 **100/100%**。14B 目前只有 `RoleAlign` 行，没有 KIVI-style 对照，因此正文必须写清这是 scope boundary，不要装作四模型 fully paired compare。

**第 4 段：诚实解释这张表到底证明了什么。**  
这一段必须把口气压稳：

> 这组结果首先说明，决定 Needle 恢复的关键首先是非对称 role-aware format；在此基础上，离线 behavior-guided calibration 并没有制造一个 winner-take-all 的大缺口，而是提供了一条可固化、可审计、可与后续 allocator 自然衔接的 low-bit 实例化路径。

这是这小节最核心的一段，不能写成夸张胜利叙事。

**第 5 段：补上 `4.3` 的短收束。**  
这里必须单独写一段，不然把旧 `4.2.3` 移走后，`4.3` 会突然断掉。建议固定成这种语气：

> 至此，`RQ2` 的证据链已经闭合：`INT8` 上成立的 behavior-guided 原则，在对称 `INT4` 上首先遭遇架构依附性的阶跃失效；完整诊断显示该失效主要由 Key 侧触发；而在共享非对称格式的公平设置下，`INT4-RoleAlign` 则把这一路径恢复为一个可审计、可扩展的 low-bit 实例。接下来的问题，不再是 low-bit path 能否被恢复，而是恢复后的行为敏感度画像能否进一步支撑跨模型预算分配。

这段是从 `4.3` 桥到 `4.4` 的关键。

### 79.5 公式安排

**不放显示公式。**

这是一节 fair-comparison section，不需要公式。

### 79.6 图表安排

建议只保留 **1 张正式表，不再新增图**：

- **表 4-8**：`INT4-RoleAlign` 与 `KIVI-style` 的跨模型主对比

我不建议在这里再强行加比较图。原因很简单：

- 这一节的 strongest evidence 已经是 **same-format comparison table**
- 如果再画图，很容易把“差 calibration philosophy”的克制比较画成“胜负图”

### 79.7 相对当前稿件的删改动作

- **整体重编号**：把旧 `4.2.2` 提升为新 `4.3.3`；
- **保留**：旧同 format 跨模型主对比材料；
- **强化首段**：把 “同 format、差 calibration” 的公平设置写成首段主句；
- **删掉或后移**：旧 `4.2.3` 的大段三层诚实分析，不再留在本节；
- **下沉**：设计差异表回到第三章 `3.5.4`，第四章不重复编号；
- **新增**：一段 `4.3` 小结式收束，补上向 `4.4` 的桥。

### 79.8 完成后的目标印象

读完 `4.3.3`，评委脑子里应该只留下这一句：

> **RoleAlign 的价值不在于在同一格式下把 KIVI-style 远远甩开，而在于它把 low-bit 恢复路径组织成了一条可固化、可审计、可继续延伸到 allocator 的 behavior-guided 实例。**

---

## 80. 4.3 相对当前稿件的总手术动作

`4.3` 的总改法可以概括为 7 条：

1. **把旧 `4.2` 开头的 cliff 段单独切出，做成新 `4.3.1`。**  
   这一步不是改内容，而是把旧导语升级成独立证据入口。

2. **把 `4.3.1` 的标题明确升级为“架构依附性失效与阶跃崩塌”。**  
   `LLaMA-3.1-8B` 的 98% Needle 例外必须在标题和首段层面被承认。

3. **把旧 `4.2.1` 整体重编号为新 `4.3.2`。**  
   图 `4-1 / 4-2` 与旧 `表 4-4` 到 `表 4-6` 的主体数据基本保留，只改段落职责和叙事位置；14B supporting row 下沉为正文补强，不再单列正式表。

4. **把旧 `4.2.2` 整体重编号为新 `4.3.3`。**  
   只保留 same-format 跨模型对比表作为主承重件；设计差异表回到第三章 `3.5.4`，本节段落口气从“结果推进”改为“fair same-format compare”。

5. **把旧 `4.2.3` 的“三层诚实分析”整体后移到 `4.6.2`。**  
   这是 story 已经冻结的动作，`4.3` 不再承载长版机制辨析。

6. **在 `4.3.3` 末尾补一个短版 `4.3` 小结。**  
   这是为了弥补旧 `4.2.3` 后移后留下的收束空洞。

7. **主方案让 `4.3.1` 用小表承重，不新开图。**  
   这样可以最大程度维持旧 `Figure 4-1 / 4-2` 在 `4.3.2` 的稳定位置，同时也与第三章 `3.2` 的压缩版图来源关系最稳。

---

## 81. 4.3 完成后的目标印象

读完整个 `4.3`，评委脑子里应该只留下这一句话：

> **RQ2 已被完整回答：behavior-guided 原则在 `INT8` 上成立，但在对称 `INT4` 上会出现架构依附性的阶跃失效；完整诊断表明这一失效主要由 Key 侧触发，而 role-aware 非对称格式则把 low-bit path 恢复成了一条可审计、可扩展的实例化路径。**

---

## 82. 第四章 4.4 写作计划

### 82.1 小节标题

保持为：

**4.4 RQ3：跨模型预算分配与策略适用区间图谱**

这个标题现在是对的，不需要再改。它比旧稿的 `Cross-model Regime 地图 + Per-Model Case Analysis` 更紧，也更符合当前章职能：这一节不是“allocator 的实验合集”，而是要回答一个单独的研究问题——**同一套 behavior sensitivity profile，是否真的能读出跨模型稳定而可命名的策略适用区间结构。**

### 82.2 小节职责

`4.4` 在第四章中的唯一任务是：

> **把 allocator 的结果从“跑分表”提升成“结构图谱”。**

也就是说，这一节必须同时完成三层工作：

1. 在 `4.4.1` 先用跨模型主表立住：不同模型在同量级预算带内，最优 policy 的落点并不一致。
2. 在 `4.4.2–4.4.5` 再把这种异质性拆成四类 regime，并分别命名。
3. 把 allocator 的贡献，从“提出一个普适赢家”改写成“提供一个能读出 structural footprint 的工具”。

这个章职能是 story 已经写死的，而且当前现稿的主表与 case 材料也正好支持这种改写方向。

### 82.3 本节不做什么

`4.4` 必须明确克制，不承担以下内容：

- 不把这里写成 matched-budget formal compare；
- 不把这里写成 allocator vs `KIVI-style` 的对决节；
- 不在这里长篇解释 heuristic 为什么强，那要留给 `4.6.3`；
- 不在这里重讲 `3.6` 的 allocator 定义；
- 不把 `4.4` 写成“BA-k 总体最好”的胜利叙事。

也就是说，`4.4` 只能做 **regime readout**，不能偷渡成另一个方法章或另一个讨论章。

### 82.4 最优总结构

建议将 `4.4` 固定为：

- **1 个短导语**
- **5 个三级小节**
- **1 段节末短收束**
- **4 张图**
- **3 张正式表**

我建议 `4.4` 的图表职责这样分：

- `4.4.1`：**1 张跨模型主表 + 3 张结构图**
- `4.4.2–4.4.3`：**共用 1 张双 block case 表**
- `4.4.4–4.4.5`：**共用 1 张双 block case 表 + 1 张 section-level 汇总图**

如果延续我们前面已经冻结的 final keep-set，那么 `4.3` 结束时表号应落在 **表 4-8**；因此 `4.4` 最稳的编号是：

- **表 4-9**：跨模型主表
- **表 4-10**：典型案例表 A（Panel A: Mistral-7B, Panel B: Qwen2.5-3B）
- **表 4-11**：典型案例表 B（Panel A: LLaMA-3.1-8B, Panel B: Qwen2.5-14B）

图号则建议保留旧序，不再改：

- **图 4-3**：AutoK 逐层保护图
- **图 4-4**：Quality–预算 Pareto 视图
- **图 4-5**：适用区间 map 的 skim 路径
- **图 4-6**：family/scale 分类排序汇总图

这个安排最大的好处，是旧稿里的 `4.3 + 4.4` 证据资产几乎都能平移，只是叙事职能被重分配了。现稿现在的 cross-model 主表、图 4-3/4-4/4-5/4-6，以及 Mistral / 3B / 14B 三个 case，本身已经构成一张完整的 regime map 雏形。

### 82.5 节首导语

`4.4` 开头建议只写 **2 段**。

**第 1 段**只做一件事：承接 `4.3`。  
推荐开头句：

> 第 4.3 节已经确认，low-bit path 可以在 role-aware 非对称格式下被恢复。本节进一步回答 `RQ3`：由 behavior-guided calibration 产出的 sensitivity profile，是否能够支撑跨模型预算分配，并揭示稳定的策略适用区间结构。

**第 2 段**只做一件事：告诉读者这里的读法不是 winner-take-all。  
建议固定为：

> 为避免将 allocator 结果误读为单一 policy 排名，本节首先在同量级 INT4 预算带内给出跨模型主表，再通过四个典型剖面把主表中的 footprint 命名为不同 regime：小模型的早层保护回救区间、中等规模的共识区间、大模型的高性能簇共存，以及 Mistral 上最显著的 AutoK 正向案例。

### 82.6 图表总策略

`4.4` 的图表策略应冻结为：

- **主表先于 case**
- **图 4-3 / 4-4 / 4-5 服务 `4.4.1`**
- **图 4-6 不放在最前面，而放在 `4.4.5` 末尾作为整节收束图**
- **四个 case 压成 2 张双 block 表**
- **`4.4.4 LLaMA-8B` 必须在双 block case 表中占据独立 panel，不能只在正文里一笔带过**

这里的真正纪律是：四个 case 依然都必须单独成节，但 case 表不再各自独立编号，而是压成两张双 block 表，以保证第四章 final keep-set 的图表承重密度可控。

---

## 83. 第四章 4.4.1 写作计划

### 83.1 小节标题

保持为：

**4.4.1 同量级预算下的跨模型主表**

### 83.2 小节职责

这一小节只承担一个任务：

> **把 allocator 的所有后续 case 统一锚定到一张诚实的 cross-model readout 上。**

也就是说，它要先立住三件事：

1. 这里比较的是 **same-order INT4 budget band**，不是严格 matched memory。
2. 四个模型的最佳 policy family 确实不一样。
3. 这些差异不是噪声，而是和 sensitivity profile 的结构形状配套出现的。

### 83.3 本节不做什么

- 不在这里就把四个 case 讲完；
- 不长篇解释每个模型为什么这样，那留给后面的剖面；
- 不在这里解释 heuristic 强基线的机制，那留给 `4.6.3`；
- 不把本节写成 budget fairness defense note。

### 83.4 最优段落结构

建议 **5 个自然段 + 1 张主表 + 3 张图**。

**第 1 段：先把口径写死。**  
第一段必须先承认预算带的诚实边界：本节比较的是同量级 INT4 预算带，不是严格 matched memory 的 ±3% 对照。这个口径现在必须写在非常前面，因为现稿已经明文说明 allocator policies 的平均位宽落在约 `4.5–5.0 bit`，而 uniform 是 `4.0 bit`，因此它的章职能是 **signature readout**，不是 matched-budget formal comparison。

**第 2 段：读跨模型主表。**  
这里引出新的主表（建议新表号 **表 4-9**），内容基本沿用旧主表：  
`Uniform / BA-k / Heuristic-k / BA-AutoK` 四个 policy family，`Qwen2.5-3B / LLaMA-3.1-8B / Qwen2.5-14B / Mistral-7B` 四个模型。正文只读 block-level footprint，不逐格解释。最重要的是把 note 读出来：

- 3B 用 `k=1`
- LLaMA-8B 用 `k=11`
- 14B 用 `k=7`
- Mistral 用 `k=3`
- AutoK 在 14B 用 `cov90`，其余用 `cov80`

这一段必须让读者一眼明白：**best-k 本身就已经是 model-dependent 的。**

**第 3 段：读图 4-3，建立结构形状。**  
图 4-3 的地位在新 `4.4.1` 里非常高，它不是漂亮插图，而是把主表背后的 profile shape 直接可视化。正文应读出三类 shape：

- 3B：早层集中
- LLaMA-8B / Mistral：中层簇集
- 14B：近乎铺满全深度的 wide-spread

这一步很关键，因为它把“不同模型 winner 不同”从表格现象，抬升成了 sensitivity structure 的差异。

**第 4 段：再读图 4-4 和图 4-5。**  
这一段只做两件事：

- 图 4-4 证明 budget-band 不是口头说说，而是可视化的 Pareto readout：Mistral 的 AutoK strongest positive region、LLaMA-8B 的近 Pareto cluster、Qwen7B 的 uniform cliff 都在这里有直观支持。
- 图 4-5 提供 skim-path 式的 “每行 winner 指向哪一类 policy” 的快速读法。

这里注意一个写作纪律：Qwen7B 在图 4-4 里出现，是 supporting evidence，不把它升级成新的 case。

**第 5 段：把主表读成四种 footprint，并埋 forward pointer。**  
这一段要把四模型 footprint 一次命名出来：

- 3B = 早层保护回救区间
- LLaMA-8B = 中等规模共识区间
- 14B = 高性能簇共存
- Mistral = AutoK 最显著正向案例

然后在末尾加一句固定的引桥：

> 值得注意的是，position-based heuristic 在多个区间内与 behavior-guided 选中集合发生重合，这一强基线现象的结构性含义将在 `4.6.3` 节进一步讨论。

这句必须写。story 已经把它冻结为硬性前向引桥。

**固定前向引桥句（正文建议直接采用）：**

> 值得注意的是，position-based heuristic 在多个区间内与 behavior-guided 选中集合发生重合，这一强基线现象的结构性含义将在 `4.6.3` 节进一步讨论。

### 83.5 公式安排

**主方案：不放显示公式。**

这一小节是主表 readout，不需要公式。即便现稿 budget band 段里用了平均位宽表达式，也建议保留为行内记号，不再在 `4.4.1` 重开显示公式环境。

### 83.6 图表安排

`4.4.1` 建议固定为：

- **表 4-9**：跨模型主表
- **图 4-3**：AutoK 逐层保护图
- **图 4-4**：Quality–预算 Pareto 视图
- **图 4-5**：适用区间 skim 路径

这四个承重件中，**表 4-9 是入口，图 4-3 是结构解释，图 4-4 是 budget-band honesty，图 4-5 是视觉摘要。**

### 83.7 相对当前稿件的删改动作

- **整体重组**：把旧 `4.3 跨模型 Regime 地图` 正式并入新 `4.4.1`；
- **保留**：旧主表、图 4-3、图 4-4、图 4-5 的主体证据；
- **强化**：same-order budget band 的诚实口径前移到本节首段；
- **新增**：明确的 forward pointer 到 `4.6.3`；
- **删掉**：任何让读者误以为这里在做 formal matched-budget comparison 的表述。

### 83.8 完成后的目标印象

读完 `4.4.1`，评委脑子里应该只留下这一句：

> **allocator 的结果不是一条全局单调排序，而是四种不同 footprint；而这些 footprint 与 sensitivity profile 的形状是对齐的。**

---

## 84. 第四章 4.4.2 写作计划

### 84.1 小节标题

保持为：

**4.4.2 Mistral-7B：AutoK 的最显著正向案例**

### 84.2 小节职责

这一小节只承担一个任务：

> **给出全文最干净、最正面的 AutoK 单模型案例。**

story 已经把这个角色冻结给 Mistral-7B，而现稿的 5-task 细节表也已经完全支持这一定位：`BA-AutoK (cov80)` 在 5 个任务中拿到最多 task-wise 胜利，mean 也最高，是当前材料里最适合承担 “AutoK strongest positive case” 的模型。

### 84.3 本节不做什么

- 不把 AutoK 写成对所有模型都稳胜；
- 不在这里重讲 cross-model 主表；
- 不在这里把 Mistral 的正向案例外推成框架通解。

### 84.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：先把 case role 写清。**  
先说清楚：Mistral-7B 不是因为总体最好才被单列，而是因为它是所有模型中 **AutoK 优势最清晰、最连续、最容易读** 的案例。

**第 2 段：读 5-task 细节表。**  
引出新的 **表 4-10 Panel A**，正文只读三件事：

- `BA-AutoK` 的 mean 最高
- 5 个 task 中赢 3 个
- 这不是单一异常任务带出来的假象

尤其要点出：`DuReader` 上 `11.62 vs 7.97` 的大幅优势，是这个 case 的尖点证据。

**第 3 段：把优胜落到 profile shape。**  
这一段必须把 “为什么是 Mistral” 写成结构读数，而不是跑分偶然性：  
Mistral 的 top-3 protected layers 落在中前层簇集，且 `cov80` 选择出的保护范围与这类簇集结构相吻合，所以 AutoK 在这里不是瞎撞，而是 profile-guided budget proposer 真正读对了层间结构。

**第 4 段：把结论压稳。**  
落句建议固定成：

> 因此，Mistral-7B 在本文中承担的不是 “AutoK 普适最优” 的证明，而是 “当 profile 具有可压缩簇集结构时，coverage-based budget proposer 可以给出最干净正向信号” 的单模型实例。

### 84.5 公式安排

**不放显示公式。**

这一节是单模型细节读表，不需要任何公式。

### 84.6 图表安排

**只放 1 张正式表。**

- **表 4-10 Panel A**：Mistral-7B 典型正向案例细节块

图 4-3 中 Mistral 的 protection map 不需要在这一小节重复贴图，只需正文回引即可。

### 84.7 相对当前稿件的删改动作

- **保留**：旧 `4.4.1 Mistral-7B-v0.3` 的主体数据与 case logic；
- **改题**：中文化并与 story 完全对齐；
- **改口气**：从 “single strongest case” 改成 “最显著正向案例”；
- **保留但收紧**：不要把这段写成 AutoK 的全面胜利宣言。

### 84.8 完成后的目标印象

读完 `4.4.2`，评委脑子里应该只留下这一句：

> **Mistral-7B 是 AutoK 最干净的正向案例，但它说明的是“coverage-based proposer 在某类 profile 上真的能读对”，不是“AutoK 永远最优”。**

---

## 85. 第四章 4.4.3 写作计划

### 85.1 小节标题

保持为：

**4.4.3 Qwen2.5-3B：早层保护回救区间**

### 85.2 小节职责

这一小节只承担一个任务：

> **给出全文最强的 behavior-guided vs heuristic 结构性差距案例。**

story 已经把 3B 固定为 **early-layer rescue regime**，而现稿的 3-task 细节表也非常尖锐：`BA-k1` 相对 `Heur-k1` 在三个任务上分别有 `+4.09 / +3.33 / +2.85` 的大幅回救，mean 差距达到 `+3.42`。这就是整个 regime map 里 “behavior-guided 不是只比启发式好一点，而是在某些结构上能直接把几乎失效的策略捞回来” 的最强证据。

### 85.3 本节不做什么

- 不把 3B 的 extreme gap 当成所有模型的普遍规律；
- 不在这里展开 heuristic 强基线的 general discussion；
- 不把 AutoK 与 BA-k1 的小差距写成方法失败。

### 85.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：先说这是“差距最大模型”。**  
直接点明：3B 之所以单列，不是因为总体分数最高，而是因为它是 **behavior-guided 与位置启发式差距最大** 的模型。

**第 2 段：读细节表。**  
引出新的 **表 4-10 Panel B**，重点只读 `BA-k1 vs Heur-k1` 的差距，不要在这里重新读 uniform 与 AutoK 的所有细节。最核心的锚点就是：

- NarrativeQA：`7.17 vs 3.08`
- HotpotQA：`4.73 vs 1.39`
- GovReport：`8.81 vs 5.96`
- Mean：`6.90 vs 3.48`

这组读数要写成 **rescue**，而不是普通 improvement。

**第 3 段：把 rescue 落到结构原因。**  
这里必须写清楚：  
3B 的 sensitivity profile 是明显的早层集中型；`BA-k1` 保护的是 layer 0，而 heuristic 的单层保护落在中层，于是直接错过了真正的瓶颈层。这个 case 的价值不在 “多赢几分”，而在于说明 **当 profile 极端集中时，位置启发式会彻底选错层。**

**第 4 段：把结论压稳。**  
固定落成：

> 因此，3B 所定义的不是一个一般性的 “small model always needs BA-k1” 规则，而是一类更窄但更重要的 regime：当 behavior sensitivity 在最早几层高度集中时，保护正确的首层会产生回救式收益，而保护位置上看似“居中”的层几乎没有作用。

### 85.5 公式安排

**主方案：不放显示公式。**

现稿旧 `4.4.2` 里用了 `\Delta(t)=score(BA-k1,t)-score(Heur-k1,t)` 的显示公式，但在新第四章里，这个式子更适合降为行内定义或表头说明，不值得单独开公式环境。

### 85.6 图表安排

**只放 1 张正式表。**

- **表 4-10 Panel B**：3B early-layer rescue 细节块

这一小节不需要单独再贴 protection map，回引 `图 4-3` 即可。

### 85.7 相对当前稿件的删改动作

- **保留**：旧 `4.4.2 Qwen2.5-3B` 的主要数据与 narrative；
- **删掉**：显示公式 `(4-2)`，改成行内定义；
- **强化**：把 “catastrophic Δ” 的含义收束为结构性回救，而不是情绪化措辞；
- **保留**：layer 0 / 早层集中的解释。

### 85.8 完成后的目标印象

读完 `4.4.3`，评委脑子里应该只留下这一句：

> **3B 证明了 regime map 不是“好看命名”，而是真会决定你保护哪一层；在早层极端集中的模型上，选错层几乎等于没保护。**

---

## 86. 第四章 4.4.4 写作计划

### 86.1 小节标题

保持为：

**4.4.4 LLaMA-3.1-8B：中等规模共识区间**

### 86.2 小节职责

这一小节只承担一个任务：

> **把 LLaMA-3.1-8B 从“主表里的一行”提升成一个独立的 regime node。**

这是新 story 相对旧稿最重要的补丁之一。旧 main 里 case section 只有 Mistral / 3B / 14B 三例，但 cross-model 主表已经明确给出：`LLaMA-3.1-8B` 的 per-model 落点是 `BA-k11`，且它与 `H_{kv}=8` 的架构稳健性一起构成一个很典型的中等规模 footprint。因此这个 case 不能继续被沉没，它必须被显式写成 story 规定的 **中等规模共识区间**。

### 86.3 本节不做什么

- 不把 LLaMA-8B 写成普适赢家；
- 不用它去推导“`H_{kv}=8` 一定更优”的强结论；
- 不在这里讲完整 GQA 机制，那是 `4.5` 和 `4.6.2` 的事。

### 86.4 最优段落结构

建议 **4 个自然段 + 1 张新建小表**。

**第 1 段：先坦白这节是“新建 case”。**  
这一段应该非常诚实：  
LLaMA-8B 在旧稿里没有独立剖面节，但 cross-model 主表已经给出了足够强的 footprint，因此这里是一次 **从主表读数中提升出的独立 case**，不是新补实验。

**第 2 段：读新建小表。**  
这里建议新建 **表 4-11 Panel A**，内容直接来自旧 cross-model 主表中 `LLaMA-3.1-8B` 那个 block：

- NarrativeQA：`10.24 / 11.14 / 10.47 / 10.74`
- HotpotQA：`6.73 / 7.88 / 5.68 / 7.57`
- GovReport：`9.24 / 9.54 / 9.47 / 9.75`
- Mean：`8.74 / 9.52 / 8.54 / 9.35`

正文的读法要很克制：`BA-k11` 的确是这组数据里的 best mean node，但 `BA-AutoK` 也很接近，说明这里更像一个 **consensus top-tier zone**，而不是单点独占。

**第 3 段：用图 4-3 / 4-4 解释“中等规模共识”。**  
这一段要把 case 名字解释清楚：

- `k=11` 本身说明保护范围已经不是 3B 那种尖锐的前层集中，而是中等规模下更广泛的 top-k 覆盖；
- Pareto 视图中，LLaMA-8B 的四类 policy 接近 Pareto，而不像 Mistral 那样由 AutoK 拉开明显区域；
- 因而它最适合被命名为 **共识区间**：存在一个相对稳定的 `BA-k11` node，但其它强 policy 也并未远离。

**第 4 段：把 `H_{kv}=8` 的作用压到“对照角色”。**  
这一段必须谨慎写：

> LLaMA-3.1-8B 在本文中承担 `H_{kv}=8` 架构稳健性的对照角色，但本节证据支持的只是“该架构与中等规模下较平滑的 allocator footprint 相关”，而不是“更高 `H_{kv}` 必然导向更优 policy”。

这句很重要，因为它能避免把 case reading 误写成强机制断言。

### 86.5 公式安排

**不放显示公式。**

这一节是新建 case，应该尽量保持轻、稳、清楚，不要再引入新公式。

### 86.6 图表安排

**只放 1 张新建的小表。**

- **表 4-11 Panel A**：LLaMA-3.1-8B 中等规模共识区间细节块

正文回引：

- `图 4-3` 说明保护范围形状
- `图 4-4` 说明 near-Pareto cluster
- 必要时在 `4.4.5` 末尾再由 `图 4-6` 做 section-level consolidation

### 86.7 相对当前稿件的删改动作

- **新增**：一个全新的 `4.4.4` 小节；
- **新增**：一张由旧 cross-model 主表抽出的 LLaMA 细节表；
- **不新增实验**：本节不要求新跑实验，主承重证据已在现稿 cross-model 主表和 Pareto 图中存在；
- **强化**：把 `BA-k11` 从 “表里的一格” 升格成 regime node。

### 86.8 完成后的目标印象

读完 `4.4.4`，评委脑子里应该只留下这一句：

> **LLaMA-8B 代表的不是极端回救，也不是 AutoK 独占，而是一类更平滑的中等规模共识区间：存在明确的 BA-k 节点，但 top-tier policies 彼此接近。**

---

## 87. 第四章 4.4.5 写作计划

### 87.1 小节标题

保持为：

**4.4.5 Qwen2.5-14B：高性能簇共存而无稳定最优**

### 87.2 小节职责

这一小节只承担一个任务：

> **证明 regime map 的最重要负例：不是每个模型都会给你一个清晰的 winner。**

story 已经把 14B 固定为 **高性能簇共存而无稳定最优**，而现稿的 14B 细节表也完全支持这个命名：top-3 policy 相对 top-1 的最大相对差距只有约 `3.5%`，平均约 `3.0%`，而且这些 gap 的量级又落在 bootstrap CI 半宽内，因此这个 case 最重要的结论不是“Uniform 最优”，而是 **当前 power 下 top-tier policies 不可分辨。**

### 87.3 本节不做什么

- 不把 14B 写成 Uniform 胜利案例；
- 不把“不可分辨”写成“allocator 没用”；
- 不把 14B 的弥散 profile 写成所有大模型都如此。

### 87.4 最优段落结构

建议 **4 个自然段 + 1 张表 + 1 张 section-level 收束图**。

**第 1 段：先把“反直觉 winner”降调。**  
直接说清楚：  
14B 在跨模型主表里看起来是 Uniform 胜出，但这一读数必须被当作 **高性能簇共存** 的入口，而不能被粗暴地读成 “Uniform is best”。

**第 2 段：读 14B 细节表。**  
引出新的 **表 4-11 Panel B**，重点只读两件事：

- 各 task 上四个 policy 的数值非常接近；
- top-3 相对 top-1 的最大 gap 约 `3.5%`、平均约 `3.0%`

这里应把旧公式 `(4-3)` 降为行内定义：  
`gap_rel(t) = (top1(t)-top3(t))/top1(t)`。  
不用再开显示公式。

**第 3 段：把不可分辨落到 profile 弥散化。**  
这一段必须把最关键的结构解释写出来：  
14B 的 sensitivity profile 是 wide-spread 的，AutoK 在 48 层里标了 42 层 protected，说明这不是少数关键层决定一切的模型。正因为 profile 本身已经近饱和，allocator 在当前预算区间里的 pick-order 才只会带来有限边际影响。这里应明确说：  
**不是 Uniform 真正最优，而是 top-tier cluster 在当前统计 power 下不可分辨。**

**第 4 段：用图 4-6 做 4.4 收束。**  
我建议把 **图 4-6** 放在这一节末尾，而不是更前面。原因很简单：它不是某个单 case 的图，而是整个 `4.4` 的分类收束图。到 14B 讲完时，四个 regime 已经都出现了，这时用图 4-6 把四模型的 policy ordering 沿 family/scale 排出来，正好形成 section-level close。现稿对这张图的定义也非常适合做这种收束：它强调横轴是 category，而不是连续 scaling law。

### 87.5 公式安排

**主方案：不放显示公式。**

现稿旧 `4.4.3` 里的 relative-gap 公式可以保留为行内定义，不需要单独的 `(4-3)` 环境。

### 87.6 图表安排

- **表 4-11 Panel B**：14B top-tier cluster 细节块
- **图 4-6**：family/scale 分类排序汇总图（作为整节 `4.4` 的收束图）

### 87.7 相对当前稿件的删改动作

- **保留**：旧 `4.4.3 Qwen2.5-14B` 的数据与主判断；
- **改题**：中文化并与 story 完全对齐；
- **删掉**：显示公式 `(4-3)`，改成行内定义；
- **强化**：把 “Uniform 胜出” 改写成 “top-tier cluster 中暂时由 Uniform 占 top1，但整体不可分辨”；
- **新增结构动作**：将 `图 4-6` 后移到本节末尾作为 `4.4` 总收束。

### 87.8 完成后的目标印象

读完 `4.4.5`，评委脑子里应该只留下这一句：

> **14B 证明了 regime map 的成熟度：不是每个模型都应该被写成清晰 winner；有些模型真正的结构读数恰恰是“高性能簇共存而无稳定最优”。**

---

## 88. 4.4 相对当前稿件的总手术动作

`4.4` 的总改法可以概括为 8 条：

1. **把旧 `4.3 跨模型 Regime 地图` 与旧 `4.4 Per-Model Case Analysis` 合并成新的 `4.4 RQ3`。**  
   即：旧 `4.3` 提供新 `4.4.1` 的主表与全局图；旧 `4.4.1–4.4.3` 则顺次变成新 case。

2. **把 old cross-model main table 升格为新 `4.4.1` 的唯一入口。**  
   它不能继续只是旧 `4.3` 的一张表，而必须成为整个 `RQ3` 的证据锚点。

3. **保留 same-order INT4 budget band 的诚实口径，并把它前移到 `4.4.1` 首段。**  
   这是答辩防守的硬线。

4. **保留 Mistral / 3B / 14B 三个旧 case，并新增 LLaMA-8B case；同时把四个 case 表压缩为两张双 block 表。**

5. **新增 `4.4.4 LLaMA-3.1-8B`。**  
   这是新 story 相对旧 main 的最关键新增，不可省略。

6. **把 heuristic 的结构性含义从本节正文后移到 `4.6.3`，这里只埋引桥。**  
   `4.4` 只命名现象，不长讲机制。

7. **把旧 `图 4-6` 放到 `4.4.5` 末尾，作为整个 `4.4` 的 section-level 收束图。**  
   这样它不再只是 14B 后的一张尾图，而是完整 regime map 的分类总结。

8. **减少公式，强化图表承重。**  
   旧 `(4-2)` 与 `(4-3)` 两个 case 公式都建议降成行内定义；第四章的核心不是符号，而是证据职责与结构读数。

---

## 89. 4.4 完成后的目标印象

读完整个 `4.4`，评委脑子里应该只留下这一句话：

> **RQ3 已被完整回答：behavior sensitivity profile 不只是校准副产物，它还能在同量级预算带内读出跨模型稳定而可命名的策略适用区间；这些区间既包括最显著的 AutoK 正向案例，也包括早层回救、中等规模共识，以及高性能簇共存而无稳定最优。**

---

## 90. 第四章 4.5 写作计划

### 90.1 小节标题

保持为：

**4.5 部署效率评估**

这个标题现在是对的，不需要再改。它比旧稿的 **“GQA-Aware 部署效率分析”** 更符合冻结版口径：`GQA/H_{kv}` 仍然是本节的重要解释变量，但它已经退回到**结果读数中的边界条件**，而不再占据节标题。story 对 `4.5` 的冻结要求也很明确：本节只讨论系统性能结果，不再重复方法动机。

### 90.2 小节职责

`4.5` 在第四章中的唯一任务是：

> **把第三章已经定义好的部署路径，转写成“什么条件下值得用、什么条件下不值得用”的经验边界。**

具体来说，这一节只需要回答四个问题：

1. 在短上下文入口处，不同 backend 的 `TPOT` 相对位置是什么；
2. 当序列长度拉长时，融合路径的收益如何放大；
3. 性能交叉边界到底由什么条件主导，尤其是 `H_{kv}` 与序列长度如何共同决定它；
4. 在显存层面，INT4 路径到底给了多大的 KV Cache 压缩，以及最终如何转写成部署建议。

### 90.3 本节不做什么

`4.5` 必须明确克制，不承担以下内容：

* 不重新解释 Triton 融合核为什么成立；
* 不重讲 `3.4.5 / 3.7` 的 kernel 设计；
* 不把 `4.5` 写成“速度证明方法更好”的结果节；
* 不在这里讨论质量边界或 low-bit 机制，那些已经在 `4.2–4.4` 与 `4.6` 各有归宿；
* 不把 `4.5.3` 写成严格因果识别，只能写成经验交叉边界与结构性相关。 

### 90.4 最优总结构

建议将 `4.5` 固定为：

* **1 个短导语**
* **4 个三级小节**
* **1 段节末短收束**
* **0 张图**
* **3 张正式表**
* **0 个正文显示公式**

如果沿用前文已经冻结的 final keep-set，那么 `4.4` 结束后表号应落在 **表 4-11**，因此 `4.5` 最自然的编号是：

* **表 4-12**：`4.5.1` 4K 序列 `TPOT` 对比
* **表 4-13**：部署边界综合表（Panel A: 长序列 scaling；Panel B: 性能交叉边界）
* **表 4-14**：`4.5.4` KV Cache 显存压缩率表

这意味着旧稿中的长序列 scaling 表与性能交叉边界表不再分开顺排，而是合并进入新的 **表 4-13**；旧 `4.5.4 8B vs 14B 同 H_{kv}=8 控制对比` 仍不单独占一个小节，而并入新的 `4.5.3` 作为 supporting control paragraph。

### 90.5 节首导语

`4.5` 开头建议只写 **2 段**。

**第 1 段**只做一件事：承接 `4.4`。
推荐开头句：

> 第 4.2–4.4 节已经分别回答了 canonical 保真、low-bit 恢复与跨模型策略图谱。本节转向部署层，考察这些路径在真实推理系统中的时间与显存代价。

**第 2 段**只做一件事：把本节读法从“系统原理”切换成“经验边界”。
建议固定为：

> 为避免与第三章的系统实现部分重复，本节不再解释 kernel 设计本身，而只报告系统结果：先给出 `4K` 序列下的 backend 入口读数，再展示长序列下的 `TPOT` 扩展规律，随后以性能交叉边界读出 `H_{kv}` 与序列长度的共同作用，最后用 KV Cache 显存压缩率与硬件约束收束为部署建议。

### 90.6 图表总策略

`4.5` 的图表策略应冻结为：

* **全节不用图，只用表**
* **`4.5.2` 与 `4.5.3` 共用 1 张部署边界综合表**
* **旧 `8B vs 14B` 同 `H_{kv}=8` 控制对比不单列成表，保持为 `4.5.3` 的 supporting paragraph**
* **部署建议不再单列成旧式 `4.5.6`，而并入 `4.5.4` 的结尾**

这是因为现稿的 `4.5` 证据本身就是高度 table-led 的：`4K TPOT`、`14B scaling`、`Phase Boundary`、KV Cache memory 这四块各自都有明确的表承重，而额外画图只会让系统节变得更像性能展示页，而不是论文中的边界读数节。

---

## 91. 第四章 4.5.1 写作计划

### 91.1 小节标题

保持为：

**4.5.1 4K 序列下的 TPOT 对比**

### 91.2 小节职责

这一小节只承担一个任务：

> **在最短、最常见的部署入口点上，把 backend landscape 先摆正。**

它的意义不是证明融合核已经大获全胜，而是先告诉读者：

* 在 `4K` 这个短上下文入口处，速度关系其实还没有充分拉开；
* 哪些模型上 `triton_ra` 已经开始接近或略胜 `torch_ref`；
* 哪些模型上它仍受固定调度开销拖累。

### 91.3 本节不做什么

* 不在这里就声称出现了明确交叉边界；
* 不把 `4K` 的小幅差异写成显著速度胜利；
* 不展开长序列 scaling，那是 `4.5.2`；
* 不讨论 `H_{kv}` 的一般规律，那是 `4.5.3`。

### 91.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：为什么先看 4K。**
先说明：`4K` 是最自然的 deployment entry point，也是判断固定 kernel 开销是否已经值得付出的起始位置。

**第 2 段：引出主表。**
这里引出新的 **表 4-12**。它沿用旧稿 `表 4-14` 的内容：`FP16 / torch_ref(INT4) / KIVI(INT4) / triton_ra(INT4) / FlashInfer(INT4)` 五种 backend，横跨 `1.5B (H_{kv}=2)`、`7B (H_{kv}=4)`、`8B (H_{kv}=8)`、`14B (H_{kv}=8)` 四个模型。正文只读 relative ordering，不逐格解释全部数值。

**第 3 段：读出 4K 入口层的三类现象。**
这一段只读三件事：

* 在 `H_{kv}=2` 的 `1.5B` 上，`triton_ra = 38.68 ms`，慢于 `torch_ref = 36.35 ms`；
* 在 `H_{kv}=4` 的 `7B` 上，`triton_ra = 38.76 ms`，仍慢于 `torch_ref = 37.61 ms`；
* 在 `H_{kv}=8` 的 `8B / 14B` 上，`triton_ra` 分别为 `44.49 ms` 与 `67.67 ms`，已经成为同模型 INT4 backend 中最快项，但优势幅度仍很小。

**第 4 段：把结论压稳。**
固定落成：

> 因而，`4K` 入口层的读数不应被写成“融合路径已经普适胜出”；它更像一个前置信号：当 `H_{kv}` 较小、Grid 规模不足时，Triton 的固定调度开销仍然可见，而在 `H_{kv}=8` 上，融合路径已开始逼近甚至轻微越过参考路径，但真正的优势仍要到长序列下才能被清晰放大。

### 91.5 公式安排

**不放显示公式。**

这一小节是 backend readout section，不需要公式。
最多允许保留少量行内记号：

* `TPOT`
* `H_{kv}=2/4/8`
* `triton_ra`
* `torch_ref`

### 91.6 图表安排

**只放 1 张正式表。**

* **表 4-12**：4K 序列 `TPOT` 对比

表下注建议保留两条信息：

1. `seq=4K, gen=128, batch=1, NVIDIA H20`
2. 粗体标注同模型 INT4 backend 中最快项

### 91.7 相对当前稿件的删改动作

* **保留**：旧 `4.5.1` 的表与核心读数；
* **改口气**：从“4K 下谁最快”改成“4K 入口层 backend landscape”；
* **删掉**：任何把 `4K` 微小差距夸成显著交叉点的表述；
* **保留**：`H_{kv}=2/4` 下 `triton_ra` 慢 `1–2 ms`、`H_{kv}=8` 下开始成为最快项这一核心结论。

### 91.8 完成后的目标印象

读完 `4.5.1`，评委脑子里应该只留下这一句：

> **短上下文入口处，融合路径还不是普适赢家；它的优势是有条件出现的，而且条件与 `H_{kv}` 明显相关。**

---

## 92. 第四章 4.5.2 写作计划

### 92.1 小节标题

保持为：

**4.5.2 长序列 TPOT 扩展规律**

### 92.2 小节职责

这一小节只承担一个任务：

> **把 `4K` 入口层仍然模糊的速度关系，推进到长序列下的清晰 scaling 证据。**

也就是说，`4.5.1` 只是告诉读者“优势可能开始出现”，而 `4.5.2` 要真正证明：

* 当序列长度拉长时，融合路径的收益不是噪声，而是会系统性放大；
* 这种收益的放大速度足够大，值得单列为部署结果。

### 92.3 本节不做什么

* 不在这里讲多模型交叉边界；
* 不在这里概括 `H_{kv}` 规律；
* 不把这一节写成一般理论解释；
* 不在这里给部署建议，那留到 `4.5.4`。

### 92.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：为什么选 14B 做 scaling anchor。**
先说清：`14B` 是旧稿里最适合承担长序列 scaling 读数的模型，因为它在 `H_{kv}=8` 下已经显现出明确的正向趋势，且长上下文下 memory traffic 足够重，最容易看清融合路径的真实收益。

**第 2 段：引出主表。**
这里引出新的 **表 4-13 Panel A**。正文只需说明：`4K / 8K / 16K / 32K` 四个长度点，比较 `torch_ref`、`KIVI` 与 `triton_ra` 的 `TPOT` 变化。

**第 3 段：读 scaling 数据。**
这一段集中读四组数：

* `4K`：`Δ = -0.44 ms`，仍在噪声范围；
* `8K`：`Δ = -14.54 ms`，约 `-17%`；
* `16K`：`Δ = -33.26 ms`，约 `-28%`；
* `32K`：`Δ = -77.08 ms`，约 `-40%`。

这组数据要写成一个很清楚的趋势句：

> 优势并不是在 `4K` 就被证明，而是在 `8K` 之后随序列长度增长而单调放大。

**第 4 段：把趋势送到交叉边界。**
结尾固定成：

> 因而，本节真正证明的不是 “14B 在某个点更快”，而是 “当历史 KV 变长后，融合路径的带宽节省可以逐步压过固定调度开销”；接下来的问题不是它是否能加速，而是这条加速边界会如何随 `H_{kv}` 改变。

### 92.5 公式安排

**不放显示公式。**

这里也不需要公式；`Δ (ms)` 与 `Δ (%)` 直接在表里呈现即可。

### 92.6 图表安排

**只放 1 张正式表。**

* **表 4-13 Panel A**：14B 长序列 `TPOT` 扩展规律

表下注建议保留：

* `gen=64, runs=10, NVIDIA H20`
* `Δ = triton_ra - torch_ref`

### 92.7 相对当前稿件的删改动作

* **保留**：旧 `4.5.2` 的主体表与四个长度点；
* **强化**：把 `4K` 的 `n.s.` 读法写清楚，避免读者误以为从短序列就已经显著胜出；
* **强化**：把 `8K→32K` 的收益放大写成这一小节的主轴；
* **删掉**：任何把 `14B` 单模型 scaling 误写成对所有模型都成立的泛化句。

### 92.8 完成后的目标印象

读完 `4.5.2`，评委脑子里应该只留下这一句：

> **融合路径的部署价值主要出现在长序列区间，而且收益会随序列长度增长而显著放大。**

---

## 93. 第四章 4.5.3 写作计划

### 93.1 小节标题

保持为：

**4.5.3 性能交叉边界与 GQA 影响**

这里的“性能交叉边界”是 story 已经冻结的中文化替换，不再使用旧稿的 **“Phase Boundary”** 作标题；同时 `GQA/H_{kv}` 仍然必须出现在标题中，因为旧稿的系统结果明确表明：交叉边界并不是只由序列长度决定，而是与 `H_{kv}` 同时变化。

### 93.2 小节职责

这一小节只承担一个任务：

> **把 `4.5.1–4.5.2` 的单点与单模型读数，上升成一个可命名的经验边界。**

它要回答三个更高层的问题：

1. `triton_ra` 何时慢、何时快；
2. 这条边界如何随 `H_{kv}` 与序列长度共同移动；
3. `H_{kv}` 与模型参数量相比，谁更接近这条边界的主导相关变量。

### 93.3 本节不做什么

* 不把这节写成严格因果识别；
* 不把 `H_{kv}` 的作用说成已经被完全隔离证明；
* 不在这里给最终部署建议；
* 不在这里重讲 kernel 机制。

### 93.4 最优段落结构

建议 **5 个自然段 + 1 张表**。

**第 1 段：先定义这是一条经验边界。**
这里要非常克制地写：
本节中的“性能交叉边界”是基于 `ΔT = triton_ra - torch_ref` 的经验读数，不是理论闭式模型；同时旧稿对 `|Δ| < 2 ms` 标注为 `n.s.`，这层谨慎口径必须保留。

**第 2 段：引出主表。**
这里引出新的 **表 4-13 Panel B**。正文只需说明：三模型、四个长度点、读的是 `triton_ra` 相对 `torch_ref` 的 `TPOT` 差值。

**第 3 段：读三条边界路径。**
这一段只读三类 regime：

* `1.5B, H_{kv}=2`：`+1.67 / +4.23 / +9.78 / +15.92`，全长度下都更慢；
* `7B, H_{kv}=4`：到 `32K` 才出现 `-4.90` 的交叉；
* `14B, H_{kv}=8`：从 `8K` 起即进入显著加速区间，随后负差值持续放大。

这段要压成一句核心判断：

> 交叉边界沿两个维度移动：`H_{kv}` 越高，交叉点越早；序列越长，融合路径越占优。

**第 4 段：并入 8B vs 14B 同 H_{kv}=8 控制对比。**
旧稿 `4.5.4` 的这段 supporting evidence 必须在这里吸收，不再单独成节。要点只读一件事：

* `8B (LLaMA-3.1, H_{kv}=8)` 与 `14B (Qwen2.5, H_{kv}=8)` 在 `4K` 处的交叉幅度高度一致：`-0.39 ms` vs `-0.44 ms`。

然后马上加一句边界声明：

> 这支持 “`H_{kv}` 比参数量更接近交叉边界主导因素” 的结构性读法，但由于当前模型集合中 `H_{kv}` 与规模部分共变，这里仍只能写成相关性支持，而不是严格因果识别。

**第 5 段：把本节结论压稳。**
建议固定成：

> 因此，部署层真正有用的不是某个 backend 在某个表格里多快几毫秒，而是一条可读出的经验边界：`H_{kv}=2` 基本落在融合路径不划算区，`H_{kv}=4` 只有到很长序列才越过边界，`H_{kv}=8` 则在较早长度点就进入收益区间。

### 93.5 公式安排

**不放显示公式。**

最多允许在正文里行内定义：

* `ΔT = triton_ra - torch_ref`

但不需要单独公式环境。
这一节的核心是 boundary readout，不是理论建模。

### 93.6 图表安排

**只放 1 张正式表。**

* **表 4-13 Panel B**：性能交叉边界表

旧 `4.5.4` 的 `8B vs 14B` 控制对比不再单独立表，只保留为正文 supporting paragraph。

### 93.7 相对当前稿件的删改动作

* **整体合并**：把旧 `4.5.3 Phase Boundary 分析` 与旧 `4.5.4 8B vs 14B 同 H_{kv}=8 控制对比` 合并为新 `4.5.3`；
* **改题**：从英文式 `Phase Boundary` 统一改成“性能交叉边界”；
* **保留**：旧边界表的所有核心读数，但并入新 `表 4-13` 的 Panel B；
* **保留但降调**：`8B vs 14B` 的 `H_{kv}` 证据，只写结构性支持，不写成因果证明；
* **删掉**：任何把这节写成 “GQA 已被严格证明是唯一决定因素” 的措辞。

### 93.8 完成后的目标印象

读完 `4.5.3`，评委脑子里应该只留下这一句：

> **部署路径的选择不是按模型名字决定，而主要取决于 `(H_{kv}, seq_len)` 是否已经越过性能交叉边界。**

---

## 94. 第四章 4.5.4 写作计划

### 94.1 小节标题

保持为：

**4.5.4 KV Cache 显存压缩率与部署建议**

这个标题对应当前冻结版也完全正确：它把旧稿的 `4.5.5 KV Cache 内存对比` 与 `4.5.6 部署建议` 合并成了一个更像论文节的收束小节。

### 94.2 小节职责

这一小节只承担一个任务：

> **把系统节从“速度边界”收束到“可执行部署结论”。**

因此它必须同时交代两件事：

1. INT4 路径在 KV Cache 存储上到底节省了多少；
2. 在给定硬件约束下，这些时间与显存读数应如何转写成部署建议。

### 94.3 本节不做什么

* 不再展开 `TPOT` 表；
* 不再讨论 `H_{kv}` 机制；
* 不在这里重复第三章复杂度公式；
* 不在这里把建议写成跨硬件通用规则。

### 94.4 最优段落结构

建议 **4 个自然段 + 1 张表**。

**第 1 段：先读显存表。**
引出新的 **表 4-14**。它只统计 `seq=4K, batch=1` 下的 KV Cache 占用，不含模型权重与激活峰值。这个限定必须写清。

**第 2 段：读显存压缩率。**
这里直接读四组数：

* `1.5B`：`115.5 MB → 30.7 MB`
* `7B`：`230.9 MB → 61.5 MB`
* `8B`：`527.9 MB → 140.5 MB`
* `14B`：`791.8 MB → 210.8 MB`

四个模型的压缩率都约为 `73.4%`。这里的落句要写成：

> 这说明在当前 INT4 路径下，KV Cache 的显存收益是稳定且可预估的；它不是只对某个模型成立的偶然现象。

**第 3 段：把显存收益与速度边界合起来。**
这一段只做一句综合判断：

> INT4 路径带来的显存节省是全局稳定收益，而速度收益则是条件性收益；因此真正的部署选择不是 “要不要压缩”，而是 “在既定压缩前提下，何时切到融合 backend 更划算”。

**第 4 段：把部署建议写成硬件限定结论。**
这里直接吸收旧 `4.5.6` 的三条建议：

* `H_{kv} ≥ 4` 且序列长度 `≥ 8K`：用 `triton_ra`
* `H_{kv}=2` 或序列长度 `< 8K`：用 `torch_ref`
* 这些建议严格限定在 `NVIDIA H20 (sm_90, 130 SM, 98 GB HBM)` 上，跨硬件时边界可能移动

然后用一句话桥到 `4.6`：

> 至此，第四章的系统层边界已经被限定清楚；下一节不再继续报结果，而转入对这些结果为何呈现出当前形状的机制辨析。

### 94.5 公式安排

**不放显示公式。**

这一节是 deployment takeaway section，不需要任何公式。

### 94.6 图表安排

**只放 1 张正式表。**

* **表 4-14**：KV Cache 显存压缩率对比

部署建议不单独立表，直接写成短段落或三条紧凑建议即可。

### 94.7 相对当前稿件的删改动作

* **整体合并**：把旧 `4.5.5` 与 `4.5.6` 合并为新 `4.5.4`；
* **保留**：旧 KV Cache memory 表的全部数值；
* **强化**：表注中的口径限定——只统计 KV Cache，不含模型权重与激活峰值；
* **保留**：旧部署建议的三条结论；
* **改写**：把建议写成 “H20 上的经验决策规则”，而不是普适部署律。

### 94.8 完成后的目标印象

读完 `4.5.4`，评委脑子里应该只留下这一句：

> **INT4 路径带来的显存收益是稳定的，而速度收益是条件性的；真正的部署策略要同时看压缩率与性能交叉边界。**

---

## 95. 4.5 相对当前稿件的总手术动作

`4.5` 的总改法可以概括为 7 条：

1. **把旧节名 `GQA-Aware 部署效率分析` 改成新 `部署效率评估`。**  
   `GQA/H_{kv}` 从节标题主位退回到 `4.5.3` 的结果解释变量。

2. **保留旧 `4.5.1 / 4.5.2 / 4.5.3 / 4.5.5` 的主体数据，但压缩为三张正式表。**  
   它们分别变成新 `表 4-12 / 4-13 / 4-14`。

3. **把旧 `4.5.4 8B vs 14B 同 H_{kv}=8 控制对比` 并入新 `4.5.3`。**  
   它只作为 supporting control paragraph，不再单独成节。

4. **把旧 `4.5.5 KV Cache 内存对比` 与旧 `4.5.6 部署建议` 合并成新 `4.5.4`。**  
   这样系统节的收束会更自然，也更符合 story 规定的四子节结构。

5. **全节去图化，只保留表。**  
   `4.5` 应是纯系统读数节，不需要额外的可视化包装。

6. **删掉一切方法动机复述。**  
   这一节不再重复 “为什么要做融合核”，只回答 “在什么条件下它值得用”。

7. **在 `4.5.4` 末尾加一条桥到 `4.6`。**  
   明确告诉读者：系统边界已经定义完毕，后面将进入机制与效度讨论，而不是再报更多实验表。

---

## 96. 4.5 完成后的目标印象

读完整个 `4.5`，评委脑子里应该只留下这一句话：

> **部署层的结论已经清楚：INT4 的 KV Cache 显存收益是稳定的，但 Triton 融合路径的速度收益具有明确边界；这条边界主要由 `H_{kv}` 与序列长度共同决定，并且当前结论严格限定在 H20 硬件条件下。**

---

## 97. 第四章 4.6 写作计划

### 97.1 小节标题

保持为：

**4.6 实验结果讨论与效度威胁**

这个标题现在是对的，不需要再改。它比旧稿的 **“综合讨论”** 更清楚，因为它把两件事一次说透了：这节既要做**机制辨析**，也要做**边界限定**。同时它也和 `story` 的硬规则完全一致：`4.6` 不是结果节，而是讨论节。

### 97.2 小节职责

`4.6` 在第四章中的唯一任务是：

> **把 `4.2–4.5` 已经成立的证据，提升为“为什么会这样、哪些地方还不能说得更强、这些限制意味着什么”的正式学术讨论。**

因此，这一节必须完成四层工作：

第一，解释 **KL 与 MSE** 为什么不是“永远谁更强”，而是存在**位宽 × 模型规模**依赖。
第二，把 **INT4 vs KIVI-style** 的旧“诚实分析”从笔记体改成正式学术写法。
第三，正面解释 **heuristic 为什么会是强基线**，并把它从“方法没赢多少”的风险点改写成“结构图谱的证据”。
第四，把所有 **scope limits / external validity / protocol limits / hardware limits** 统一收束。 

### 97.3 本节不做什么

`4.6` 必须明确克制，不承担以下内容：

* 不再重报 `4.2–4.5` 的主表数字；
* 不再把 `RQ1–RQ3` 逐条重做一遍，那是 `4.7` 和第五章的工作；
* 不在这里重新定义方法或补充新的主算法；
* 不把 `4.6.1` 写回第三章那种“方法合理性证明”；
* 不把 `4.6.4` 写成自我否定式“问题一大堆”的局限清单。

一句话说，`4.6` 只能做 **interpretation + scope control**，不能变成第二个结果章。

### 97.4 最优总结构

建议将 `4.6` 固定为：

* **1 个短导语**
* **4 个三级小节**
* **0 张图**
* **1 张小型 promoted table**
* **0 个正文显示公式**

这里我给出一个明确施工判断：

> **`4.6` 应当做成“段落主导、表格从属”的 discussion section。**

也就是说，全节最多只允许在 `4.6.1` 放一张由 `A.19.3` 前移来的小表；其余三节尽量全部依靠正文段落与对前文图表的回引完成，不再新增新的主图主表。这样能保证讨论节的气质不再滑回结果节。

### 97.5 节首导语

`4.6` 开头建议只写 **2 段**。

**第 1 段**只做一件事：承接 `4.5`。
推荐开头句：

> 第 4.2–4.5 节已经分别回答了规范路径保真、低比特恢复、跨模型策略图谱与部署效率边界。本节不再继续报结果，而转入这些结果为何呈现出当前形状的机制辨析。

**第 2 段**只做一件事：说明四个子节的分工。
建议固定为：

> 本节首先讨论 KL 与 MSE 目标在不同规模与位宽上的必要性差异，其后把 INT4 相关结果提升为分层机制讨论，再单独解释 heuristic 作为强基线的结构性含义，最后统一收束评测协议、比较口径与外推范围的边界。

### 97.6 图表总策略

`4.6` 的图表策略应冻结为：

* **不放图**
* **只在 `4.6.1` 放 1 张 promoted small table**
* **`4.6.2–4.6.4` 一律不新开主图主表**
* **所有讨论尽量回引 `4.2–4.5` 已有表图，而不是再复制一次**

这是因为当前 story 明确要求 `4.6` 做升维解释，而不是第二轮结果复述；如果在这里再铺图表，读者会立刻把它读回结果层。

---

## 98. 第四章 4.6.1 写作计划

### 98.1 小节标题

保持为：

**4.6.1 KL 与 MSE 校准目标的规模依赖性机制辨析**

这个标题必须锁死。它直接对应 `story` 里对 `A.19.3` 的冻结归宿，也与第三章已经刻意回避的边界完全对齐：`3.4.1` 负责回答“为什么 KL 可用”，而 `4.6.1` 才负责回答“KL 在什么条件下更必要”。

### 98.2 小节职责

这一小节只承担一个任务：

> **把“为什么用 KL”从第三章的方法合理性，推进成第四章的条件性结论。**

也就是说，这一节不能再写成“KL 理论上优于 MSE”；它要写成：

* 在某些 setting 下，分布代理更有必要；
* 在另外一些 setting 下，KL 与数值代理会趋同；
* 这种差异主要受 **bit-width** 与 **模型鲁棒性/规模** 共同调制。

当前现稿自己已经给出了这个讨论入口：旧 `3.3.1` 末尾曾提到“校准目标的规模依赖性”，后来被移出正文；附录 `A.19.3` 则正式保存了 **7B KL vs MSE 趋同验证**，这正是新 `4.6.1` 的核心证据来源。 

### 98.3 本节不做什么

* 不重写 `3.4.1` 的 KL 理由；
* 不把 `4.6.1` 写成又一个“实验对比主节”；
* 不声称“KL 在所有模型、所有位宽都严格优于 MSE”；
* 不把 7B 单块 appendix 证据外推成普适定律。

### 98.4 最优段落结构

建议 **5 个自然段 + 1 张小表**。

**第 1 段：为什么这个问题必须放到讨论节。**
直接承接 `4.2` 的 forward pointer：读者在 canonical `INT8` 成立后，自然会问“为什么这里需要 KL，而不是 MSE”。这段必须说明：这个问题不是方法定义问题，而是**必要性边界**问题，所以只能在讨论节回答。

**第 2 段：先给总体判断。**
这一段固定写成：

> 本文当前证据支持的不是 “KL 永远显著优于 MSE”，而是 “KL 的必要性随位宽压缩强度与模型鲁棒性而变化”：当量化扰动足以改变 attention ranking 时，分布侧代理更重要；当量化扰动已被模型与位宽共同吸收时，KL 与数值代理会趋同。

这段是本小节的总论点。

**第 3 段：引出并阅读 A.19.3 小表。**
这里把附录 `A.19.3 7B KL vs MSE 校准目标趋同验证` 前移为一张小型 promoted table。正文只读它的结构含义，不必逐格抄数：
7B 这块证据的关键不是“KL 赢了多少”，而是**KL 与 MSE 已经在该规模上高度趋同**。也就是说，模型越稳、位宽越不极端，行为代理与数值代理的差异越容易被吸收。

**第 4 段：机制解释。**
这里要把机制说得很克制：
在更敏感的小模型或更激进的低比特 setting 下，softmax 排序对 Key 误差更脆弱，attention 分布的错位会先于张量范数恶化暴露出来，因此 KL 这类分布代理更有诊断价值；而在较大模型、较高位宽或更平滑的架构上，quantization noise 不足以把 ranking 推离原本稳定区域，于是 KL 与 MSE 选出的参数会逐步收敛。
注意，这一段必须写成**解释性推断**，而不是定理式证明。

**第 5 段：把结论压稳。**
建议固定落成：

> 因而，本文对 KL 的主张应理解为“在 attention ranking 容易被量化噪声改写的场景中，KL 是更贴近行为损伤的工程代理”，而不是“KL 在所有规模与所有位宽下都会制造同样大的经验优势”。

### 98.5 公式安排

**不放显示公式。**

这节是讨论节，不再重开 KL 公式。最多允许少量行内符号：

* `KL`
* `MSE`
* `INT8`
* `INT4`

### 98.6 图表安排

**只放 1 张小型 promoted table。**

建议：

* **表 4-15**：由附录 `A.19.3` 前移而来的 `7B KL vs MSE` 趋同验证小表

这张表的职责不是提供新主结论，而是承担“规模依赖性边界”这句话的最小实证支撑。
我不建议在这里再配图，因为 `4.6.1` 的价值不在视觉冲击，而在于把“KL 的必要性”从绝对化语言降成条件性语言。 

### 98.7 相对当前稿件的删改动作

* **整体前移**：把附录 `A.19.3` 正式前移到正文 `4.6.1`；
* **保留**：7B 作为“趋同验证”的尺度锚点；
* **改口气**：从“KL 更好”改成“KL 更必要/更不必要”的条件性表述；
* **删掉**：任何 universal superiority 的措辞；
* **回扣第三章**：明确说明这节回答的是“何时更必要”，而不是“为什么可用”。

### 98.8 完成后的目标印象

读完 `4.6.1`，评委脑子里应该只留下这一句：

> **KL 的价值是条件性的：在敏感模型与低比特压力下更必要，在较稳的规模与较缓和的 setting 下则会与 MSE 趋同。**

---

## 99. 第四章 4.6.2 写作计划

### 99.1 小节标题

保持为：

**4.6.2 INT4 结果的分层机制辨析**

这个标题现在也必须锁死。它承担的是 story 明确规定的两件事：
一是把旧 `4.2.3 INT4 vs KIVI 的三层诚实分析` 学术化重写；
二是把 `inv_tau × GQA` 的诊断副产品并入本节的**架构依附性段内讨论**，不再单独抬成新的子节。

### 99.2 小节职责

这一小节只承担一个任务：

> **把 `RQ2` 的 low-bit 证据链，从“结果成立”提升成“机制上可以说到哪一步”。**

它必须保留旧 `4.2.3` 的三层结构，但写法要更正式：

* **事实层**：哪些现象是当前证据已经稳定支持的；
* **解释层**：哪些机制解释是合理但仍属 inference；
* **开放层**：哪些问题当前还不能说得更强。

当前主稿的旧 `4.2.3` 已经给出了这套框架，而且旧 `4.2.2` 末尾那段 “RoleAlign 的独立价值” 也已经提供了三条很好的解释入口：非对称格式的决定性、可审计 artifact 的价值、与 allocator 的自然接口。新 `4.6.2` 的任务就是把这些散落判断收束成学术讨论。

### 99.3 本节不做什么

* 不重报 `4.3.1–4.3.3` 的所有表格数值；
* 不再把这节写成 “RoleAlign vs KIVI 谁赢了”；
* 不把 `inv_tau` 升格回主线组件；
* 不把架构依附性写成已被严格因果识别的定论。

### 99.4 最优段落结构

建议 **6 个自然段，0 图，0 表**。

**第 1 段：先说这节讨论什么，不讨论什么。**
这一段必须写清：本节讨论的是 `INT4` 结果的解释边界，而不是重新比较 `INT4-RoleAlign` 与 `KIVI-style` 的主表输赢。

**第 2 段：事实层。**
这里直接吸收旧 `4.2.3 L1` 的口径，但用更正式的语言：
在当前 clean-provenance 范围内、同一非对称格式下，`INT4-RoleAlign` 与 `KIVI-style` 在 quality / PPL / Needle 上的差距总体很小；Needle 恢复共同指向 role-aware 非对称格式的重要性，而不是 calibration philosophy 先造成巨大质量分化。
这一段只报**结论型事实**，不必再逐格报数。旧稿已经把 L1 写得很清楚：`|ΔPPL| ≤ 0.15`、Needle 均 100%、LongBench mean 差距很小。

**第 3 段：解释层——格式是一阶，校准是一阶还是二阶？**
这一段要稳稳落成：

> 当前证据更支持“非对称 role-aware format 是 low-bit 恢复的一阶条件”，而离线 behavior-guided calibration 的经验优势更多体现为二阶性质：它把该格式背后的架构选择解释化、把参数来源固化为可审计产物，并为 allocator 留出统一接口。

这里还必须显式锁定口径，避免旧说法回流：

> 这里所谓的 offline role-aware calibration，不应被理解为由同一个 KL 分数统一选出 `(p_K,p_V)`；更准确地说，`K-path` 以 attention-distribution KL 为主代理，并在离线搜索纪律下确定参数，`V-path` 由独立输出扰动代理主导，两者共享的是 workflow、artifact interface 与稳健选择纪律。

这句话本质上就是把旧 `4.2.2` 的“RoleAlign 的独立价值”学术化重写。

**第 4 段：架构依附性段——family / scale / GQA。**
这里要把 `4.3.1` 和 `4.3.2` 的发现升维：
low-bit cliff 的强度并不只由位宽决定，还受 family、scale 与 `H_{kv}` 调制；`LLaMA-3.1-8B` 之所以在对称 `INT4` 下表现为架构例外，说明我们面对的不是一个“单一位宽规律”，而是一类架构依附的误差传播现象。
这段要非常注意语气：是**结构相关**，不是严格证明。

**第 5 段：把 `inv_tau` 放回历史探索位置。**
这一段必须单独写，但只占一小段：
附录 `A.19.1–A.19.2` 已经给出 `\tau^{-1}` 的量化平坦化动机与 `Q` 预缩放等价实现；然而第三章主线已经明确，`RoleAlign` 的最终默认配置是 `inv_tau = None`，逐头温度校正只保留为诊断性历史探索项。因此，在第四章讨论里，`inv_tau × GQA` 只能被写成**架构依附性的 suggestive diagnostic**, 不能被重新抬成正文主方法。 

**第 6 段：开放层。**
结尾应当落到真正的 open question 上：

> 当前同-format 对比已经说明 RoleAlign 的价值不在 winner-take-all 跑分差，而在方法组织层；但在哪些更极端 setting 下 calibration philosophy 会重新变成一阶因素——例如更小 chunk、更接近 streaming 的 decode 粒度、更大 official task 覆盖——仍是开放问题。

这样写，既不会把问题说死，也不会让讨论段显得空泛。

### 99.5 公式安排

**不放显示公式。**

本节是纯 discussion。
最多允许保留行内符号：

* `|ΔPPL| ≤ 0.15`
* `H_{kv}`
* `\tau^{-1}`
* `inv_tau=None`

### 99.6 图表安排

**不放图，不放表。**

这节的 strongest evidence 已经在 `4.3` 与附录里存在；新 `4.6.2` 的价值是重组解释，不是再搬一张新图。

### 99.7 相对当前稿件的删改动作

* **整体后移并重写**：把旧 `4.2.3 INT4 vs KIVI 的三层诚实分析` 整体迁入 `4.6.2`；
* **保留**：原来的 L1 / L2 / L3 三层框架；
* **吸收**：旧 `4.2.2` 末尾 “RoleAlign 的独立价值” 那段三条解释；
* **并入**：`inv_tau × GQA` 只作为本节架构依附性段内讨论；
* **删掉**：任何仍然像 internal note 一样的 “honest analysis” 研究笔记体语气。

### 99.8 完成后的目标印象

读完 `4.6.2`，评委脑子里应该只留下这一句：

> **INT4 结果已经足以支持“格式是一阶、校准是二阶组织增益”的当前判断，但这一判断仍然受到 family / scale / GQA 与 decode setting 的条件限制。**

---

## 100. 第四章 4.6.3 写作计划

### 100.1 小节标题

保持为：

**4.6.3 Heuristic 强基线的结构性含义**

这个标题也必须锁死。因为当前 frozen story 和摘要都已经把这点抬升成全文结论的一部分：heuristic 并不是“本方法没赢多少”的尴尬残差，而是 regime map 自身的结构发现。 

### 100.2 小节职责

这一小节只承担一个任务：

> **把 heuristic 的“强”从威胁，改写成本文贡献的一部分。**

它要回答的核心问题不是“heuristic 为什么没那么差”，而是：

* 为什么在多个 regime 中，position-based protection 会与 behavior-guided policy 选中相近层集；
* 为什么这种重合不是对方法主线的削弱，反而说明 regime map 是真的在读结构，而不是在过拟合表格。

### 100.3 本节不做什么

* 不再逐任务重报 `4.4` 的所有分数；
* 不做“为自己辩护”式写法；
* 不把 heuristic 重说成 universal baseline；
* 不把 `3B` 的极端 gap 与 `14B` 的 top-tier cluster 混在一起不分层解释。

### 100.4 最优段落结构

建议 **4 个自然段，0 图，0 表**。

**第 1 段：先立观察。**
直接写清：`4.4` 的跨模型主表与典型剖面已经显示，在若干模型与预算带内，heuristic 与 behavior-guided 的结果非常接近，甚至选中集合部分重合。这一观察在摘要里也已经被正式写成“heuristic is a strong baseline”。

**第 2 段：第一层解释——窄选择空间。**
这一段必须把最值钱的解释写出来：
在某些 regime 下，真正重要的层本来就集中在一个很窄的位置区间，因此“保护哪几层”并不是一个高自由度问题，而是一个接近决定论的窄选择空间。此时 heuristic 能逼近 behavior-guided，不是因为它更聪明，而是因为结构本身已经把可行解压窄了。

**第 3 段：第二层解释——重合不是处处发生。**
这里必须回扣 3B 的反例：
Qwen2.5-3B 的 early-layer rescue case 说明，一旦真正的敏感层极端集中于最早层，而 heuristic 的位置假设恰好偏离，差距就会突然放大到回救级别。因此 heuristic 的“强”，与 3B 的“大失败”并不矛盾；它们共同构成 regime map 的两极。

**第 4 段：把贡献重新定义。**
建议固定落成：

> 因此，behavior-guided allocator 的价值不应被简单理解为“始终大幅击败 heuristic”，而应理解为“在跨模型条件下识别何时 heuristic 已经足够、何时它会选错关键层”。正是这种区分能力，把本文从单纯跑分比较提升为结构图谱工作。

### 100.5 公式安排

**不放显示公式。**

这一节是纯结构讨论，不需要公式。

### 100.6 图表安排

**不放图，不放表。**

前面 `4.4.1–4.4.5` 的主表、heatmap、Pareto 与 case 表已经足够；新 `4.6.3` 的价值在于命名与解释，不在于再贴一个比较图。

### 100.7 相对当前稿件的删改动作

* **新建**：这是旧稿里没有被独立写出来的一节；
* **吸收**：把旧 `4.3` 和 `4.4` 里关于 heuristic 的零散句子统一抽出；
* **强化**：从“强基线提醒”升级成“结构性含义”；
* **删掉**：任何防御式口吻，例如“这并不意味着本文失败”。

### 100.8 完成后的目标印象

读完 `4.6.3`，评委脑子里应该只留下这一句：

> **heuristic 的强，不是本文贡献的反证；恰恰相反，它说明某些 regime 的可行层集本来就很窄，而本文真正贡献的是识别这种窄区间何时存在、何时失效。**

---

## 101. 第四章 4.6.4 写作计划

### 101.1 小节标题

保持为：

**4.6.4 威胁效度与外推边界**

这个标题不需要改。它和 story 的最后一节完全对应，而且正好承担所有答辩时会被追问的“你这结论能外推出多远”的问题。

### 101.2 小节职责

这一小节只承担一个任务：

> **把第四章所有结论的适用范围一次性讲清。**

它必须统一收束四类边界：

1. **评测协议边界**：合成 LongBench-style 主矩阵 vs 官方 LongBench sanity check
2. **比较口径边界**：same-order INT4 budget band vs matched-budget formal compare
3. **系统边界**：H20、batch=1、greedy decode、当前 backend 组合
4. **数据溯源边界**：clean rerun vs supporting legacy/backport rows

### 101.3 本节不做什么

* 不再重做“局限性陈述”的情绪化写法；
* 不把未来工作写满这一节；
* 不把限制夸大到像是在撤回已成立结论；
* 不在这里重新开启 prompt-adaptive 等 exploratory branch。

### 101.4 最优段落结构

建议 **5 个自然段，0 图，0 表**。

**第 1 段：评测协议边界。**
先把最重要的一条写清：
正文主矩阵统一使用 **LongBench 风格合成基准任务**，而官方 LongBench 真实数据对照只在 `4.2.2` 以 `1.5B + 3 任务 + 单种子` 的有限 scope 出现，因此它承担的是协议一致性 sanity check，而不是全面真实场景泛化证明。这个边界必须再次重申，因为这是第四章最容易被 overclaim 的位置。 

**第 2 段：比较口径边界。**
这一段要把 `4.4` 的诚实口径锁死：
cross-model allocator 结果报告的是 **same-order INT4 budget band** 内的结构读数，而不是严格 matched-budget formal compare。也就是说，`4.4` 可以支持 “regime map exists” 这类结论，但不能被写成“在严格同预算下某单一 policy 已被形式化证明更优”。 

**第 3 段：系统与硬件边界。**
这里要明确写出：
部署效率结论严格绑定于当前 `NVIDIA H20`、固定软件栈、当前 kernel 实现、`batch=1` 与贪婪解码设定；`TPOT` 交叉边界的具体位置在其他 GPU、其他 batch、其他 runtime 上都可能移动。因此 `4.5` 支持的是**经验边界**，不是 hardware-agnostic 定律。

**第 4 段：数据溯源边界。**
这一段要把 provenance 纪律再次收口：
正文主结论以 clean-provenance rerun 为准；个别 supporting rows 或 external-validity references 若来自 legacy/backport，必须在表注中显式说明，且不得用于替代主表结论。当前现稿在 `RoleAlign vs KIVI-style` 对比里已经对 `14B` 的 external-validity role 与 paired scope boundary 作过说明，这种写法必须保留。

**第 5 段：把边界写成 future-work interface。**
最后用一段非常克制的方式把未来外推方向点到为止：
更完整的 official dataset 验证、matched-budget formal compare、更广 hardware matrix、更成熟的 role-aware allocator 与 prompt-adaptive routing，都是自然的后续工作；但它们应被理解为**扩大当前结论适用域**，而不是修补当前结论本身失效。
这一句对答辩非常重要。

### 101.5 公式安排

**不放显示公式。**

这一节是范围限定节，不需要公式。

### 101.6 图表安排

**不放图，不放表。**

这节最重要的是口径清晰，不是视觉化。

### 101.7 相对当前稿件的删改动作

* **整体新建**：旧 `4.6.2 威胁效度分析` 需要扩写成更完整的 `4.6.4`；
* **吸收**：官方 LongBench scope 限制、same-order budget band 口径、H20 边界、legacy/backport 说明；
* **强化**：把限制写成“适用域说明”，而不是“自我削弱”；
* **删掉**：凡是像 memo 或自我防守 note 的口头语句。

### 101.8 完成后的目标印象

读完 `4.6.4`，评委脑子里应该只留下这一句：

> **第四章的结论已经足够成立，但它们成立在一个被清楚限定的协议、预算口径、硬件环境和数据溯源边界之内。**

---

## 102. 4.6 相对当前稿件的总手术动作

`4.6` 的总改法可以概括为 7 条：

1. **把旧 `4.6 综合讨论` 拆成新的四子节讨论结构。**
   这是从“结果复述”走向“讨论分层”的根本动作。

2. **把附录 `A.19.3` 前移为 `4.6.1` 的唯一 promoted table。**
   它不再只是附录里的支持材料，而是“KL 何时更必要”的正式正文证据。 

3. **把旧 `4.2.3` 的三层诚实分析整体后移并学术化为 `4.6.2`。**
   保留事实层 / 推测层 / 开放层结构，但去掉研究笔记体口吻。

4. **把 `inv_tau × GQA` 吸收到 `4.6.2` 的架构依附性段内讨论。**
   它只能作为诊断副产品出现，不能单列为新的主方法小节；主线配置仍是 `inv_tau=None`。 

5. **把 heuristic 强基线从零散提醒升级为 `4.6.3` 的正式讨论节。**
   这是保护全文贡献叙事的关键动作。

6. **把官方 LongBench、same-order budget band、H20 边界和 provenance 限制统一收口到 `4.6.4`。**
   不再让这些诚实边界散落在各节脚注里。 

7. **全节去图化，最多只保留 1 张小表。**
   让 `4.6` 保持真正的 discussion 气质，而不是第二轮结果章。

---

## 103. 4.6 完成后的目标印象

读完整个 `4.6`，评委脑子里应该只留下这一句话：

> **第四章到这里已经不再只是“证据成立”，而是“证据被解释、边界被限定、贡献被重新命名”——KL 的价值是条件性的，INT4 的恢复是分层成立的，heuristic 的强是结构信号，而所有结论都被明确限制在当前协议与部署边界内。**

---

## 104. 第四章 4.7 写作计划

### 104.1 小节标题

保持为：

**4.7 本章小结**

这个标题不需要再改。它的功能非常单纯：不是再做一轮“综合讨论”，也不是提前进入第五章，而是把第四章已经建立好的证据链按研究问题顺序收束成**章级答案**。冻结版 `story` 对这一点写得很清楚。

### 104.2 小节职责

`4.7` 在第四章中的职责只有两个：

第一，**把 `4.2–4.6` 已经成立的证据压缩成对 `RQ1–RQ3` 的章级回答。**
第二，**把读者从“第四章证据已经成立”平稳送到“第五章按研究问题归总全文”。**

也就是说，`4.7` 回答的不是新问题，而是：

> **第四章到底已经证明了什么。**

它必须让评委在离开第四章时，脑中只剩下三个非常清楚的判断：
`RQ1` 已回答到什么程度；
`RQ2` 已回答到什么程度；
`RQ3` 已回答到什么程度；
以及这些回答已经被部署边界与效度边界限定到哪里。第四章的职责本来就是“回答证据是否成立、边界在哪里”，而 `4.7` 就是这个职责的最后封口。

### 104.3 本节不做什么

`4.7` 必须非常克制，不承担以下内容：

* 不重报表格和图的细节数字；
* 不再展开 `4.6` 的机制讨论；
* 不再单独讨论威胁效度；
* 不提前写第五章的“创新点与学术价值”；
* 不把未来工作塞进本节；
* 不把本章小结写成摘要或结论章节的缩写版。

一句话说，`4.7` 不是“长版结果复述”，也不是“第五章预演”，而是**第四章内部的短封口**。这一点和冻结版 story、以及当前施工文档的写作分工完全一致。

### 104.4 最优总结构

建议将 `4.7` 固定为：

* **5 个短自然段**
* **0 张图**
* **0 张表**
* **0 个正文显示公式**
* **篇幅控制在 0.5–0.8 页**

这是最稳的长度。太短会像一句空话，太长又会重新滑回“再讲一遍第四章”。

我建议 5 段的分工固定成下面这样：

**第 1 段：章级收束句。**
只用一段很短的话说清：第四章已经围绕 `RQ1–RQ3` 建立起从 canonical fidelity 到 low-bit recovery、再到 cross-model regime map 的完整证据链，并进一步给出了部署边界与解释边界。

**第 2 段：按 `RQ1` 收束。**
这里概括 `4.2` 的两步证据链：
behavior-guided calibration 已在 `INT8 canonical path` 上形成最干净、最可复现的基础保真路径；官方 LongBench 的小范围真实数据对照进一步表明，这一结论至少没有被主文的合成评测协议系统性带偏。
这里允许保留**一个**最承重的锚点数字，最合适的就是 `mean Δ=+0.02`，不要再多。

**第 3 段：按 `RQ2` 收束。**
这里概括 `4.3` 的完整 failure→diagnosis→recovery 链：
同一原则并不能机械地下推到对称 `INT4`；对称 `INT4` 会出现架构依附性的阶跃失效，完整诊断显示真正首先触发崩塌的是 Key 侧精度下降，而 role-aware 非对称格式则把 low-bit path 恢复为一个可审计、可扩展的实例化路径。
这里也只允许保留**一组压缩锚点**；最合适的是“同 format 下 `100% Needle / |ΔPPL|≤0.15`”这一组压缩表述，但不要把整张表再讲一遍。

**第 4 段：按 `RQ3` 收束。**
这里概括 `4.4` 的 regime-map 贡献：
behavior sensitivity profile 不只是校准副产物，它还能支撑同量级预算带内的跨模型分配，并读出四种可命名的结构区间：Mistral 的 AutoK 正向案例、3B 的早层回救区间、LLaMA-8B 的中等规模共识区间，以及 14B 的高性能簇共存而无稳定最优。
这一段必须显式保留一句：**heuristic 是强基线**，但它在这里不是负面结果，而是 regime map 的结构证据。

**第 5 段：部署与桥接。**
这一段只做两件事：
一是把 `4.5–4.6` 的边界收成一句话——INT4 的 KV Cache 显存收益是稳定的，但融合 decode 路径的速度收益依赖于 `(H_{kv}, seq_len)` 是否越过性能交叉边界；KL 的价值、INT4 的恢复以及 heuristic 的强基线，都应理解为条件性结论而不是普适定律。
二是给出一个很短的桥：第五章将不再补证据，而是按 `RQ1–RQ3` 对全文进行最终归总。

### 104.5 公式安排

**不放显示公式。**

`4.7` 是章级收束节，不是方法节，也不是讨论节。
最多允许出现极少量行内记号，例如：

* `RQ1–RQ3`
* `INT8`
* `INT4`
* `H_{kv}`

但不应该出现任何新的公式环境，也不应把前文公式重新贴一遍。

### 104.6 图表安排

**不放图，不放表。**

这一点最好锁死。
`4.7` 的功能是把前面已经承重的图表结论压成一句话，而不是再给一张“总览图”或“总结表”。一旦这里再贴图表，读者会立刻误读成“又来了一个结果节”。

### 104.7 相对当前稿件的删改动作

`4.7` 相对当前现稿，实际上是一次**从无到有**的新建动作。

因为现稿目录里第四章当前止于 `4.6 综合讨论`，随后第五章直接进入“主要发现”；也就是说，旧稿事实上把第四章自己的章级收束工作，外包给了第五章开头。新结构下这不再合适：第四章既然已经被重构成 `RQ1 → RQ2 → RQ3 → 部署效率 → 讨论` 的证据章，就必须有自己的短封口。

具体手术动作建议定为 4 条：

**第一，新增 `4.7 本章小结` 小节。**
这不是从旧稿里平移，而是正式补建。

**第二，把旧 `4.6.1 主要发现` 中可保留的 load-bearing 结论抽出来，但必须重写。**
不能把旧“主要发现”原封不动搬到 `4.7`，因为旧稿那一节仍带有结果罗列式写法；新 `4.7` 必须改写成按 `RQ1–RQ3` 排列的章级答案。

**第三，把部署边界与讨论边界压成一句条件性收束。**
`4.5–4.6` 的作用不是在 `4.7` 再展开一轮，而是被压成“哪些结论成立、成立到哪里为止”的一句话。

**第四，给第五章留出真正的章职能。**
也就是：
`4.7` 负责“第四章已经证明了什么”；
第五章负责“整篇论文对 `RQ1–RQ3` 的最终回答、创新点与展望”。
两者不能再互相代劳。

### 104.8 完成后的目标印象

读完 `4.7`，评委脑子里应该只留下这一句话：

> **第四章已经把三件事证实清楚了：behavior-guided calibration 在 canonical 位宽上成立，在 low-bit 场景下可经角色感知路径恢复，并且还能进一步读出跨模型的策略适用区间；这些结论同时被部署边界与效度边界清楚限定。**

---

## 105. 4.7 建议直接落笔时的段落骨架

你后面真写正文时，最稳的骨架可以直接照这个顺序写：

**第 1 段**
本章围绕 `RQ1–RQ3` 对 behavior-guided framework 的实证有效性进行了系统验证，并在 canonical fidelity、low-bit recovery、cross-model regime map、部署效率边界与机制讨论五个层面建立了完整证据链。

**第 2 段（RQ1）**
针对 `RQ1`，本章首先在 `INT8 canonical path` 上确认了行为引导校准能够形成稳定、可复现的基础保真路径；同时，官方 LongBench 的小范围真实数据对照表明，该结论与主文合成协议的方向一致，从而为规范路径提供了额外可信度补强。

**第 3 段（RQ2）**
针对 `RQ2`，本章进一步表明：同一原则在对称 `INT4` 上会遭遇架构依附性的阶跃失效，其主导触发侧来自 Key 精度下降；而在 role-aware 非对称格式下，`INT4-RoleAlign` 则将 low-bit path 恢复为一条可审计、可扩展的行为引导实例。

**第 4 段（RQ3）**
针对 `RQ3`，本章证明了行为敏感度画像不仅服务于校准，还能够支撑跨模型预算分配，并揭示由 family、scale 与 task 共同决定的策略适用区间图谱。该图谱既包含 AutoK 的最显著正向案例，也包含早层回救区间、中等规模共识区间与高性能簇共存区间；与此同时，heuristic 作为强基线的存在被解释为一项结构观察，而非对本文方法主线的否定。

**第 5 段（部署与桥接）**
在此基础上，本章还进一步限定了上述结论的系统与外推边界：INT4 的 KV Cache 压缩收益是稳定的，而融合 decode 路径的速度收益则受 `H_{kv}` 与序列长度共同决定。第五章将在这些已被限定的证据基础上，进一步按 `RQ1–RQ3` 对全文结论与学术价值作最终归总。

---

## 106. 第四章图表制作清单

### 106.1 用途

本区块不再保留“施工期 inventory”口径，而直接冻结第四章 **final keep-set** 的图表系统，用于：

1. 锁定第四章最终保留的图表数量、编号与职责
2. 明确哪些旧资产可以直接平移，哪些必须合并、下沉或删除
3. 为后续 Chapter 4 正文 draft 与最终 `tex` 回写提供唯一图表基线

### 106.2 最终连续编号

- **正式图**：6 张（`图 4-1 ~ 图 4-6`）
- **正式表**：15 张（`表 4-1 ~ 表 4-15`）

说明：

- 这一版是第四章当前唯一有效的 **final keep-set**
- 早先 `表 4-1 ~ 表 4-21` 的 inventory 编号，只应视为施工期盘点，不再作为最终回写编号

### 106.3 已冻结的 4 个压缩动作

1. **删除**第四章中的 `RoleAlign vs KIVI-style` 设计差异表  
   该表的正式归宿固定在第三章 `3.5.4`；第四章只保留 same-format 的跨模型结果表。

2. **下沉**`4.3.2` 的 14B supporting extension  
   它不再作为第四章正式表保留，只作为 supporting evidence 存在于旧资产或正文说明层。

3. **合并**`4.3.2` 的 RULER 与 LongBench-style 多任务结果  
   合并为一张双 panel 表，避免同一结论分两张表重复承重。

4. **合并**`4.4` 与 `4.5` 的部分细节表  
   - `4.4` 的 4 个 case 表压为 2 张双 block 表  
   - `4.5.2` 与 `4.5.3` 合并为 1 张部署边界综合表

### 106.4 使用纪律

- 先按本清单锁定图表职责，再写正文
- caption / table note 视为正式证据的一部分
- 若旧正文资产与本清单冲突，以本清单和对应小节施工说明为准
- 若小节正文里仍残留旧表号建议，以本清单的最终连续编号为准

---

## 107. 图 4-1 ~ 图 4-6 制作清单

### 图 4-1

- **标题建议**：`K/V` 精度敏感性机制图（RULER，32K）
- **归属小节**：`4.3.2`
- **章内职责**：把“Key 主导退化 + MixedKV 恢复 + LLaMA 架构例外”压成主摘要图
- **必须包含**
  - 横轴模型：`Qwen2.5-1.5B / Qwen2.5-7B / LLaMA-3.1-8B`
  - 纵轴：RULER 通过率（%）
  - 四配置：`K-only / V-only / K4V8 / MixedKV`
  - 明确突出 Qwen 系列的 `K4V8 = 0` 与 LLaMA-8B 的架构例外
  - 图注写明：`32K, seed=1234–1236, n=3, greedy decode`
- **不得包含**
  - 不混 Needle / PPL / LongBench 其他指标
  - 不写成 “RoleAlign 胜利图”
- **资产状态**：旧 `Figure 4-1` 直接平移，caption 重写

### 图 4-2

- **标题建议**：Qwen2.5-1.5B 与 LLaMA-3.1-8B 的配对 `K/V` 重建误差热力图
- **归属小节**：`4.3.2`
- **章内职责**：作为支持性误差结构图，解释 `MixedKV` 在小模型与 `H_{kv}=8` 模型上的不同误差形状
- **必须包含**
  - `2 × 3` 布局
  - 上排 `Qwen2.5-1.5B`，下排 `LLaMA-3.1-8B`
  - 左 Key 热力图，中 Value 热力图，右逐层汇总曲线
  - 统一色标、峰值层标记
  - 图注保留“支持性误差分布图，不替代主指标”
- **不得包含**
  - 不拆成两张图
  - 不用不同色标分别画两个模型
- **资产状态**：旧 `Figure 4-2` 直接平移

### 图 4-3

- **标题建议**：Behavior-guided AutoK 的逐层保护决策（4 模型）
- **归属小节**：`4.4.1`
- **章内职责**：把 cross-model footprint 从表格现象提升为 profile shape 可视化
- **必须包含**
  - 四模型：`Qwen2.5-3B / LLaMA-3.1-8B / Qwen2.5-14B / Mistral-7B`
  - `layer × {K,V}` 二维保护图
  - `INT4(default)` 与 `INT8(protected)` 二元状态
  - coverage 阈值与平均 bit 写入子图标题
  - 结构形状可一眼区分：早层集中 / 中层簇集 / wide-spread
- **不得包含**
  - 不叠加 heuristic 结果
  - 不把它改成 budget-quality 图
- **资产状态**：旧 `Figure 4-3` 直接平移

### 图 4-4

- **标题建议**：Quality–预算 Pareto 视图（3 模型 subplot）
- **归属小节**：`4.4.1`
- **章内职责**：可视化 same-order budget band，而不是 matched-budget formal compare
- **必须包含**
  - 三个 subplot：`Qwen2.5-7B / LLaMA-3.1-8B / Mistral-7B`
  - 横轴 `Average KV bit budget`
  - 纵轴 `task-core mean quality`
  - 四类 policy marker：`Uniform / BA-k / Heuristic-k / BA-AutoK`
  - Pareto front
  - 图中标出：
    - `Qwen7B` 的 quality cliff
    - `Mistral` 的 strongest positive region
    - `LLaMA-8B` 的 near-Pareto cluster
- **不得包含**
  - 不加入 `3B / 14B`
  - 不写成严格 memory matched 图
- **资产状态**：旧 `Figure 4-4` 直接平移

### 图 4-5

- **标题建议**：跨模型策略适用区间 heatmap
- **归属小节**：`4.4.1`
- **章内职责**：作为 skim 入口，让读者一眼看到“4 模型 4 落点”
- **必须包含**
  - `4 × 4` heatmap
  - 行：`Qwen2.5-3B / LLaMA-3.1-8B / Qwen2.5-14B / Mistral-7B`
  - 列：`Uniform / BA-k / Heuristic-k / BA-AutoK`
  - 单元格显示 raw mean quality
  - 行内归一化色条
  - 每行最佳 cell 红框标出
- **不得包含**
  - 不混 task-level 细节
  - 不做聚类树
- **资产状态**：旧 `Figure 4-5` 直接平移

### 图 4-6

- **标题建议**：family/scale 分类排序汇总图
- **归属小节**：`4.4.5` 节末
- **章内职责**：作为 `4.4` 的 section-level close，统一命名四种 regime
- **必须包含**
  - 四条横向卡片/泳道：
    - `Qwen2.5-3B`：早层保护回救区间
    - `LLaMA-3.1-8B`：中等规模共识区间
    - `Qwen2.5-14B`：高性能簇共存而无稳定最优
    - `Mistral-7B`：AutoK 最显著正向案例
  - 每条卡片写：模型名、`H_{kv}`、rank1 policy、near-top competitors、一句结构解释
- **不得包含**
  - 不再放 task-level 数字
  - 不与图 4-5 重复为 heatmap
- **资产状态**：旧 `Figure 4-6` 后移并重定位到 `4.4.5` 末尾

---

## 108. 表 4-1 ~ 表 4-15 制作清单

### 4.1 实验设置与可信性声明

- **表 4-1**：模型、家族、规模、GQA 配置与实验角色  
  归属：`4.1.1`  
  必填列：模型、模型族、参数规模、GQA `(H_q,H_{kv})`、Layers、头维度、实验角色

- **表 4-2**：比较方法与配置总表  
  归属：`4.1.3`  
  必填列：量化模式、calibration philosophy、量化轴、Key bit、Value bit、默认后端

### 4.2 `RQ1`：行为引导校准的保真度验证

- **表 4-3**：INT8 规范路径保真度  
  归属：`4.2.1`  
  必填列：任务、`FP16`、`INT8-Canonical`、`Δ`  
  **硬纪律**：本表必须净化为纯 `FP16 vs INT8-Canonical`；旧正文里的 `INT4-RoleAlign / KIVI-style` 参考列不得同权保留。

- **表 4-4**：官方 LongBench 真实数据对照（protocol sanity check）  
  归属：`4.2.2`  
  必填列：官方任务、`FP16`、`INT8-Canonical`、`Δ(pp)`  
  **硬纪律**：表注必须写清单模型、单种子、小样本、仅作一致性检验。

### 4.3 `RQ2`：低比特路径从对称失效到 RoleAlign 恢复

- **表 4-5**：对称 INT4 cliff summary  
  归属：`4.3.1`  
  必填列：模型、`H_{kv}`、`FP16 Needle`、对称 `INT4 Needle`、诊断标签  
  说明：这是 `4.3.1` 的独立承重件，不能再只靠导语文字。

- **表 4-6**：Key/Value 单侧干预的 PPL 诊断表  
  归属：`4.3.2`  
  必填列：消融配置（`FP16 / K@INT8+V@FP16 / K@FP16+V@INT8 / K@INT4+V@FP16 / K@FP16+V@INT4`）、PPL、相对 `FP16` 变化

- **表 4-7**：K/V 消融多任务结果  
  归属：`4.3.2`  
  结构：双 panel  
  - Panel A：RULER，列为 `模型 / FP16 / K-only / V-only / K4V8 / MixedKV`
  - Panel B：LongBench-style task-core mean，列为 `模型 / FP16 / K-only / V-only / K4V8 / MixedKV`
  **硬纪律**：此表替代原来分开的 RULER 与 LongBench-style 两张正式表。

- **表 4-8**：`INT4-RoleAlign` 与 `KIVI-style` 的跨模型主对比  
  归属：`4.3.3`  
  必填列：模型、`H_{kv}`、`FP16 PPL`、`KIVI-style PPL`、`RoleAlign PPL`、`ΔPPL`、`KIVI-style Needle`、`RoleAlign Needle`、备注  
  **硬纪律**：
  - 第四章不再重复“设计差异表”
  - 设计差异的正式归宿固定在第三章 `3.5.4`
  - 第四章只保留 same-format 的结果表

### 4.4 `RQ3`：跨模型预算分配与策略适用区间图谱

- **表 4-9**：同量级预算下的跨模型主表  
  归属：`4.4.1`  
  必填列：模型块、任务/汇总、`Uniform INT4`、`BA-k`、`Heuristic-k`、`BA-AutoK`、行内 winner、备注  
  **硬纪律**：caption 或表注中必须显式写出 **same-order INT4 budget band**，不得写成 strict matched-budget compare。

- **表 4-10**：典型案例表 A  
  归属：`4.4.2–4.4.3`  
  结构：双 block  
  - Panel A：`Mistral-7B`
  - Panel B：`Qwen2.5-3B`

- **表 4-11**：典型案例表 B  
  归属：`4.4.4–4.4.5`  
  结构：双 block  
  - Panel A：`LLaMA-3.1-8B`
  - Panel B：`Qwen2.5-14B`

### 4.5 部署效率评估

- **表 4-12**：4K 序列下的 TPOT 对比  
  归属：`4.5.1`  
  必填列：模型、`H_{kv}`、`FP16`、`torch_ref(INT4)`、`KIVI(INT4)`、`triton_ra(INT4)`、`FlashInfer(INT4)`、最快项

- **表 4-13**：部署边界综合表  
  归属：`4.5.2–4.5.3`  
  结构：双 panel  
  - Panel A：14B 长序列 TPOT 扩展规律  
  - Panel B：跨模型性能交叉边界  
  **硬纪律**：旧 `8B vs 14B 同 H_{kv}=8` 控制对比不再单独成表，只保留为正文 supporting paragraph。

- **表 4-14**：KV Cache 显存压缩率对比  
  归属：`4.5.4`  
  必填列：模型、`H_{kv}`、`FP16 KV Cache (MB)`、`INT4 KV Cache (MB)`、压缩率、备注  
  **硬纪律**：部署建议不再单独成表，合并进 `4.5.4` 正文结尾。

### 4.6 实验结果讨论与效度威胁

- **表 4-15**：7B 上 KL 与 MSE 校准目标的趋同验证  
  归属：`4.6.1`  
  必填列建议：设置 / path、`KL-selected params`、`MSE-selected params`、参数是否趋同、`KL` 结果、`MSE` 结果、`Δ`  
  **硬纪律**：这是 discussion section 唯一允许的 promoted small table，不承担新的主结果宣称。

---

## 109. LaTeX 回写与填数纪律

### 109.1 最终回写规则

第一，第四章最终编号以本清单为准，即：

- **图：`4-1 ~ 4-6`**
- **表：`4-1 ~ 4-15`**

第二，`4.3.3` 的设计差异表不在第四章重复。  
它的正式归宿固定在第三章 `3.5.4`；第四章只保留跨模型结果表。

### 109.2 回写优先级

推荐按下面顺序回写正文资产：

1. `4.2`：先清掉 `RQ1` 主表污染与 appendix promoted table
2. `4.3`：再回写 low-bit failure → diagnosis → recovery
3. `4.4`：再回写 cross-model regime map 与 case 表
4. `4.5`：最后回写系统表与部署边界

### 109.3 caption / note 负载纪律

- `4.2.2` 与 `4.6.1` 的 promoted table 必须显式声明 **scope limitation**
- `4.4.1` 主表必须显式保留 **same-order INT4 budget band** 的诚实口径
- `4.5` 全节不用图，因此各系统表的 caption / note 必须承担 protocol 负载

### 109.4 `4.5` 系统表 protocol footnote 必填项

`4.5` 的每张正式表至少应在 caption 或 note 中写清以下信息：

- `seq / gen / batch`
- backend 比较范围（如 `torch_ref / triton_ra / KIVI / FlashInfer`）
- hardware = `NVIDIA H20`
- 对 `表 4-14`，必须注明“仅统计 KV Cache，不含模型权重与激活峰值”

### 109.5 旧正文高风险残留提醒

在真正回写 `tex` 前，至少要显式清掉三类旧资产风险：

1. `RQ1` 主表里残留的 `INT4-RoleAlign / KIVI-style` 同权列
2. `RoleAlign` 被旧正文写成统一 KL 搜索 `(p_K,p_V)` 的表述
3. `4.4` allocator 结果被误读成 matched-budget formal compare 的旧语气
