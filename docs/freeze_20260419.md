# Freeze State — 2026-04-19

> 用途：记录 2026-04-19 这轮 L2 + clean-provenance 收口后的本地冻结状态。  
> 这份文档不替代 readout；它只回答：**哪些结果已经在本地、哪些路径受 gitignore 管理、当前冻结覆盖到哪里。**

## 1. Freeze Scope

本轮冻结覆盖：

- `L2 Phase A` (`results/l2_kv_asymmetric/`)
- `L2 Phase B` (`results/l2_pareto/`)
- `L2 Phase C official`（8B × 5 tasks）
- `L2 Phase C off-protocol exploratory`（1.5B / 7B × 5 tasks）
- `clean-provenance` (`docs/results/artifacts/clean_rerun_20260419T09/`)
- 工作台与进度文件：
  - `docs/thesis_upgrade_live_plan.md`
  - `docs/mainline_execution_queue.md`
  - `iteration.md`

## 2. Local Canonical Artifact Map

| Path | Visibility | Count | Notes |
|---|---|---:|---|
| `docs/clean_rerun_20260419T09/` | tracked-path | 5 md | `MANIFEST.md` / `overnight_report_20260419.md` / `readout_phase1.md` / `readout_final.md` / `completion_report_20260419.md` |
| `results/l2_kv_asymmetric/` | gitignored on-disk | 180 files / 108 CSV | L2 Phase A raw + summaries |
| `results/l2_pareto/` | gitignored on-disk | 710 files / 335 CSV / 32 JSON | L2 Phase B v4 raw + Pareto tables/fronts |
| `results/l2_prompt_adaptive/` | gitignored on-disk | 240 files / 135 CSV / 15 JSON | 8B official + 1.5B/7B off-protocol exploratory |
| `results/clean_rerun_20260419T09/raw/` | gitignored on-disk | 462 files / 278 CSV | clean rerun raw |
| `results/clean_rerun_20260419T09/summary_{phase1,final}.csv` | gitignored on-disk | 2 CSV | clean rerun aggregate summaries |
| `artifacts/clean_rerun_20260419T09/` | gitignored on-disk | 73 JSON | 3 regenerated calibrations + 70+ policy JSONs |
| `artifacts/allocator/` | gitignored on-disk | 143 JSON | exploratory / sweep allocator policies synced from remote |

## 3. Protocol Boundary

### 正式协议内（可用于正式判读）

- `L2 Phase A`
- `L2 Phase B`
- `L2 Phase C`: **8B × 5 tasks only**
- `clean-provenance` P0 / P1 / P2 / P3

### 仅 exploratory（不得伪装成正式 completion）

- `L2 Phase C`: `1.5B × 5 tasks`
- `L2 Phase C`: `7B × 5 tasks`

## 4. Frozen Thesis Readout

当前冻结口径如下：

1. 可升级为 `final-ready support` 的 5 条结论：
   - Mistral-specific auto-k win
   - 3B early-layer rescue regime（限 QA tasks）
   - 14B top-tier not winner
   - INT8 canonical path fidelity
   - heuristic is a strong baseline
2. `L2 Phase A/B`：保留为 exploratory positive support / method-extension evidence
3. `L2 Phase C official`：结论固定为 **weak / mixed**，不进入 final claim
4. `1.5B/7B Prompt-adaptive`：仅作 off-protocol exploratory / future-work seed

## 5. Repository Boundary

- `docs/` 下 readout / manifest / freeze / workbench 文件：**应进入 git commit**
- `results/` 与 `artifacts/`：按项目政策保留为 **gitignored on-disk artifacts**
- 本轮冻结的可回溯性依赖于：
  - git commit（tracked docs / scripts / tests / iteration）
  - 本地 `results/` / `artifacts/` 目录
  - `docs/clean_rerun_20260419T09/` 与本文件

## 6. Reproducibility Note

- exploratory worktree base: `codex/phase2-a-rerun`
- clean-provenance pin: `ddada19`
- 远端与本地主要结果目录文件总数在 freeze 前已再次对账
- 本文件对应的是 **2026-04-19 freeze snapshot**
