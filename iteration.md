# 🔄 Project Iteration Log (项目迭代记录)

本文档用于记录在开发过程中发现的**持续性、系统性**问题或改进点。
旨在将临时的痛点（Pain Points）转化为永久性的系统优化（System Improvements）。

## 待处理列表 (Pending)

| ID | 发现日期 | 问题描述 / 改进点 | 触发场景 | 建议解决方案 | 状态 |
|----|----------|-------------------|----------|--------------|------|
| 001 | 2026-01-22 | 示例：远程环境依赖包版本频繁冲突 | 运行 smoke_test.py 时 | 锁定 requirements.txt 具体版本并加入启动检查 | [示例] |
| 002 | 2026-01-22 | 文档/脚本/矩阵与实现口径不一致，复现入口与进度认知漂移 | 以 development_record 对齐代码与矩阵时 | 建立单一入口脚本并强制配置快照；统一 kv_mode 与量化参数透传；加入自动化一致性检查 | 已记录 |
| 003 | 2026-02-08 | PPL 评测先全量拼接 token 导致超长警告与潜在内存浪费，易引发口径误解 | 运行 `scripts/eval_ppl.py` (WikiText-2) 时 | 改为分块/流式 tokenization；或显式设置 `tokenizer.model_max_length` 并记录实际 `max_length/stride`；必要时限制 `max_samples` | 已解决 |
| 004 | 2026-02-08 | 远程验证使用直连 SSH 易受本地超时影响，长任务/下载容易中断 | 远程跑 `eval_ppl.py` 小样本验证时 | 统一改为 tmux 后台 + 日志持久化；必要时显式增加超时时间 | 已记录 |

## 已解决 (Resolved)

## 归档 (Archived)
