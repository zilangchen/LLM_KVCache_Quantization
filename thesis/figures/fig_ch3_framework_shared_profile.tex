\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
    x=1cm, y=1cm,
    >=Stealth,
    every node/.style={font=\rmfamily\small, align=center, text=black},
    arr/.style={-stealth, line width=0.95pt, draw=black!76},
    goal/.style={
      rectangle, rounded corners=3.5pt,
      draw=blue!58!black, line width=0.95pt,
      fill=blue!5,
      text width=11.9cm, minimum height=0.92cm, inner sep=5pt
    },
    profile/.style={
      rectangle, rounded corners=4pt,
      draw=gray!62, line width=0.95pt,
      fill=gray!5,
      text width=8.1cm, minimum height=1.18cm, inner sep=6pt
    },
    lane/.style={
      rectangle, rounded corners=4pt,
      draw=black!64, line width=0.9pt,
      fill=white,
      text width=5.45cm, minimum height=1.48cm, inner sep=6pt
    },
    chipA/.style={
      rectangle, rounded corners=3pt,
      draw=cyan!58!black, line width=0.8pt,
      fill=cyan!8,
      text width=1.80cm, minimum height=0.68cm, inner sep=3pt,
      font=\footnotesize
    },
    chipB/.style={
      rectangle, rounded corners=3pt,
      draw=orange!72!black, line width=0.8pt,
      fill=orange!10,
      text width=1.90cm, minimum height=0.68cm, inner sep=3pt,
      font=\footnotesize
    },
    deploy/.style={
      rectangle, rounded corners=3.5pt,
      draw=black!64, line width=0.9pt,
      fill=gray!7,
      text width=10.9cm, minimum height=0.92cm, inner sep=5pt
    }
  ]

    \node[goal] (goal) at (0,3.75)
      {\textbf{目标：保持量化前后的注意力行为}\\[-1pt]
      \footnotesize 关注分布侧与聚合侧共同诱导的功能偏移，而不是孤立张量距离};

    \node[profile] (profile) at (0,2.05)
      {\textbf{行为敏感度画像 $\mathcal S$}\\[-1pt]
      \footnotesize 来自同一离线校准链路的逐层读数；共享的是来源、接口与选择纪律，不是一个全路径通用的单值评分};

    \node[lane, draw=cyan!58!black, fill=cyan!4] (calib) at (-3.25,0.05)
      {\textbf{校准层：如何量化}\\[-1pt]
      \footnotesize 搜索路径相关参数，生成冻结的离线校准产物};

    \node[lane, draw=orange!72!black, fill=orange!6] (alloc) at (3.25,0.05)
      {\textbf{分配层：哪里优先保护}\\[-1pt]
      \footnotesize 读取同源画像，输出逐层预算与 AutoK 建议};

    \node[chipA] (int8) at (-5.00,-1.48) {INT8\\基准路径};
    \node[chipA] (sym4) at (-3.05,-1.48) {对称 INT4\\试探路径};
    \node[chipA] (ra) at (-1.02,-1.48) {INT4\\RoleAlign};

    \node[chipB] (bak) at (2.35,-1.48) {BA-$k$ /\\Heuristic-$k$};
    \node[chipB] (autok) at (4.72,-1.48) {AutoK};

    \node[deploy] (deploy) at (0,-3.05)
      {\textbf{系统接口：离线产物，在线只读执行}\\[-1pt]
      \footnotesize 第三章只定义接口与实现语义；速度、显存和交叉边界留到第四章验证};

    \draw[arr] (goal.south) -- (profile.north);
    \draw[arr] ([xshift=-1.35cm]profile.south) |- (calib.north);
    \draw[arr] ([xshift=1.35cm]profile.south) |- (alloc.north);

    \draw[arr] (calib.south) -- ++(0,-0.38) -| (int8.north);
    \draw[arr] (calib.south) -- ++(0,-0.38) -| (sym4.north);
    \draw[arr] (calib.south) -- ++(0,-0.38) -| (ra.north);

    \draw[arr] (alloc.south) -- ++(0,-0.38) -| (bak.north);
    \draw[arr] (alloc.south) -- ++(0,-0.38) -| (autok.north);

    \draw[arr] (ra.south) -- ++(0,-0.45) -| ([xshift=-2.0cm]deploy.north);
    \draw[arr] (autok.south) -- ++(0,-0.45) -| ([xshift=2.0cm]deploy.north);
  \end{tikzpicture}
  \caption{行为引导量化框架总览：校准与分配共享同一行为敏感度画像。校准层回答“如何量化”，分配层回答“哪里优先保护”；两者共享离线来源、产物接口与稳健选择纪律，但不要求所有路径使用同一个数值评分函数。}
  \label{fig:ch3-framework-shared-profile}
\end{figure}
