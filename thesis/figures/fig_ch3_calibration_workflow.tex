\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
    x=1cm, y=1cm,
    >=Stealth,
    every node/.style={font=\rmfamily\small, align=center, text=black},
    arr/.style={-stealth, line width=0.95pt, draw=black!76},
    stage/.style={
      rectangle, rounded corners=3.5pt,
      draw=black!62, line width=0.9pt,
      fill=white,
      text width=2.75cm, minimum height=1.12cm, inner sep=5pt
    },
    ref/.style={stage, draw=blue!58!black, fill=blue!5},
    cand/.style={stage, draw=orange!72!black, fill=orange!8, text width=3.05cm},
    select/.style={stage, draw=red!58!black, fill=red!5, text width=2.95cm},
    outcard/.style={stage, draw=cyan!58!black, fill=cyan!7, text width=3.00cm},
    role/.style={
      rectangle, rounded corners=3pt,
      draw=black!55, line width=0.75pt,
      fill=gray!4,
      text width=2.25cm, minimum height=0.72cm, inner sep=3pt,
      font=\footnotesize
    },
    note/.style={
      rectangle, rounded corners=3.5pt,
      draw=black!45, line width=0.8pt,
      fill=gray!6,
      text width=11.5cm, minimum height=0.78cm, inner sep=5pt,
      font=\footnotesize
    }
  ]

    \node[font=\bfseries\large] at (0,3.25) {行为引导离线校准工作流};

    \node[ref] (data) at (-4.95,1.65)
      {\textbf{校准样本}\\FP16 前向记录\\attention distribution\\与输出响应};

    \node[cand] (paths) at (-1.55,1.65)
      {\textbf{路径候选}\\INT8 基准路径\\对称 INT4 试探路径\\INT4-RoleAlign};

    \node[select] (select) at (1.95,1.65)
      {\textbf{稳健选择}\\可行域约束\\尾部坏例优先\\裁剪率检查};

    \node[outcard] (artifact) at (5.25,1.65)
      {\textbf{离线输出}\\路径特定产物\\共享行为读数\\在线只读接口};

    \draw[arr] (data.east) -- (paths.west);
    \draw[arr] (paths.east) -- (select.west);
    \draw[arr] (select.east) -- (artifact.west);

    \node[role] (kpath) at (-2.78,-0.48)
      {K-path\\attention-distribution KL};
    \node[role] (vpath) at (-0.28,-0.48)
      {V-path\\输出扰动代理};
    \draw[arr, draw=orange!72!black] ([xshift=-0.70cm]paths.south) -- (kpath.north);
    \draw[arr, draw=orange!72!black] ([xshift=0.70cm]paths.south) -- (vpath.north);

    \node[note] at (0,-1.83)
      {统一的是离线流程、产物接口与稳健选择纪律；不同路径可以使用不同参数家族与行为代理，RoleAlign 不被写成单一 KL 分数统一选择 $(p_K,p_V)$。};

  \end{tikzpicture}
  \caption{行为引导离线校准的统一工作流。输入侧提取 FP16 参考行为，候选侧按路径组织参数空间，选择侧执行可行域约束与尾部稳健优先，输出侧冻结为路径特定校准产物与共享行为读数。对于 INT4-RoleAlign，Key 路径以 attention-distribution KL 为主代理，Value 路径以输出扰动代理为主代理。}
  \label{fig:ch3-calibration-workflow}
\end{figure}
