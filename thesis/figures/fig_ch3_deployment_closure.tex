\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
    every node/.style={font=\small, align=center},
    stage/.style={rectangle, rounded corners=4pt, draw=blue!55!black, thick,
                  fill=blue!8, minimum width=2.9cm, minimum height=2.4cm},
    artifact/.style={rectangle, rounded corners=4pt, draw=purple!60!black, very thick,
                     fill=purple!10, minimum width=3.6cm, minimum height=3.0cm},
    pipe/.style={rectangle, rounded corners=3pt, draw=green!45!black, thick,
                 fill=green!10, minimum width=2.6cm, minimum height=1.0cm},
    note/.style={rectangle, rounded corners=3pt, draw=gray!70, thick,
                 fill=gray!10, minimum width=8.2cm, minimum height=0.9cm},
    arr/.style={-stealth, thick}
  ]
    \node[stage] (offline) at (-5.1,1.0) {\textbf{离线校准阶段}\\[2pt]
      校准样本\\
      行为引导搜索\\
      INT8 / 对称 INT4 /\\
      \texttt{INT4-RoleAlign}};
    \node[artifact] (art) at (0,1.0) {\textbf{校准产物}\\[3pt]
      scale / zero-point / percentile\\
      group size / axis metadata\\
      protection margin / mode id\\[4pt]
      \footnotesize Read-only at runtime};
    \node[pipe] (prefill) at (5.1,1.9) {Prefill\\量化写入缓存};
    \node[pipe] (decode) at (5.1,0.1) {Decode\\读取量化缓存并执行后端};
    \node[note] (freeze) at (0,-1.5) {\textbf{写入即冻结；自适应保护仅作用于当前 token，不回写历史缓存}};

    \draw[arr] (offline.east) -- (art.west);
    \draw[arr] (art.east) -- (prefill.west);
    \draw[arr] (art.east) -- (decode.west);
    \draw[arr] (prefill.south) |- (freeze.east);
    \draw[arr] (decode.south) |- (freeze.east);
  \end{tikzpicture}
  \caption{离线校准产物到在线推理执行的部署闭环。图中强调运行时读取的是固定的离线校准产物，在线阶段只执行既定量化规则与后端路由；缓存的核心语义是“append 时确定、历史不回写”。}
  \label{fig:ch3-deployment-closure}
\end{figure}
