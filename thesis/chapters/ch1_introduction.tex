% ============================================================
% ch1_introduction.tex — 第一章 绪论
% ============================================================
\chapter{绪论}

\section{研究背景}

随着大语言模型（Large Language Model, LLM）的参数规模从数十亿增长至数千亿、
上下文窗口从 4K 扩展至 128K tokens，
GPT-4、LLaMA~3~\cite{grattafiori2024llama3}、Qwen2.5~\cite{qwen2025qwen25} 等模型
在文本生成、阅读理解和逻辑推理等任务上展现出卓越能力，
但推理阶段的显存与延迟开销也随之急剧增长，成为制约大规模部署的核心瓶颈。

现代大语言模型普遍采用 Transformer~\cite{vaswani2017attention} 架构，
其自回归解码（autoregressive decoding）过程中，每生成一个新的 token 都需要与所有历史 token 的键值对（Key-Value pair）进行注意力计算。
为避免重复计算，推理系统会将已计算的键值对缓存起来，形成键值缓存（KV Cache）。
KV Cache 机制是自回归解码的核心加速手段，能够将单步解码的时间复杂度从 $O(n^2)$ 降低至 $O(n)$，
但其代价是显存占用随序列长度线性增长。

具体而言，对于一个具有 $L$ 层 Transformer、每层 $h_{KV}$ 个键值头（KV head）、头维度为 $d_h$ 的模型，
在序列长度为 $s$ 时，KV Cache 的显存占用可表示为：
\begin{equation}
  M_{\text{KV}} = 2 \times L \times h_{KV} \times d_h \times s \times b
  \label{eq:kv_memory}
\end{equation}
其中，因子 $2$ 对应键（Key）和值（Value）两个矩阵，$b$ 为每个元素占用的字节数（FP16 下 $b=2$）。

以 Qwen2.5-7B-Instruct 模型（28 层，GQA~\cite{ainslie2023gqa}，4 个 KV 头，头维度 128）为例，
在 FP16 精度、32K 上下文下，单个请求的 KV Cache 即占用约 1.75~GB 显存。
在批量推理场景下，KV Cache 的总显存消耗将随批大小成倍增长，可轻易超过模型参数本身的显存占用。
vLLM~\cite{kwon2023vllm} 等推理框架通过 PagedAttention 技术改善了显存碎片问题，但并未从根本上减少 KV Cache 的绝对存储量。

量化（quantization）技术是降低 KV Cache 显存占用的重要手段。
通过将 FP16 精度的键值对压缩为 INT8 甚至 INT4 表示，可以将 KV Cache 的显存分别降至原始的 $1/2$ 和 $1/4$，
从而在有限的 GPU 显存中支持更长的上下文或更大的并发批次。

\section{问题定义与研究动机}

在上述背景下，低比特（INT4）量化是最具吸引力的压缩目标——
理论上可将 KV Cache 压缩至 FP16 的 $1/4$，
在相同硬件预算下支持 4 倍的上下文长度或并发批次。
然而，现有的 INT4 量化方法在长上下文场景下表现急剧恶化：
Needle 检索通过率可从 100\% 降至 0\%，困惑度退化超过 15\%——
这意味着对称 INT4 量化的模型在长上下文下
不仅无法完成精确检索任务，其语言建模能力也发生灾难性崩溃。
更重要的是，传统校准目标（MSE、percentile）\textbf{无法解释}这种失效——
即使数值重建误差看似合理，模型的注意力行为仍可能完全崩溃。
这一矛盾揭示了当前校准范式的根本局限：
仅优化数值误差不足以保障量化后的模型行为，
需要一种新的校准原则，能够\textbf{同时}指导量化参数搜索和诊断失效模式。

现有的 KV Cache 量化方法在校准策略（calibration strategy）的选择上，
通常采用基于均方误差（Mean Squared Error, MSE）或百分位裁剪（percentile clipping）的方案。
MSE 校准通过最小化量化前后张量的逐元素数值误差来确定量化参数；
百分位裁剪则通过截断分布尾部的异常值来缩小量化范围，以降低大多数数值的量化误差。

然而，上述方法存在本质局限性。MSE 校准优化的是数值层面的重建误差，
但数值误差的最小化并不等同于模型行为的保真。
在注意力机制中，键和值经过缩放点积（scaled dot-product）运算后通过 softmax 函数映射为概率分布。
softmax 函数的非线性特性意味着，即使键矩阵中微小的数值偏差，
也可能在某些注意力模式下被放大，导致注意力权重分布发生显著改变；
反之，某些看似较大的数值误差可能因 softmax 的归一化效应而对最终分布影响甚微。
百分位裁剪策略同样面临问题：其裁剪阈值的选择缺乏与下游注意力行为的关联，
对不同层、不同头的特征分布差异缺乏自适应能力。

本文的核心洞察在于：KV Cache 量化的最终目标不是最小化存储表示的数值误差，
而是保持量化前后注意力行为（attention behavior）的一致性。
具体而言，若将 FP16 精度下的注意力权重分布记为 $\bm{p}$，
量化后的注意力权重分布记为 $\bm{q}$，
则量化质量应当以两者之间的分布差异来衡量：
\begin{equation}
  D_{\KL}(\bm{p} \| \bm{q}) = \sum_{i} p_i \log \frac{p_i}{q_i}
  \label{eq:kl_divergence}
\end{equation}
其中，$D_{\KL}$ 为 Kullback-Leibler 散度，衡量量化引入的注意力行为偏移程度。

基于上述洞察，本文提出以注意力权重分布的 KL 散度作为校准目标函数，
直接优化量化参数使得注意力行为尽可能保持不变。
这一"行为对齐"（Behavior Alignment）的思路将校准过程从数值优化层面提升到语义保真层面，
为 KV Cache 量化提供了新的理论视角和技术路径。

\section{国内外研究现状}

KV Cache 量化的研究建立在模型量化和高效注意力计算两大技术方向之上，
以下从三个方面简述相关研究现状，更详细的分析将在第二章展开。

\subsection{模型量化}

模型量化通过降低数值精度来压缩模型体积、加速推理。
代表性工作包括面向权重-激活联合量化的 SmoothQuant~\cite{xiao2023smoothquant}、
基于近似二阶信息的训练后量化方法 GPTQ~\cite{frantar2023gptq}、
以及激活感知权重量化策略 AWQ~\cite{lin2024awq}。
上述方法主要面向模型权重量化，与本文关注的 KV Cache 量化在目标和技术路径上有所不同，
但其量化理论和误差分析方法为本文提供了重要参考。
更详细的技术分析见第二章。

\subsection{KV Cache 量化}

KV Cache 量化是近年来的研究热点。
KIVI~\cite{liu2024kivi} 揭示了 Key 和 Value 在量化敏感性上的不对称特征，
提出逐通道 Key + 逐 token Value 的非对称量化策略；
KVQuant~\cite{hooper2024kvquant} 探索了面向极低比特的非均匀量化方案；
ZipCache~\cite{he2024zipcache} 则基于 token 重要性实现了混合精度 KV Cache 压缩。
上述方法在量化策略和压缩技术上取得了重要进展，
但其校准仍依赖于数值误差指标，未从注意力行为保真的角度建立校准目标；
K/V 各自的量化敏感性边界及其与模型架构的关系也缺乏系统性消融验证。
更详细的工作对比见第二章。

\subsection{高效注意力计算}

FlashAttention~\cite{dao2022flashattention,dao2024flashattention2} 通过分块计算与内核融合
显著降低了注意力计算的访存开销，但其主要面向 FP16 精度的标准注意力，
尚未充分支持量化 KV Cache 的在线反量化融合。
本文基于 Triton~\cite{tillet2019triton} 实现了 INT8/INT4 融合量化解码注意力核函数，
将反量化与注意力计算整合在单个 GPU 核函数中。
更详细的技术讨论见第二章和第三章。

\section{本文研究内容与主要贡献}

本文的核心贡献在于提出一个统一原则
并将其推进至低比特设计：
KV Cache 量化的关键不是最小化数值重建误差，
而是保持注意力行为。

\textbf{贡献一：统一原则。}
提出 attention-KL 同时作为校准目标与诊断透镜。
作为校准目标，它在离线阶段搜索使注意力行为偏移
最小化的量化参数；作为诊断透镜，它揭示不同量化
配置下的失效模式，为精度分配提供结构性依据。

\textbf{贡献二：INT8 规范验证实例。}
在该原则下给出 INT8 的 canonical validated instance，
在三个模型族上实现 44\% KV 显存降低与 8--38\% 延迟降低，
证明行为对齐校准的有效性，并为低比特诊断建立参照基线。

\textbf{贡献三：低比特失效诊断。}
同一诊断透镜揭示对称 INT4 的系统性失效并非偶然：
受控 K/V 消融在三个模型族上一致确认 Key 量化为退化
主导因素，且呈显著的 GQA 架构依赖特征。
该发现为精度分配提供了定量依据。

\textbf{贡献四（核心方法贡献）：INT4-RoleAlign。}
上述诊断直接导出角色感知的低比特设计——
INT4-RoleAlign 采用 per-channel Key + per-token Value
非对称量化轴与行为对齐 percentile 校准。
在 Qwen2.5-1.5B、Qwen2.5-7B、LLaMA-3.1-8B 三个模型上
Needle 从 0\%$\to$100\%，
PPL 退化从 KIVI-style 的 2.4\%--12\%$\to$0.3\%--1.2\%，
在 75\% KV Cache 压缩率下实现上述质量保持。
INT4-RoleAlign 完成``原则$\to$诊断$\to$设计$\to$验证''的闭环，
构成本文的核心方法贡献。

\textbf{系统支撑。}
设计 Triton 融合量化解码核函数为上述框架
提供高效系统支撑，采用 Bootstrap CI + BH-FDR
统计框架确保结论的统计显著性。

本文的核心论点是：KV Cache 量化的关键不是最小化数值重建误差，
而是保持注意力行为。Attention-KL 作为统一的校准与诊断原则，
首先通过 INT8 规范验证实例证明其有效性，
进而揭示低比特退化的 Key 主导根因，
最终导出本文的核心方法贡献——INT4-RoleAlign，
以 75\% KV Cache 压缩率实现三模型 Needle 100\%、PPL 退化 $<$1.2\%。
图~\ref{fig:ch1-pipeline} 展示了本文的论证结构。

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
    stage/.style={rectangle, draw, rounded corners=3pt, minimum width=6.0cm,
                  minimum height=1.0cm, text centered, font=\small, text width=5.8cm},
    star/.style={stage, fill=onlineB, line width=1.2pt, draw=blue!60},
    arr/.style={-{Stealth[length=3mm]}, semithick, gray!70},
  ]
  \node[stage, fill=red!8, draw=red!40]
    (problem) at (0, 0)
    {\textbf{数值校准目标失配}\\
     \scriptsize 对称 INT4 Needle 0\%，传统校准无法解释};

  \node[stage, fill=offlineB]
    (principle) at (0, -2.0)
    {\textbf{attention-KL 统一原则}\\
     \scriptsize 校准目标 + 诊断透镜};

  \node[stage, fill=artifact]
    (int8) at (0, -4.0)
    {\textbf{INT8 规范验证实例}\\
     \scriptsize canonical validation · 44\% 显存降低};

  \node[stage, fill=onlineA]
    (diagnosis) at (0, -6.0)
    {\textbf{低比特诊断}\\
     \scriptsize Key 主导退化 + GQA 架构依赖};

  \node[star]
    (rolealign) at (0, -8.2)
    {\textbf{$\bigstar$ INT4-RoleAlign（核心方法贡献）}\\
     \scriptsize 75\% 压缩 $|$ Needle 0\%$\to$100\% $|$ PPL $<$1.2\%};

  \draw[arr] (problem) -- (principle);
  \draw[arr] (principle) -- (int8);
  \draw[arr] (int8) -- (diagnosis);
  \draw[arr] (diagnosis) -- (rolealign);
  \end{tikzpicture}
  \caption{本文论证层级：统一原则$\to$规范验证$\to$诊断$\to$INT4-RoleAlign 核心贡献}
  \label{fig:ch1-pipeline}
\end{figure}

\section{论文组织结构}

本文共分五章。
第一章阐述研究背景与动机，综述国内外研究现状，介绍研究内容与主要贡献。
第二章介绍 Transformer 注意力机制、量化理论基础及 KV Cache 量化相关工作。
第三章详述行为对齐量化框架的两级设计：
INT8 对称路径（KL 校准 + 自适应保护）
和 INT4-RoleAlign 核心设计（角色感知非对称量化），
以及 Triton 融合核函数的系统实现。
第四章按三个核心 claims 组织实验证据——
行为对齐校准的首次验证（INT8）、
低比特失效的结构性诊断（Key 主导 + GQA 依赖）、
以及诊断导出的 INT4-RoleAlign 设计验证，
最后进行效率验证与综合讨论。
第五章总结全文核心观察，回扣引言问题，分析局限性并展望未来方向。
