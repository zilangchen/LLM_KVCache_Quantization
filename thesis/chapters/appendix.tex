% ============================================================
% appendix.tex — 附录
% ============================================================

\chapter{补充材料}

% ============================================================
\section{PPL 评测协议与基线汇总}
\label{sec:app-fp16-protocol}% 附录 P1 (story §16.18)
\label{sec:app-ppl-baseline-matrix}
% ============================================================

本文 PPL 评测在不同章节使用了三种略有差异的协议，
表~\ref{tab:app-ppl-baseline-matrix} 统一列出各模型 FP16 PPL 基线与对应协议，
供读者在交叉比较时参照。
同一模型同一精度在不同协议下可能出现 $\sim$0.1--0.5 PPL 差异，
各表的\emph{绝对}数字不可直接跨表比较；
各退化率（相对同表内 FP16 基线）方可在表内对比。

\begin{table}[!htbp]
  \centering
  \caption{FP16 PPL 基线 $\times$ 评测协议汇总。}
  \label{tab:app-ppl-baseline-matrix}
  \small
  \begin{threeparttable}
  \begin{tabular}{llccl}
    \toprule
    模型 & 协议 & \code{max\_length} / stride & \code{chunk\_size} & FP16 PPL \\
    \midrule
    Qwen2.5-1.5B & 32K 上下文短窗   & 1024 / 512 & 128 & 8.93 \\
    Qwen2.5-1.5B & 完整 test split  & 32K / 32K  & 128 & 9.31 \\
    Qwen2.5-7B   & 32K 上下文短窗   & 1024 / 512 & 128 & 6.71 \\
    Qwen2.5-7B   & 完整 test split  & 32K / 32K  & 128 & 7.14 \\
    Qwen2.5-7B   & 附录 KL/MSE 对比 & 同前       & 128 & 6.7097\tnote{$\dagger$} \\
    LLaMA-3.1-8B & 32K 上下文短窗   & 1024 / 512 & 128 & 6.92 \\
    LLaMA-3.1-8B & 完整 test split  & 32K / 32K  & 128 & 6.73 \\
    Qwen2.5-14B  & 32K prefix       & 32K / ---  & 128 & 4.685\tnote{$\star$} \\
    \bottomrule
  \end{tabular}
  \begin{tablenotes}
  \footnotesize
  \item[$\dagger$] 附录表~\ref{tab:app-7b-kl-mse} 的 7B FP16 baseline PPL 精确到四位小数为 $6.7097$，
    与本表第四行的 $7.14$（完整 test split）出自同一评测协议但不同脚本输出；
    该表对比 INT4 KL/MSE 校准在 7B 上产生的\emph{逐位一致} PPL（$7.1121$，属 INT4 侧观察，非 FP16 基线）。
  \item[$\star$] 14B 因 H20 显存限制仅在 test split 前 32767 tokens 上评测，
    与其他模型的完整 $\sim$302K test split 不可直接比较。
  \end{tablenotes}
  \end{threeparttable}
\end{table}

\noindent
注：表~\ref{tab:t3-cross-model-main} 与~\ref{sec:app-kv-ablation-full} 使用 32K 上下文短窗协议；
表~\ref{tab:t2-int4-kivi} 使用完整 test split。

% ============================================================
\section{实验环境详情}
\label{sec:app-env}% 附录 P2 (story §16.18)
% ============================================================

表~\ref{tab:app-env} 列出了本文所有实验所使用的软硬件环境配置。

\begin{table}[!htbp]
  \centering
  \caption{实验软硬件环境}
  \label{tab:app-env}
  \begin{tabular}{ll}
    \toprule
    \textbf{项目} & \textbf{配置} \\
    \midrule
    GPU             & NVIDIA H20, 98\,GB HBM \\
    CPU             & Intel Xeon (AutoDL Cloud) \\
    操作系统        & Ubuntu 22.04 LTS \\
    Python          & 3.12 \\
    PyTorch         & 2.8.0 (CUDA 12.8) \\
    Transformers    & 4.48+ \\
    Triton          & 3.2+ \\
    NumPy           & 2.x \\
    pandas          & 2.x \\
    Matplotlib      & 3.x \\
    \bottomrule
  \end{tabular}
\end{table}

所有实验均采用确定性设置：固定随机种子（主实验 1234--1238，吞吐实验 1234--1241），贪心解码策略（\code{temperature=0.0}, \code{top\_p=1.0}, \code{top\_k=0}），以确保结果的可复现性。

% ============================================================
\section{复现脚本与覆盖范围}
\label{sec:app-reproduce}
% ============================================================

本文主线实验的冻结复现入口位于仓库根目录 \code{REPRODUCE.md}，
对应 10 个编号脚本覆盖正文第~\ref{sec:exp-main}--\ref{sec:exp-deployment}~节
的 C1 校准依赖、C2 诊断与实例化（含 14B 外部效度）、
C3 融合核效率，以及附录 \ref{subsec:app-7b-kl-mse} 的 7B KL=MSE 趋同溯源。
BitDecoding 工程案例与官方 LongBench v2 数据保留于仓库用于审计，
但不在主线复现保证范围内，详见 \code{REPRODUCE.md}。

关于校准样本数量：正文引用 $N=128$ 为标准规模，复现脚本实际使用
$N\in\{16, 32\}$。B10 校准样本数量敏感性消融
（表~\ref{sec:app-fp16-protocol}，覆盖 $s\in\{16, 64, 256\}$）确认
PPL/Needle/RULER/LongBench 四类指标均已进入平坦收敛域，
两个实参指向同一校准参数配置。

% ============================================================
\section{校准产物格式}
\label{sec:app-calib-schema}
% ============================================================

离线校准阶段产出一个 JSON 文件（$<$10~KB），
包含逐层 Key/Value 的 float32 scale、
逐头温度校正因子 $\tau^{-1}$，
以及非对称模式下的 zero point。
推理时一次性加载到 GPU 显存，不引入运行时校准开销。

% ============================================================
\section{校准超参数搜索空间}
\label{sec:app-calib-search-space}
% ============================================================

表~\ref{tab:app-search-space} 列出了校准过程中各超参数的候选值集合。
INT8 路径在 $\mathcal{P}_c \times \mathcal{G}$ 的笛卡尔积上进行两阶段网格搜索（第~\ref{subsec:ch3-two-stage}~节），
INT4-RoleAlign 在 $\mathcal{P}_K \times \mathcal{P}_V$ 上搜索最优 percentile 组合（第~\ref{sec:ch3-rolealign} 节）。

\begin{table}[!htbp]
  \centering
  \caption{校准超参数搜索空间与对称量化数据结构}
  \label{tab:app-search-space}
  \small
  \begin{tabular}{lll}
    \toprule
    超参数 & 符号 & 候选值集合 / 默认配置 \\
    \midrule
    \multicolumn{3}{l}{\textit{INT8 / INT4 对称量化数据结构}} \\
    位宽 & $n$ & INT8: 8 / INT4: 4 \\
    量化范围 & $[-q_{\max}, q_{\max}]$ & INT8: $[-127, 127]$ / INT4: $[-7, 7]$ \\
    Scale 精度 & --- & float32（与输入 dtype 解耦） \\
    Scale 类型 & --- & 对称（无 zero-point） \\
    Scale 形状 & --- & $[B, H, S, D/g]$ \\
    量化轴 & --- & per-group（沿 $d_k$ 分组） \\
    \midrule
    \multicolumn{3}{l}{\textit{INT8 对称量化路径（校准搜索）}} \\
    Clip percentile & $\mathcal{P}_c$ & $\{99.0,\; 99.5,\; 99.9,\; 100.0\}$（默认 99.5） \\
    Group size & $\mathcal{G}$ & $\{16,\; 32,\; 64,\; 128\}$（默认 16） \\
    Outlier rescue ratio & --- & $\{0,\; 0.0025,\; 0.005,\; 0.01\}$ \\
    温度因子 & $\mathcal{T}$ & $\{0.5,\; 0.7,\; 0.85,\; 1.0,\; 1.2,\; 1.5,\; 2.0\}$ \\
    \midrule
    \multicolumn{3}{l}{\textit{INT4-RoleAlign 非对称量化路径}} \\
    K percentile & $\mathcal{P}_K$ & $\{95.0,\; 97.0,\; 99.0,\; 99.5,\; 99.9,\; 100.0\}$ \\
    V percentile & $\mathcal{P}_V$ & $\{95.0,\; 97.0,\; 99.0,\; 99.5,\; 99.9,\; 100.0\}$ \\
    \midrule
    \multicolumn{3}{l}{\textit{共用参数}} \\
    校准样本数 & $N$ & 128（默认；B10 消融验证 16--256 不敏感） \\
    校准序列长度 & --- & 512 tokens \\
    校准数据源 & --- & WikiText-2 (test split) \\
    \bottomrule
  \end{tabular}
\end{table}

INT8 路径的第一阶段搜索遍历 $|\mathcal{P}_c| \times |\mathcal{G}| = 16$ 组参数组合，
第二阶段在固定最优 $(p_c^*, g^*)$ 后遍历 $|\mathcal{T}| = 7$ 个温度因子候选，
总搜索规模为 23 组。
INT4-RoleAlign 路径遍历 $|\mathcal{P}_K| \times |\mathcal{P}_V| = 36$ 组 percentile 组合。

% ============================================================
\section{完整 LongBench 结果}
\label{sec:app-longbench-full}
% ============================================================

表~\ref{tab:app-longbench-full} 列出了三种模型在 32K 上下文长度下、七种量化模式的
LongBench 聚合结果，包含 F1 宏平均、包含匹配宏平均以及综合评分。
数值后的``$\pm$''表示 Bootstrap 95\% 置信区间半宽。
INT8-Canonical 在所有模型上均与 FP16 基线保持可比的质量，
而 INT4 方案（尤其在 1.5B 和 7B 上）出现显著退化。

\begin{table}[!htbp]
  \centering
  \caption{LongBench 完整结果（32K 上下文，三模型 $\times$ 七量化模式）}
  \label{tab:app-longbench-full}
  \zihao{5}
  \begin{tabular}{llrrr}
    \toprule
    \textbf{模型} & \textbf{量化模式} & \textbf{F1 宏平均} & \textbf{包含匹配率(\%)} & \textbf{综合评分} \\
    \midrule
    \multirow{7}{*}{1.5B}
      & fp16            & $5.53 \pm 0.05$ & $99.9 \pm 0.2$ & $0.0482 \pm 0.0004$ \\
      & int8\_baseline  & $5.50 \pm 0.02$ & $99.9 \pm 0.2$ & $0.0478 \pm 0.0002$ \\
      & int8\_ours      & $5.54 \pm 0.04$ & $99.9 \pm 0.1$ & $0.0482 \pm 0.0004$ \\
      & int4\_baseline  & $2.91 \pm 0.26$ & $45.9 \pm 2.0$ & $0.0298 \pm 0.0024$ \\
      & int4\_fused     & $0.00 \pm 0.00$ & $0.0 \pm 0.0$  & $0.0515 \pm 0.0068$ \\
      & int4\_ours      & $0.00 \pm 0.00$ & $0.0 \pm 0.0$  & $0.0241 \pm 0.0025$ \\
      & kivi\_style     & $5.58 \pm 0.07$ & $99.9 \pm 0.1$ & $0.0481 \pm 0.0005$ \\
    \midrule
    \multirow{7}{*}{7B}
      & fp16            & $4.29 \pm 0.15$ & $100.0 \pm 0.0$ & $0.0378 \pm 0.0008$ \\
      & int8\_baseline  & $4.38 \pm 0.14$ & $100.0 \pm 0.0$ & $0.0383 \pm 0.0007$ \\
      & int8\_ours      & $4.44 \pm 0.14$ & $100.0 \pm 0.0$ & $0.0383 \pm 0.0009$ \\
      & int4\_baseline  & $0.00 \pm 0.00$ & $0.0 \pm 0.0$   & $0.0172 \pm 0.0024$ \\
      & int4\_fused     & $0.45 \pm 0.13$ & $4.2 \pm 0.9$   & $0.0183 \pm 0.0020$ \\
      & int4\_ours      & $0.00 \pm 0.01$ & $0.4 \pm 0.5$   & $0.0027 \pm 0.0011$ \\
      & kivi\_style     & $4.28 \pm 0.14$ & $99.9 \pm 0.1$  & $0.0378 \pm 0.0011$ \\
    \midrule
    \multirow{7}{*}{8B}
      & fp16            & $10.29 \pm 0.76$ & $99.7 \pm 0.5$ & $0.0792 \pm 0.0066$ \\
      & int8\_baseline  & $10.71 \pm 0.85$ & $99.7 \pm 0.5$ & $0.0817 \pm 0.0092$ \\
      & int8\_ours      & $10.72 \pm 0.85$ & $99.6 \pm 0.7$ & $0.0817 \pm 0.0092$ \\
      & int4\_baseline  & $12.95 \pm 1.02$ & $99.7 \pm 0.5$ & $0.1037 \pm 0.0087$ \\
      & int4\_fused     & $15.89 \pm 1.37$ & $99.8 \pm 0.3$ & $0.1505 \pm 0.0138$ \\
      & int4\_ours      & $19.88 \pm 1.34$ & $99.4 \pm 0.5$ & $0.1908 \pm 0.0111$ \\
      & kivi\_style     & $8.72 \pm 0.98$  & $99.6 \pm 0.3$ & $0.0682 \pm 0.0069$ \\
    \bottomrule
  \end{tabular}
  \begin{tablenotes}
    \footnotesize
    \item 注：1.5B = Qwen2.5-1.5B-Instruct，
    7B = Qwen2.5-7B-Instruct，
    8B = LLaMA-3.1-8B-Instruct。
    合成 LongBench（7 类子任务），5 seeds 均值。
    8B INT4 的较高 F1 可能与模型固有能力差异有关。
  \end{tablenotes}
\end{table}

% ============================================================
\section{种子级统计明细}
\label{sec:app-seed-detail}
% ============================================================

表~\ref{tab:app-sig-quality} 合并列出了 INT8-Canonical 相对于 INT8-Baseline
在质量指标（PPL/Needle/LongBench/RULER，$n=5$ 种子）与推理延迟
（TPOT，$n=8$ 种子）上的统计检验结果。
所有检验均基于配对种子的 sign-flip 置换检验，
多重比较校正采用 Benjamini-Hochberg FDR 控制（$\alpha=0.05$）。

\begin{table}[!htbp]
  \centering
  \caption{INT8-Canonical vs INT8-Baseline 质量与延迟统计检验（32K 上下文）}
  \label{tab:app-sig-quality}
  \zihao{5}
  \begin{tabular}{llrrrrrr}
    \toprule
    \textbf{模型} & \textbf{指标} & \textbf{基线均值} & \textbf{本文均值} & \textbf{增益(\%)} & \textbf{$p$值} & \textbf{$q$值} & \textbf{显著} \\
    \midrule
    \multicolumn{8}{l}{\textit{质量指标（$n=5$ 种子 1234--1238）}} \\
    \midrule
    \multirow{4}{*}{1.5B}
      & perplexity$\downarrow$      & 8.9316 & 8.9497 & $-0.20$ & 0.0909 & 0.0974 & 否 \\
      & needle\_pass\_rate$\uparrow$ & 100.00 & 100.00 & $+0.00$ & 1.0000 & 1.0000 & 否 \\
      & longbench\_score$\uparrow$  & 0.0478 & 0.0486 & $+1.77$ & 0.0909 & 0.1435 & 否 \\
      & ruler\_pass\_rate$\uparrow$ & 24.45  & 23.81  & $-2.64$ & 0.0909 & 0.7273 & 否 \\
    \midrule
    \multirow{4}{*}{7B}
      & perplexity$\downarrow$      & 6.7145 & 6.7322 & $-0.26$ & 0.0909 & 0.0974 & 否 \\
      & needle\_pass\_rate$\uparrow$ & 100.00 & 100.00 & $+0.00$ & 1.0000 & 1.0000 & 否 \\
      & longbench\_score$\uparrow$  & 0.0383 & 0.0383 & $+0.17$ & 0.9394 & 0.9553 & 否 \\
      & ruler\_pass\_rate$\uparrow$ & 24.92  & 24.84  & $-0.31$ & 1.0000 & 1.0000 & 否 \\
    \midrule
    \multirow{4}{*}{8B}
      & perplexity$\downarrow$      & 6.9193 & 6.9252 & $-0.09$ & 0.0909 & 0.0974 & 否 \\
      & needle\_pass\_rate$\uparrow$ & 100.00 & 100.00 & $+0.00$ & 1.0000 & 1.0000 & 否 \\
      & longbench\_score$\uparrow$  & 0.0817 & 0.0817 & $+0.09$ & 0.8788 & 0.9416 & 否 \\
      & ruler\_pass\_rate$\uparrow$ & 32.58  & 31.25  & $-4.05$ & 0.0909 & 0.7273 & 否 \\
    \midrule
    \multicolumn{8}{l}{\textit{解码延迟 TPOT（$n=8$ 种子 1234--1241，batch=1，gen\_len=64）}} \\
    \midrule
    1.5B & TPOT (ms)$\downarrow$ & 51.42 & 47.14 & $+8.32$  & 0.0117 & 0.0159 & \textbf{是} \\
    7B   & TPOT (ms)$\downarrow$ & 60.52 & 50.07 & $+17.26$ & 0.0117 & 0.0159 & \textbf{是} \\
    8B   & TPOT (ms)$\downarrow$ & 91.77 & 57.23 & $+37.64$ & 0.0117 & 0.0159 & \textbf{是} \\
    \bottomrule
  \end{tabular}

  \vspace{2pt}
  {\footnotesize 注：质量指标的差异均未达到 BH-FDR 校正后的统计显著性；三种模型上的 TPOT 降低均达到显著性（$q < 0.05$），且加速幅度随模型规模增大而增大，与更大的 KV Cache 使融合核函数的带宽节省效果更显著一致。1.5B Needle 行报告 mainline 配置（5 种子均 100\%）；LongBench/RULER 的 INT8-Canonical 值为所有配置变体（mainline + ablation）的混合均值，RULER 退化（$-$2.64\%）主要受非 mainline 配置拉低。}
\end{table}

% ============================================================
\section{质量评测补充图表}
\label{sec:app-quality-plots}
% ============================================================

本节提供主线质量评测指标的可视化补充。
图~\ref{fig:app-ruler-vs-context} 展示了 RULER 通过率随上下文长度的变化趋势，
图~\ref{fig:app-longbench-vs-context} 展示了 LongBench-style 合成综合评分的对应趋势。

\begin{figure}[!htbp]
  \centering
  \subcaptionbox{RULER 通过率\label{fig:app-ruler-vs-context}}
    [0.48\textwidth]{\includegraphics[width=0.48\textwidth]{figures/ruler_pass_rate_vs_context.pdf}}
  \hfill
  \subcaptionbox{LongBench 综合评分\label{fig:app-longbench-vs-context}}
    [0.48\textwidth]{\includegraphics[width=0.48\textwidth]{figures/longbench_score_vs_context.pdf}}
  \caption{补充质量指标随上下文长度变化趋势（Qwen2.5-1.5B-Instruct）。
  （a）RULER 通过率，含 S-NIAH、MK-NIAH、VT 和 CWE 四子任务聚合；
  （b）LongBench-style 综合评分，为 7 类合成任务宏平均。}
  \label{fig:app-quality-vs-context}
\end{figure}

% ============================================================
\section{Needle-in-a-Haystack 深度-位置热力图}
\label{sec:app-needle-heatmaps}
% ============================================================

图~\ref{fig:app-needle-depth-grid} 将 4K、8K、16K 和 32K 的 Needle 深度热力图
合并为一个共享色标的 2$\times$2 大图，
便于直接观察 INT4 系列模式从短上下文到长上下文的渐进退化。
图~\ref{fig:app-needle-exact} 进一步以精确匹配（exact match）这一更严格标准
展示了各模式随上下文长度的通过率变化。
热力图横轴为 needle 在 haystack 中的相对位置（深度百分比），
纵轴对应量化模式，颜色表示通过率。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/needle_depth_grid.pdf}
  \caption{Needle-in-a-Haystack 深度-位置热力图总览。
  四个面板分别对应 4K、8K、16K 和 32K 上下文长度，并共享同一色标。
  INT4 系列量化模式在 32K 长上下文下出现显著的通过率退化，
  尤其在 haystack 深层位置（60\%--90\% 区间），
  说明量化噪声对远距离注意力检索的影响更为严重。}
  \label{fig:app-needle-depth-grid}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/needle_exact_match_vs_context.pdf}
  \caption{Needle-in-a-Haystack 精确匹配率随上下文长度的变化。
  精确匹配（exact match）采用比通过率更严格的评判标准。}
  \label{fig:app-needle-exact}
\end{figure}

% ============================================================
\section{推理效率与扩展性补充图表}
\label{sec:app-efficiency-plots}
% ============================================================

本节提供推理延迟、吞吐量和显存的补充可视化。
图~\ref{fig:app-tpot-gain} 展示了各量化模式相对 FP16 的 TPOT 变化，
图~\ref{fig:app-throughput-dashboard} 将总吞吐量、单序列吞吐量与 prefill 吞吐量
压缩为统一的吞吐 dashboard，
图~\ref{fig:app-memory-dashboard} 则合并展示 KV Cache 显存和总峰值显存
随 batch size 的变化，以减少重复图例和页面跳转。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/latency_tpot_gain_vs_fp16.pdf}
  \caption{各量化模式相对 FP16 基线的 TPOT 变化（Qwen2.5-1.5B-Instruct，batch=1）。
  正值表示加速（TPOT 降低），负值表示减速。
  在 batch=1 的单序列解码场景下，量化操作的计算开销尚未被批处理摊薄，
  因此所有量化模式的 TPOT 均高于 FP16 基线。
  INT8-Canonical 的开销（$-$75\%\textasciitilde{}$-$96\%）低于 INT8-Baseline（$-$78\%\textasciitilde{}$-$112\%），
  体现了融合核函数对反量化开销的缓解作用。}
  \label{fig:app-tpot-gain}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{figures/appendix_throughput_dashboard.pdf}
  \caption{吞吐扩展性三联图（Qwen2.5-1.5B-Instruct）。
  面板（a）展示总吞吐量，面板（b）展示单序列吞吐量，
  面板（c）展示 prefill 吞吐量。统一图例和坐标轴便于比较量化模式在 batch 扩展中的并发收益与单序列代价。}
  \label{fig:app-throughput-dashboard}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{figures/appendix_memory_dashboard.pdf}
  \caption{显存扩展性双联图（Qwen2.5-1.5B-Instruct）。
  面板（a）展示 KV Cache 显存随 batch size 的线性增长，
  面板（b）展示总峰值显存；两者共同说明量化节省会随着并发度提升而持续转化为更高的可容纳 batch。}
  \label{fig:app-memory-dashboard}
\end{figure}


% ============================================================
\section{批处理扩展：容量上限与 INT4-RoleAlign 扩展性}
\label{sec:app-batch-capacity}
% ============================================================

表~\ref{tab:app-batch} 合并呈现 batch 维度的两类数据：
（a）各量化模式在 seq\_len=8192 条件下的\textbf{最大可支持 batch size}；
（b）INT4-RoleAlign 在不同 batch 下的 TPOT 与 KV Cache 显存\textbf{扩展轨迹}。
两者共同反映量化的显存节省在实际批量推理中的价值。

\begin{table}[!htbp]
  \centering
  \caption{批处理扩展综合数据（Qwen2.5-1.5B-Instruct，seq\_len=8192，独占 GPU）}
  \label{tab:app-batch}
  \small
  \begin{tabular}{lccc}
    \toprule
    \multicolumn{4}{l}{\textit{（a）各量化模式最大可支持 batch size（1--32 扫描）}} \\
    \midrule
    \textbf{量化模式} & \textbf{最大成功 batch} & \textbf{测试范围} & \textbf{状态} \\
    \midrule
    fp16            & 16 & 1--16 & 完整通过 \\
    int8\_baseline  & 24 & 1--32 & batch=32 OOM \\
    int8\_ours      & 32 & 1--32 & 完整通过 \\
    int4\_baseline  & 16 & 1--16 & 完整通过 \\
    int4\_fused     & 32 & 1--32 & 完整通过 \\
    int4\_ours      & 32 & 1--32 & 完整通过 \\
    \midrule
    \multicolumn{4}{l}{\textit{（b）INT4-RoleAlign 批处理扩展轨迹（FP16 vs INT4-RA 对照）}} \\
    \midrule
    \textbf{Batch} & \textbf{TPOT FP16 / INT4-RA (ms)} & \textbf{KV FP16 / INT4-RA (MB)} & \textbf{KV 压缩比} \\
    \midrule
    1  & 24.78 / 60.18  & 227  / 60  & 3.78$\times$ \\
    4  & 24.65 / 73.17  & 910  / 242 & 3.76$\times$ \\
    8  & 24.21 / 93.07  & 1820 / 484 & 3.76$\times$ \\
    16 & 24.23 / 134.46 & 3640 / 968 & 3.76$\times$ \\
    \bottomrule
  \end{tabular}

  \vspace{2pt}
  {\footnotesize 注：（a）INT8-Canonical 与 INT4 融合模式均可支持 batch=32，INT8-Baseline 在 batch=32 时因 torch\_ref 中间 FP16 张量分配 OOM；Triton 融合核函数将可服务 batch 从 24 提升到 32（$+$33\%）。（b）KV 压缩比约 3.76$\times$ 恒定（由位宽决定），FP16 TPOT 随 batch 几乎不变（24.2--24.8~ms），INT4-RA TPOT 因 torch\_ref 解码路径在 batch$>$1 时需要逐序列反量化而线性增长（60$\to$134~ms）；batch=16 时 INT4-RA KV 仅占 968~MB（vs FP16 3640~MB），节省的 2672~MB 显存可容纳更多并发请求。}
\end{table}

% [实验预期验证表和校准产物版本说明已移除——属于工程文档而非学术论文内容]

% ============================================================
\section{INT4 量化精度敏感性补充实验}
\label{sec:app-eng066}
% ============================================================

表~\ref{tab:app-eng066} 报告了确保 INT4 scale 全链路以 float32 精度维护后的对照实验结果，
旨在分析运行时 scale 精度对 INT4 量化质量的影响。

\begin{table}[!htbp]
  \centering
  \caption{INT4 量化 scale 精度敏感性实验
    （seq\_len=32K, seeds=\{1234,1235,1236\}, greedy decoding）}
  \label{tab:app-eng066}
  \begin{tabular}{llcccc}
    \toprule
    \textbf{模型} & \textbf{模式} & \textbf{PPL} & \textbf{Needle (\%)} & \textbf{校准} & \textbf{Scale dtype} \\
    \midrule
    \multirow{2}{*}{Qwen2.5-1.5B} & int4\_baseline & 19.65 & 0 & percentile & float32 \\
                                    & int4\_ours     & 22.80 & 0 & kl\_attn   & float32 \\
    \midrule
    \multirow{2}{*}{Qwen2.5-7B}   & int4\_baseline & 86.56 & --- & percentile & float32 \\
                                    & int4\_ours     & 52.19 & --- & kl\_attn   & float32 \\
    \midrule
    \multirow{2}{*}{LLaMA-3.1-8B} & int4\_baseline & 6.97  & 100 & percentile & float32 \\
                                    & int4\_ours     & 7.64  & 100 & kl\_attn   & float32 \\
    \bottomrule
  \end{tabular}
  \begin{tablenotes}
    \footnotesize
    \item 注：所有 scale 参数均以 float32 精度维护。
    7B Needle 因资源限制未在本轮运行（标记 ---）。
    1.5B Needle 0\% 为该模型在 32K INT4 下的固有限制。
    7B baseline PPL=86.56 与主实验数据（85.49）量级一致，
    反映了该模型对 INT4 percentile 校准的固有敏感性。
  \end{tablenotes}
\end{table}

\textbf{主要发现。}
（1）确保 scale 以 float32 精度维护后，Qwen2.5-7B 的 INT4 PPL 从约 86 降至 52，
改善幅度约 40\%，表明运行时 scale 精度是影响 INT4 量化质量的重要因素。
（2）LLaMA-3.1-8B 的 Needle 通过率在 baseline 和 ours 中均恢复至 100\%，
说明 scale 精度的改善同时惠及了两种校准策略。
（3）Qwen2.5-1.5B 和 LLaMA-3.1-8B 的 PPL 中 ours 仍略逊于 baseline，
表明 KL 校准在 INT4 下的收益不具备跨模型一致性，
进一步确认了第~\ref{subsec:exp-int4-honest}~节关于 INT4 量化局限性的分析。

% ============================================================
\section{K/V 精度消融与 MixedKV 完整数据}
\label{sec:app-kv-ablation-full}% orphan ref resolved (Phase 3 §4.2)
% ============================================================

本节补充正文第~\ref{sec:exp-kv-sensitivity}~节中因数据可比性限制
而未在主表中展示的完整数据。

\textbf{Mistral-7B LongBench 数据说明。}
Mistral-7B-Instruct-v0.3 的 LongBench 评测结果在所有量化配置下均为 1.0，
包括 K/V 消融的三种配置（K-only, V-only, K4V8）
和 MixedKV 对比的三种方法（FP16, KIVI-style, MixedKV）。
该结果源于 Mistral 模型的 LongBench 评测管线使用了合成数据源，
评分不反映模型的实际长文本理解能力，
因此在正文的跨模型 LongBench 对比中排除了 Mistral 的数据。
需强调的是，这一 artifact 影响的仅是 LongBench 评测本身，
而非量化方法。Mistral-7B 在 PPL、Needle 和 RULER 上的评测均正常有效。

在三个主评测模型上，K-only（K@INT8 + V@INT4）与 V-only（K@FP16 + V@INT4）的
LongBench 宏平均 F1（$\times$100）分别为：Qwen2.5-1.5B 上 $4.57 \pm 0.01$ 与 $4.54 \pm 0.04$，
Qwen2.5-7B 上 $4.10 \pm 0.11$ 与 $3.85 \pm 0.02$，
LLaMA-3.1-8B 上 $7.31 \pm 0.43$ 与 $6.21 \pm 0.66$，
均显著高于 K4V8（K@INT4）配置（分别为 $2.58 \pm 0.08$、$0.80 \pm 0.05$、$8.92 \pm 0.79$）。
该趋势与正文第~\ref{sec:exp-kv-sensitivity}~节报告的 Key 主导退化结论一致。
Mistral-7B 因 LongBench 评测使用合成数据源，所有消融配置得分恒为 1.0，
不纳入跨模型分析。

% ============================================================
\section{INT4 量化噪声与序列长度交互效应的形式化推导}
\label{sec:app-sqnr-derivation}
% ============================================================

本节提供正文第~\ref{subsec:exp-int4-honest}~节中量化步长与序列长度交互效应的完整推导。

Needle-in-a-Haystack 任务的检索成功条件可形式化为：
目标位置 $t$ 的 attention logit 与最强干扰位置的差值
$\delta = z_t - \max_{i \neq t} z_i$
（其中 $z_i = \bq^\top \bk_i / \sqrt{d_k}$）
需在量化后仍保持正值，使 softmax 将最大概率分配给目标位置。

当 Key 矩阵经 INT4 量化后，每个 logit $z_i$ 受到量化噪声扰动 $\epsilon_i$，
其方差约为：
\begin{equation}
  \text{Var}(\epsilon_i) \approx \|\bq\|^2 \cdot \sigma_{q,4}^2 / d_k
\end{equation}
其中 $\sigma_{q,4}^2 = \Delta_4^2 / 12$ 为 INT4 量化的逐元素噪声方差。

随序列长度 $n$ 增大，$\max_{i \neq t} (z_i + \epsilon_i)$ 的期望值
以 $O(\sqrt{2 \ln n} \cdot \sigma_\epsilon)$ 的速率增长（极值统计理论），
而目标信号 $\delta$ 不随 $n$ 增大。
因此存在临界序列长度 $n^*$，满足：
\begin{equation}
  \delta \approx \sqrt{2 \ln n^*} \cdot \sigma_\epsilon
\end{equation}
当 $n > n^*$ 时，量化噪声以高概率淹没目标信号，检索失败。

由于 INT4 的逐元素噪声方差 $\sigma_{q,4}^2 / \sigma_{q,8}^2 = (255/15)^2 = 289$，
INT4 的 $n^*$ 远小于 INT8，从理论上解释了 INT4 在 1.5B 模型上
从短上下文到 32K 的 Needle 通过率急剧下降趋势。


% ============================================================
\section{第四章补充材料}
\label{sec:app-ch4-supp}
% ============================================================

\subsection{MixedKV 跨模型逐模型详细分析}
\label{subsec:app-mixedkv-detail}

表~\ref{sec:app-kv-ablation-full}（第四章）的 MixedKV 结果在各模型上呈现差异化模式，以下给出逐模型详细分析。

\textbf{LLaMA-3.1-8B（$H_{kv}=8$）：}
MixedKV 全指标不劣于 FP16：PPL 6.75（甚至低于 FP16 的 6.92），Needle 100\%，RULER 33.28\%（高于 FP16 的 31.48\%），LongBench 7.42\%（接近 FP16 的 7.92\%）。这是 GQA 噪声稀释效应最显著的模型：8 个 KV 头的独立量化噪声经统计平均后有效降低了整体误差。

\textbf{Qwen2.5-1.5B（$H_{kv}=2$）：}
PPL 退化 4.9\%（9.37 vs 8.93），超出非劣性阈值，但 Needle 保持 100\%，RULER 24.87\% 与 FP16 可比。仅 2 个 KV 头提供的噪声稀释有限，但长上下文检索指标的保持表明 MixedKV 仍具实用性。

\textbf{Qwen2.5-7B（$H_{kv}=4$）：}
Needle 退化至 72.4\%（FP16 为 100\%），是四个模型中唯一出现 Needle 退化的情况。4 个 KV 头的中等噪声稀释可能在特定 Key 通道分布下不足以抑制 INT4 量化噪声对注意力排序的破坏。

\textbf{Mistral-7B-Instruct-v0.3（$H_{kv}=8$）：}
PPL 5.20（vs FP16 5.19）和 RULER 53.67\%（vs 53.98\%）均接近 FP16 基线，进一步验证了高 GQA 头数模型对 MixedKV 方案的良好适配性。Mistral-7B 的 RULER 整体通过率（53.98\%）显著高于其他模型（24\%--31\%），可能与其更大的原生上下文窗口有关。


% ============================================================
\section{量化评估粒度鲁棒性}
\label{sec:app-chunksize}
% ============================================================

本节验证 BA 校准优势在全量化协议下是否仍然成立。
前述各节采用 chunk\_size=128 协议（chunk 内 KV 为 FP16，仅历史 KV 量化）。
chunk\_size=1 是最严格的全量化协议，每步所有历史 KV 均经量化存取，
更接近实际部署的 streaming decode 场景。

表~\ref{tab:app-chunksize} 呈现了 Qwen2.5-1.5B-Instruct 上
三种 chunk\_size 下 FP16、INT4-RoleAlign 和 KIVI-style INT4 的 PPL 对比。

\begin{table}[!htbp]
  \centering
  \caption{量化评估粒度敏感性（Qwen2.5-1.5B-Instruct，PPL，seed=1234）}
  \label{tab:app-chunksize}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{lccc}
    \toprule
    \textbf{配置} & \textbf{cs=128} & \textbf{cs=8} & \textbf{cs=1} \\
    \midrule
    FP16                     & 9.31            & 9.31          & 9.31          \\
    INT8-Canonical                & 8.95            & 9.34 (+0.3\%$^{\dagger}$) & 9.27 (+3.6\%$^{\dagger}$) \\
    \textbf{INT4-RoleAlign}  & \textbf{10.58} (+13.7\%) & \textbf{62.10} (+567\%) & $>$10{,}000$^{\star}$ \\
    KIVI-style INT4          & 10.43 (+12.0\%) & 60.54 (+550\%) & 10{,}332 (+101{,}000\%)$^{\star}$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\paragraph{表注与结果分析。}
括号内为相对 FP16 的 PPL 退化率，cs = chunk\_size，FP16 不含量化噪声故 PPL 不随 cs 变化。
$^{\dagger}$ INT8-Canonical 的 cs=128 PPL 为 8.95（低于 FP16，因 max\_length/stride 配置不同），
全量化（cs=1）退化 +3.6\%，自适应保护机制部分缓解了超出校准范围的激活值裁剪。
$^{\star}$ \textbf{重要 caveat（KIVI 基线的简化实现）}：
本表中 KIVI-style 的 cs=1 PPL=10{,}332 来自本文的简化实现，
该实现\emph{未启用} KIVI 原论文中的 FP16 Residual Buffer（$R=128$）与 residual-group-scale 更新组件。
KIVI 原论文的 Residual Buffer 为 streaming decode（cs=1）场景专门设计，
通过将最近 $R$ 个 token 保持 FP16 来缓解 per-channel K 在极小解码粒度下的 scale range 失配问题；
若启用 $R=128$ 的 Residual Buffer，
KIVI 原论文在 streaming 下的 PPL 失效应显著缓解。
因此表中 cs=1 的 10{,}332 数字\emph{不代表} KIVI 原论文方法在 streaming 设置下的性能上界，
仅说明``不含 Residual Buffer 的 per-channel K + per-token V 非对称格式''
在 cs=1 下的失效是本文 KIVI-style 基线与 INT4-RoleAlign 共享的边界。
\textbf{per-channel K Scale 在 cs=1 下的共同崩溃}（不含 Residual Buffer 时）：
两种方法共享同一根因——per-channel K Scale 在 cs=1 下仅基于单 token 建立，
scale 范围极窄，后续 token 几乎 100\% 超出 clamp 范围。
INT8-Canonical 的 256 个量化级别为 scale 范围外的值提供了更大容错空间，故退化相对温和。
上述 cs=1 边界仅在 Qwen2.5-1.5B 单模型单种子条件下验证
（见第~\ref{sec:conclusion-limitations}~节），
并受上述 KIVI 简化实现 caveat 所限定。

% ============================================================
\section{官方 LongBench 对照验证}
\label{sec:app-longbench-official}
% ============================================================

前述各节的 LongBench 评测均使用自行实现的确定性合成数据源，
评分只反映方法间的相对差异，绝对值不可与社区结果直接对比。
为验证合成评测结论在真实数据上的泛化性，
本节选取 LongBench~\cite{bai2024longbench} 官方数据集中
3 个代表性子任务进行对照实验：
NarrativeQA（单文档 QA，F1）、HotpotQA（多文档 QA，F1）和
GovReport（摘要，Rouge-L）。

\paragraph{实验设置与结果。}
模型为 Qwen2.5-1.5B-Instruct，上下文截断至 32K，seed=1234，
每任务最多 50 个样本（从官方 HuggingFace 数据源加载，非本文合成）。
量化配置为 INT8-Canonical 主线配置（KL 行为对齐校准 + 自适应静态 Scale + Triton 融合核函数）。
三任务 F1/Rouge-L 分数（$\times$100）如下：
NarrativeQA 上 FP16 为 7.07，INT8-Canonical 为 6.64（$\Delta = -0.43$ pp）；
HotpotQA 上 FP16 为 4.90，INT8-Canonical 为 5.21（$\Delta = +0.31$ pp）；
GovReport 上 FP16 为 9.21，INT8-Canonical 为 9.29（$\Delta = +0.08$ pp）；
宏平均 FP16=7.06 vs INT8-Canonical=7.05，差异仅 0.01 pp，所有单任务差异均 $<$0.5 pp。
这种任务间的随机波动符合量化噪声不偏不倚的预期行为，
初步回应了``合成数据源可能高估量化保真度''的质疑——
至少在 INT8 精度下，合成评测与官方评测的结论方向一致。

\textbf{局限}：本验证限于 1.5B 单模型、3 个任务、单种子，
尚不构成全面的统计检验；跨模型、INT4 精度及更多任务的官方验证为后续工作。


% ============================================================
\section{逐头温度校正机制详细推导}
\label{sec:app-invtau-diagnostic}% story §16.18 diagnostic note
\label{sec:app-invtau-detail}

本节给出逐头温度校正 $\tau^{-1}$ 的核心推导。

\subsection{量化平坦化与温度校正}

量化误差使 logits $\bq\hat{\bK}^T / \sqrt{d_k}$ 受到近似均匀噪声扰动，
softmax 输出趋于平坦化（熵增），对长距离检索尤为有害。
不同层和头受影响程度不同，构成逐头校正的动机。
校正后的注意力计算为：
\begin{equation}
  \bm{p}_{\text{corrected}} = \softmax\!\left(\frac{\bq\hat{\bK}^T \cdot \tau^{-1}_{l,h}}{\sqrt{d_k}}\right)
  \label{eq:app-corrected-attn}
\end{equation}
$\tau^{-1} > 1$ 锐化分布以补偿平坦化，
$\tau^{-1} = 1$ 退化为标准基线。

\subsection{Q 预缩放等价性}

实现上 $\tau^{-1}$ 通过预缩放 Query 向量等价实现：
\begin{equation}
  \text{Attn}(\bQ \cdot \tau^{-1},\; \hat{\bK},\; \hat{\bV})
  = \softmax\!\left(\frac{\bQ \hat{\bK}^T \cdot \tau^{-1}}{\sqrt{d_k}}\right) \hat{\bV}
  \label{eq:app-q-prescale}
\end{equation}
$\bQ'_{l,h} = \bQ_{l,h} \cdot \tau^{-1}_{l,h}$（逐头广播），
无需修改注意力核函数签名，无额外 kernel launch。

% [数据溯源表已移除——属于工程数据管理文档而非学术论文内容]

\subsection{7B KL vs MSE 校准目标趋同验证}
\label{subsec:app-7b-kl-mse}

表~\ref{tab:app-7b-kl-mse} 提供第~\ref{sec:exp-main}~节
``校准目标灵敏度的规模依赖''所引用的 7B 数据溯源。
在 Qwen2.5-7B-Instruct 上，KL 与 MSE 两种校准目标搜索到
完全相同的 percentile 参数（$k\_\text{percentile}=100.0$,
$v\_\text{percentile}=99.9$），PPL 结果在贪婪解码下逐位一致。

\begin{table}[!htbp]
  \centering
  \caption{7B KL vs MSE 校准对比数据溯源}
  \label{tab:app-7b-kl-mse}
  \small
  \begin{tabular}{lccccl}
    \toprule
    \textbf{校准目标} & $k\_\text{pct}$ & $v\_\text{pct}$ & \textbf{PPL} & \textbf{Seeds} & \textbf{Calib 文件} \\
    \midrule
    KL (RoleAlign v3) & 100.0 & 99.9 & 7.1121 & 3 & \code{kv\_calib\_rolealign\_7b\_v3.json} \\
    MSE               & 100.0 & 99.9 & 7.1121 & 3 & \code{kv\_calib\_mse\_7b\_int4\_rolealign\_v1.json} \\
    FP16 (baseline)   & ---   & ---  & 6.7097 & 1 & --- \\
    \bottomrule
  \end{tabular}
  \begin{tablenotes}
    \scriptsize
    \item 贪婪解码下 PPL 为确定量，多 seed 验证一致性而非统计方差。
    7B ($H_{kv}=4$) 趋同与 1.5B ($H_{kv}=2$) 分歧形成对比，
    表明校准 profile 的平坦度随模型冗余度增大。
  \end{tablenotes}
\end{table}

% [实验预期验证状态总结已移除——核心结论已在正文第四章讨论中呈现]


% ============================================================
\section{附录 A：Prompt-Adaptive Selector 在 Llama-3.1-8B 上的 5-Task Matrix}
\label{sec:app-prompt-adaptive-8b}
% ============================================================

本节给出 prompt-adaptive selector 在 Llama-3.1-8B-Instruct 上的完整 5-task 评测，
作为第~\ref{sec:conclusion-future}~节所列 per-prompt routing 方向的实证起点。
评测协议沿用第~\ref{sec:exp-setup}~节的 5 个 LongBench 任务（NarrativeQA、HotpotQA、GovReport、DuReader、LCC），
对比 fixed-$k$、auto-$k$ 与 prompt-adaptive 三种预算选择策略在 clean-provenance pin \code{ddada19} 下的表现。

\begin{table}[!htbp]
  \centering
  \small
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{l ccc c}
    \toprule
    \textbf{Task} & \textbf{fixed-$k$} & \textbf{auto-$k$} & \textbf{prompt-adaptive} & \textbf{Best} \\
    \midrule
    NarrativeQA  & 9.73 & \textbf{10.77} & 9.73 & auto-$k$ \\
    HotpotQA     & \textbf{8.16} & 7.92 & 7.92 & fixed-$k$ \\
    GovReport    & 9.48 & \textbf{9.72} & \textbf{9.72} & auto-$k$ / prompt-adaptive (tie) \\
    DuReader     & \textbf{12.16} & 9.90 & 9.90 & fixed-$k$ \\
    LCC          & 10.61 & 10.96 & \textbf{11.36} & prompt-adaptive \\
    \midrule
    \textit{Mean (5 tasks)} & \textit{\textbf{10.03}} & \textit{9.85} & \textit{9.73} & fixed-$k$ \\
    \bottomrule
  \end{tabular}
  \caption{Llama-3.1-8B-Instruct 上 fixed-$k$、auto-$k$ 与 prompt-adaptive 三种预算选择策略的完整 5-task 对比
  （clean-provenance pin=\code{ddada19}；每个 task 50 个样本；分数为 F1 / Rouge-L / EditSim 各自原生尺度）。}
  \label{tab:app-prompt-adaptive-8b}
\end{table}

表~\ref{tab:app-prompt-adaptive-8b} 给出的读数具有双重性质。
在 task-level 视图下，prompt-adaptive 仅在 LCC 上取得独立最优
（$11.36$，较 auto-$k$ 高 $0.40$ 分），其余四个任务要么与 auto-$k$ 打平，要么落后于当前最优策略；
five-task mean 略低于 fixed-$k$（$9.73$ vs $10.03$），表明在当前 task-bucket 级的 selector 实现下，
prompt-adaptive 尚未取得跨任务的 systematic 优势。
然而 LCC 上的独立正向信号并非偶然——该任务是代码补全（code completion）类型，
与其它四个以问答和摘要为主的任务在输入分布上存在明确差异；
prompt-adaptive 在这类 task-domain 边界处的优势，
为更精细的 per-prompt routing（第~\ref{sec:conclusion-future}~节）提供了具体的起点。

% ============================================================
\section{附录 B：Qwen2.5-1.5B 与 Qwen2.5-7B 上的 Prompt-Adaptive Supplementary Data}
\label{sec:app-prompt-adaptive-offprotocol}
% ============================================================

附录~\ref{sec:app-prompt-adaptive-8b} 的 8B 评测之外，
我们在 Qwen2.5-1.5B 与 Qwen2.5-7B 上以相同的 5-task 协议执行了 supplementary 评测，
用于观察三种预算选择策略在更小规模模型上的 ordering。
这些结果属于 supplementary scope：它们的协议与正文主矩阵严格一致，
但未与主矩阵的 Level-5 clean-provenance 数据一起用于第四章的 final-ready claims；
它们的作用是为 per-prompt routing 的未来工作提供规模外推的参照。

\begin{table}[!htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{l l ccc}
    \toprule
    \textbf{Model} & \textbf{Task} & \textbf{fixed-$k$} & \textbf{auto-$k$} & \textbf{prompt-adaptive} \\
    \midrule
    \multirow{6}{*}{Qwen2.5-1.5B}
      & NarrativeQA  & \textbf{6.94} & 6.77 & \textbf{6.94} \\
      & HotpotQA     & \textbf{4.87} & 4.46 & 4.46 \\
      & GovReport    & 9.10 & \textbf{9.12} & \textbf{9.12} \\
      & DuReader     & 9.30 & \textbf{12.64} & \textbf{12.64} \\
      & LCC          & 13.38 & \textbf{14.00} & 12.07 \\
      \cmidrule(l){2-5}
      & \textit{Mean} & \textit{8.72} & \textit{\textbf{9.40}} & \textit{9.04} \\
    \midrule
    \multirow{6}{*}{Qwen2.5-7B}
      & NarrativeQA  & \textbf{6.85} & 6.46 & \textbf{6.85} \\
      & HotpotQA     & 4.93 & \textbf{5.26} & \textbf{5.26} \\
      & GovReport    & 8.58 & \textbf{8.81} & \textbf{8.81} \\
      & DuReader     & 9.47 & \textbf{9.71} & \textbf{9.71} \\
      & LCC          & 16.56 & \textbf{18.35} & 16.43 \\
      \cmidrule(l){2-5}
      & \textit{Mean} & \textit{9.28} & \textit{\textbf{9.72}} & \textit{9.41} \\
    \bottomrule
  \end{tabular}
  \caption{Qwen2.5-1.5B 与 Qwen2.5-7B 上 fixed-$k$、auto-$k$ 与 prompt-adaptive 三种策略的
  supplementary 5-task 评测。每个 task 50 个样本；\textbf{加粗}为该任务的最佳分数。}
  \label{tab:app-prompt-adaptive-offprotocol}
\end{table}

表~\ref{tab:app-prompt-adaptive-offprotocol} 在两个规模上呈现一致的 ordering：
auto-$k$ 的 5-task mean 均高于 fixed-$k$ 与 prompt-adaptive（1.5B 上 $9.40$ vs $8.72 / 9.04$；
7B 上 $9.72$ vs $9.28 / 9.41$），
且在多个任务（特别是 DuReader）上，auto-$k$ 与 prompt-adaptive 取得完全一致的分数——
这说明当前实现下的 prompt-adaptive selector 在多数 prompt 上退化为 auto-$k$ 的选择。
与 8B 上 prompt-adaptive 在 LCC 独立胜出的局部信号相比，
更小规模下该信号尚未复现；这一 scale-dependent 差异
与第~\ref{subsec:exp-per-model-mistral}~节讨论的 regime 观察相互印证：
AutoK 本身（其上层包装即 prompt-adaptive selector）的有效性在不同 family / scale 下存在结构性差异，
精细化 per-prompt routing 的设计空间需要先在较大规模的 LCC-like 输入分布上展开。
数据完整原始 CSV 见 \code{results/l2\_prompt\_adaptive/\{1p5b,7b\}/}。
