{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Read(*)",
      "Write(*)",
      "Edit(*)",
      "WebFetch(*)",
      "WebSearch(*)",
      "Glob(*)",
      "Grep(*)",
      "NotebookEdit(*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/check_memory_freshness.sh 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo \"$TOOL_INPUT\" | python3 -c \"import sys,json; print(json.load(sys.stdin).get('file_path',''))\" 2>/dev/null || echo ''); case \"$FILE\" in */CLAUDE.md) echo 'BLOCKED: CLAUDE.md 是只读文件' >&2; exit 2;; */experiment_sop.md) echo 'BLOCKED: experiment_sop.md 是只读文件' >&2; exit 2;; */.claude/agents/*.md) echo 'BLOCKED: agents/*.md 是只读文件' >&2; exit 2;; *) exit 0;; esac"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo \"$TOOL_INPUT\" | python3 -c \"import sys,json; print(json.load(sys.stdin).get('file_path',''))\" 2>/dev/null || echo ''); [ -n \"$FILE\" ] && echo \"$FILE\" | grep -q '\\.py$' && python3 -m py_compile \"$FILE\" 2>&1; exit 0",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo \"$TOOL_INPUT\" | python3 -c \"import sys,json; print(json.load(sys.stdin).get('file_path',''))\" 2>/dev/null || echo ''); echo \"$FILE\" | grep -qE '/src/.*\\.py$|/scripts/(eval_|calibrate_|profile_|run_|aggregate_).*\\.py$' && echo \"§5.1 审查提醒: $FILE 已修改，commit 前须通过 Codex + Sub-Agent 双重审查\"; exit 0"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "DIRTY=$(git status --porcelain -- src/ scripts/ tests/ 2>/dev/null | head -10); [ -n \"$DIRTY\" ] && printf '⚠ Session 结束时有未提交的代码修改:\\n%s\\n请确认是否需要 commit。\\n' \"$DIRTY\"; exit 0"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, os, re\n\nmarker = '.claude/.auto_iterate_active'\n\nif not os.path.exists(marker):\n    sys.exit(0)\n\ntry:\n    content = open(marker).read().strip().lower()\nexcept Exception:\n    sys.exit(0)\n\nallow_statuses = ['complete', 'completed', 'stalled', 'blocked', 'needs_input']\nfor s in allow_statuses:\n    if s in content:\n        sys.exit(0)\n\nif os.path.exists('iteration.md'):\n    with open('iteration.md', 'r') as f:\n        progress = f.read()\n    legacy_markers = [\n        'Auto-Iterate 完成报告', 'auto-iterate-complete',\n        'auto-iterate-stalled', 'auto-iterate-needs-user-input',\n        'auto-iterate-blocked', '达到迭代上限', 'iteration-cap-reached'\n    ]\n    for m in legacy_markers:\n        if m in progress:\n            sys.exit(0)\n\nprint('Auto-iterate active, objectives not yet complete. Keep going.')\nsys.exit(1)\n\""
          }
        ]
      }
    ]
  }
}
